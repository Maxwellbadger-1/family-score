---
phase: 01-foundation
plan: "03"
type: execute
wave: 2
depends_on: ["01-PLAN-01", "01-PLAN-02"]
files_modified:
  - FamilyScore/FamilyScore/Supabase.swift
  - FamilyScore/FamilyScore/FamilyScoreApp.swift
  - FamilyScore/supabase/migrations/20260515_initial_schema.sql
autonomous: false
requirements: []

must_haves:
  truths:
    - "supabase.from('families').select() gibt [] zurück (kein Fehler, kein 403) — RLS aktiv, kein Auth-User"
    - "supabase.from('weekly_summaries').select() gibt [] zurück — Tabelle existiert, RLS aktiv"
    - "Alle 6 Tabellen existieren in Supabase: families, family_members, activity_entries, category_config, family_invites, weekly_summaries"
    - "Supabase.swift ist NUR im FamilyScore App Target — nicht im Widget Extension Target"
    - "git grep -r 'supabase.co' findet keine Treffer in committed *.swift *.plist *.xcconfig Dateien"
  artifacts:
    - path: "FamilyScore/FamilyScore/Supabase.swift"
      provides: "SupabaseClient Singleton — nur App Target"
      exports: ["supabase"]
    - path: "FamilyScore/supabase/migrations/20260515_initial_schema.sql"
      provides: "Vollständiges DDL + RLS + Trigger für alle 6 Tabellen"
      contains: "alter table public.families enable row level security"
  key_links:
    - from: "FamilyScore/Config/Secrets.xcconfig"
      to: "FamilyScore/FamilyScore/Resources/Info.plist"
      via: "xcconfig Build-Setting-Injection → $(SUPABASE_URL)"
      pattern: "SUPABASE_URL_SECRET.*https://"
    - from: "FamilyScore/FamilyScore/Supabase.swift"
      to: "Supabase Cloud PostgreSQL"
      via: "SupabaseClient(supabaseURL:supabaseKey:)"
      pattern: "Bundle.main.object.*SUPABASE_URL"
    - from: "supabase/migrations/20260515_initial_schema.sql"
      to: "Supabase Cloud DB"
      via: "supabase db push"
      pattern: "create table public.families"

user_setup:
  - service: supabase
    why: "Supabase-Projekt-URL und Publishable Key müssen in Secrets.xcconfig eingetragen werden bevor App gebaut werden kann"
    env_vars:
      - name: SUPABASE_URL_SECRET
        source: "Supabase Dashboard → Project Settings → API → Project URL (Format: https://xxx.supabase.co)"
      - name: SUPABASE_KEY_SECRET
        source: "Supabase Dashboard → Project Settings → API → Project API keys → anon/publishable (sb_publishable_xxx oder anon-key)"
    dashboard_config:
      - task: "Supabase CLI installieren (falls nicht vorhanden): brew install supabase/tap/supabase"
        location: "Terminal"
      - task: "supabase login ausführen und mit Browser-Auth bestätigen"
        location: "Terminal: supabase login"
      - task: "supabase link --project-ref <project-ref> ausführen um lokales Supabase-Verzeichnis mit Projekt zu verknüpfen"
        location: "Terminal im FamilyScore/ Verzeichnis"
---

<objective>
Bindet supabase-swift v2.46.0 via SPM in das App Target ein, erstellt den SupabaseClient-Singleton, schreibt die vollständige SQL-Migration (DDL + RLS + Trigger), pusht das Schema zu Supabase Cloud, und verifiziert die Verbindung mit echtem JWT.

Purpose: Ohne Live-Schema und ohne verifizierten Swift-Client-Verbindungstest ist Phase 1 nicht abgeschlossen. SC-3 fordert explizit einen Swift-Client-Aufruf (nicht Supabase-Dashboard) als Beweis für aktives RLS. SC-4 verlangt Nachweis dass kein Secret in Git ist.

Output:
- FamilyScore/FamilyScore/Supabase.swift (SupabaseClient Singleton, nur App Target)
- FamilyScore/FamilyScore/FamilyScoreApp.swift (erweitert um verifySupabaseConnection DEBUG-Code)
- FamilyScore/supabase/migrations/20260515_initial_schema.sql (vollständiges DDL + RLS)
- Supabase Cloud: alle 6 Tabellen live mit RLS
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/01-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Voraussetzungen aus Plan 01 + Plan 02 -->
<!-- Plan 01 liefert: Config/Secrets.xcconfig (mit echten Keys befüllt), Info.plist mit $(SUPABASE_URL)/$(SUPABASE_KEY) -->
<!-- Plan 02 liefert: FamilyScoreKit Package mit appGroupIdentifier und WidgetData -->

<!-- Supabase.swift exportiert: -->
```swift
// FamilyScore/FamilyScore/Supabase.swift
import Supabase
// let supabase: SupabaseClient  (global, App Target only)
```

<!-- FamilyScoreApp.swift nutzt: -->
```swift
// verifyAppGroup() aus Plan 01
// verifySupabaseConnection() (neu in Plan 03)
// Task { await verifySupabaseConnection() } in App.init oder .task{}
```

<!-- SC-3 Verifikationsmuster (aus RESEARCH.md): -->
```swift
let families: [AnyJSON] = try await supabase
    .from("families")
    .select()
    .execute()
    .value
// Erwartet: [] (RLS aktiv, kein Auth-User → leer, kein Fehler)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: supabase-swift SPM-Package hinzufügen + Supabase.swift erstellen</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Abschnitt "Standard Stack" → Installation SPM, Abschnitt "Pattern 2: Secrets.xcconfig" → Supabase.swift Code)
    - .planning/phases/01-foundation/01-PATTERNS.md (Abschnitt "FamilyScore/Supabase.swift (singleton-client)")
    - .planning/phases/01-foundation/01-01-SUMMARY.md (prüfen ob Config/Secrets.xcconfig mit echten Keys befüllt)
    - CLAUDE.md (Abschnitt "Supabase" → SDK NUR im Hauptapp-Target)
  </read_first>
  <files>
    FamilyScore/FamilyScore/Supabase.swift,
    FamilyScore/FamilyScore/FamilyScoreApp.swift
  </files>
  <action>
**Schritt 1: supabase-swift via SPM hinzufügen (in Xcode auf Mac):**
```
File > Add Package Dependencies...
URL: https://github.com/supabase/supabase-swift
Version Rule: Up to Next Major Version: 2.46.0
Product to add: Supabase
Target: FamilyScore (App) ONLY
```

**KRITISCH:** Im Dialog "Add to Target" NUR `FamilyScore` auswählen. `FamilyScoreWidgetExtension` DARF NICHT ausgewählt werden.

Nach dem Hinzufügen sofort prüfen:
```bash
grep -A3 "FamilyScoreWidgetExtension" FamilyScore/FamilyScore.xcodeproj/project.pbxproj | grep -i "supabase"
# Erwartet: 0 Zeilen — Supabase darf nicht im Widget Extension Target erscheinen
```

**Schritt 2: FamilyScore/FamilyScore/Supabase.swift erstellen:**

```swift
// FamilyScore/Supabase.swift
// Target Membership: FamilyScore (App) ONLY — NIEMALS Widget Extension!
// Source: Supabase iOS Quickstart docs
import Supabase

let supabase = SupabaseClient(
    supabaseURL: URL(string: Bundle.main.object(
        forInfoDictionaryKey: "SUPABASE_URL") as! String)!,
    supabaseKey: Bundle.main.object(
        forInfoDictionaryKey: "SUPABASE_KEY") as! String
)
```

**KRITISCH:** In Xcode prüfen dass Supabase.swift Target Membership NUR `FamilyScore` (Häkchen), NICHT `FamilyScoreWidgetExtension`.

**Schritt 3: FamilyScoreApp.swift um verifySupabaseConnection erweitern:**

Die bestehende `FamilyScoreApp.swift` (aus Plan 01) um den Supabase-Verbindungstest ergänzen:

```swift
import SwiftUI

@main
struct FamilyScoreApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}

#if DEBUG
func verifyAppGroup() {
    let suite = "group.com.familyscore"
    guard let defaults = UserDefaults(suiteName: suite) else {
        assertionFailure("App Group UserDefaults konnte nicht initialisiert werden: \(suite)")
        return
    }
    let testKey = "phase1_verification"
    defaults.set(true, forKey: testKey)
    defaults.synchronize()
    let result = defaults.bool(forKey: testKey)
    print("[Phase1] App Group Test: \(result ? "PASS" : "FAIL")")
    assert(result, "App Group FAIL — Developer Portal prüfen!")
}

func verifySupabaseConnection() async {
    do {
        // Test 1: Verbindung (unauthentifiziert — anon key, kein user)
        // Mit RLS: families gibt [] zurück (kein Fehler, kein 403)
        let families: [AnyJSON] = try await supabase
            .from("families")
            .select()
            .execute()
            .value
        print("[Phase1] Supabase Connection PASS — families returned \(families.count) rows (expected 0 with RLS, no auth)")

        // Test 2: weekly_summaries Tabelle existiert
        let summaries: [AnyJSON] = try await supabase
            .from("weekly_summaries")
            .select()
            .execute()
            .value
        print("[Phase1] weekly_summaries table exists PASS — \(summaries.count) rows")

        // Test 3: RLS-Verhalten bestätigen
        print("[Phase1] RLS appears active — anon user sees 0 rows (correct RLS behavior)")
        print("[Phase1] NOTE: Vollstaendige RLS-Verifikation erfordert Auth (Phase 2)")

    } catch {
        print("[Phase1] Supabase Connection FAIL: \(error)")
        assertionFailure("Supabase connection failed: \(error)")
    }
}
#endif
```

ContentView.swift (aus Plan 01) um Supabase-Verifikation erweitern:

```swift
import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "figure.2.and.child.holdinghands")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Family Score")
                .font(.title)
        }
        .padding()
        .onAppear {
            #if DEBUG
            verifyAppGroup()
            #endif
        }
        .task {
            #if DEBUG
            await verifySupabaseConnection()
            #endif
        }
    }
}
```

**Vor dem Build: Secrets.xcconfig mit echten Werten befüllen**
(Falls noch nicht in Plan 01 eingetragen — aus Supabase Dashboard kopieren):
```
SUPABASE_URL_SECRET = https://xxxxxxxxxxxxx.supabase.co
SUPABASE_KEY_SECRET = sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
```
  </action>
  <verify>
    ```bash
    # Supabase.swift existiert und enthält SupabaseClient
    grep "SupabaseClient" FamilyScore/FamilyScore/Supabase.swift
    # Erwartet: Treffer

    # Bundle.main.object Pattern (kein hardcoded Key)
    grep "Bundle.main.object" FamilyScore/FamilyScore/Supabase.swift
    # Erwartet: mindestens 2 Treffer (URL + Key)

    # Kein hardcoded Supabase-Key in Swift-Dateien
    grep -r "supabase.co" FamilyScore/FamilyScore/ --include="*.swift"
    # Erwartet: 0 Zeilen

    # Kein Supabase im Widget Extension Target
    grep -A5 "FamilyScoreWidgetExtension" FamilyScore/FamilyScore.xcodeproj/project.pbxproj \
      | grep -i "supabase" | wc -l
    # Erwartet: 0

    # Build
    xcodebuild \
      -project FamilyScore/FamilyScore.xcodeproj \
      -scheme FamilyScore \
      -destination "platform=iOS Simulator,name=iPhone 16" \
      build 2>&1 | tail -3
    # Erwartet: BUILD SUCCEEDED
    ```
  </verify>
  <acceptance_criteria>
    - `FamilyScore/FamilyScore/Supabase.swift` existiert
    - Datei enthält `let supabase = SupabaseClient(`
    - Datei enthält `Bundle.main.object(forInfoDictionaryKey: "SUPABASE_URL")`
    - Datei enthält `Bundle.main.object(forInfoDictionaryKey: "SUPABASE_KEY")`
    - `grep -r "supabase.co" FamilyScore/FamilyScore/ --include="*.swift"` gibt 0 Zeilen aus (kein hardcoded Key)
    - `grep "Supabase" FamilyScore/FamilyScore.xcodeproj/project.pbxproj | grep -i widget` gibt 0 Zeilen aus
    - `xcodebuild ... build` gibt `BUILD SUCCEEDED` aus
    - `FamilyScoreApp.swift` enthält `verifySupabaseConnection()` Funktion
  </acceptance_criteria>
  <done>supabase-swift v2.46.0 nur im App Target. Supabase.swift Singleton mit xcconfig-Injection. Kein hardcoded Key. Build erfolgreich.</done>
</task>

<task type="auto">
  <name>Task 2: SQL-Migration erstellen (vollständiges DDL + RLS + Trigger)</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Abschnitt "Supabase Schema (vollständiges DDL)" Zeilen 349-555 UND Abschnitt "Supabase RLS Policies (vollständig)" Zeilen 560-708)
    - .planning/phases/01-foundation/01-PATTERNS.md (Abschnitt "supabase/migrations/20260515_initial_schema.sql (db-migration)")
    - .planning/phases/01-foundation/01-RESEARCH.md (Abschnitt "Common Pitfalls" — Pitfall 2 RLS, Pitfall 5 Trigger)
  </read_first>
  <files>FamilyScore/supabase/migrations/20260515_initial_schema.sql</files>
  <action>
Verzeichnis `FamilyScore/supabase/migrations/` erstellen.

`FamilyScore/supabase/migrations/20260515_initial_schema.sql` mit dem vollständigen DDL aus 01-RESEARCH.md erstellen.

**Die Datei muss exakt diese Abschnitte enthalten (vollständig aus RESEARCH.md übernehmen):**

```sql
-- =============================================================================
-- Family Score: Initial Schema
-- Phase 1 Foundation
-- =============================================================================

-- Enable required extensions
create extension if not exists "pgcrypto";  -- gen_random_uuid(), gen_random_bytes()

-- =============================================================================
-- TABLE: families
-- =============================================================================
create table public.families (
  id          uuid primary key default gen_random_uuid(),
  name        text not null,
  created_at  timestamptz not null default now(),
  created_by  uuid references auth.users(id) on delete set null
);

-- =============================================================================
-- TABLE: family_members (erweitertes Profil, 1:1 mit auth.users)
-- =============================================================================
create table public.family_members (
  id            uuid primary key references auth.users(id) on delete cascade,
  family_id     uuid references public.families(id) on delete set null,
  display_name  text not null,
  avatar_color  text not null default '#4A90D9',
  role          text not null default 'adult'
                  check (role in ('admin', 'adult', 'child')),
  created_at    timestamptz not null default now(),
  updated_at    timestamptz not null default now()
);

create or replace function public.handle_new_user()
returns trigger
language plpgsql
security definer
set search_path = ''
as $$
begin
  insert into public.family_members (id, display_name)
  values (new.id, coalesce(new.raw_user_meta_data->>'full_name', 'New Member'));
  return new;
end;
$$;

create trigger on_auth_user_created
  after insert on auth.users
  for each row execute procedure public.handle_new_user();

-- =============================================================================
-- TABLE: category_config
-- =============================================================================
create table public.category_config (
  id            uuid primary key default gen_random_uuid(),
  family_id     uuid not null references public.families(id) on delete cascade,
  name          text not null,
  icon          text,
  color         text,
  point_weight  numeric(5,2) not null default 1.0,
  is_enabled    boolean not null default true,
  sort_order    int not null default 0,
  created_at    timestamptz not null default now(),
  unique (family_id, name)
);

-- =============================================================================
-- TABLE: activity_entries (append-only, Score NIEMALS als mutabler Wert)
-- =============================================================================
create table public.activity_entries (
  id                uuid primary key default gen_random_uuid(),
  family_id         uuid not null references public.families(id) on delete cascade,
  user_id           uuid not null references auth.users(id) on delete cascade,
  category_id       uuid not null references public.category_config(id),
  duration_minutes  int not null check (duration_minutes > 0),
  points            numeric(8,2) not null check (points >= 0),
  title             text,
  logged_at         timestamptz not null default now(),
  created_at        timestamptz not null default now()
);

create index activity_entries_family_user_logged
  on public.activity_entries(family_id, user_id, logged_at desc);

-- =============================================================================
-- TABLE: family_invites
-- =============================================================================
create table public.family_invites (
  id          uuid primary key default gen_random_uuid(),
  family_id   uuid not null references public.families(id) on delete cascade,
  created_by  uuid not null references auth.users(id) on delete cascade,
  role        text not null default 'adult'
                check (role in ('admin', 'adult', 'child')),
  token       text not null unique
                default encode(gen_random_bytes(12), 'base64'),
  used_by     uuid references auth.users(id) on delete set null,
  used_at     timestamptz,
  expires_at  timestamptz not null default (now() + interval '7 days'),
  created_at  timestamptz not null default now()
);

-- =============================================================================
-- TABLE: weekly_summaries (materialisiert via DB-Trigger, NIEMALS Client-seitig)
-- =============================================================================
create table public.weekly_summaries (
  id              uuid primary key default gen_random_uuid(),
  family_id       uuid not null references public.families(id) on delete cascade,
  user_id         uuid not null references auth.users(id) on delete cascade,
  week_start      date not null,
  total_minutes   int not null default 0,
  total_points    numeric(10,2) not null default 0,
  by_category     jsonb,
  updated_at      timestamptz not null default now(),
  unique (family_id, user_id, week_start)
);

-- =============================================================================
-- DB TRIGGER: activity_entries → weekly_summaries (Upsert bei INSERT/DELETE)
-- NIEMALS inkrementell — immer vollständige Neuberechnung
-- =============================================================================
create or replace function public.update_weekly_summary()
returns trigger
language plpgsql
security definer
set search_path = ''
as $$
declare
  v_family_id   uuid;
  v_user_id     uuid;
  v_week_start  date;
begin
  if (TG_OP = 'DELETE') then
    v_family_id  := OLD.family_id;
    v_user_id    := OLD.user_id;
    v_week_start := date_trunc('week', OLD.logged_at)::date;
  else
    v_family_id  := NEW.family_id;
    v_user_id    := NEW.user_id;
    v_week_start := date_trunc('week', NEW.logged_at)::date;
  end if;

  insert into public.weekly_summaries
    (family_id, user_id, week_start, total_minutes, total_points, by_category, updated_at)
  select
    v_family_id,
    v_user_id,
    v_week_start,
    coalesce(sum(ae.duration_minutes), 0),
    coalesce(sum(ae.points), 0),
    jsonb_object_agg(
      ae.category_id::text,
      jsonb_build_object(
        'minutes', coalesce(cat_sum.minutes, 0),
        'points',  coalesce(cat_sum.points, 0)
      )
    ) filter (where ae.category_id is not null),
    now()
  from public.activity_entries ae
  where ae.family_id  = v_family_id
    and ae.user_id    = v_user_id
    and date_trunc('week', ae.logged_at)::date = v_week_start
  cross join lateral (
    select
      coalesce(sum(ae2.duration_minutes), 0) as minutes,
      coalesce(sum(ae2.points), 0) as points
    from public.activity_entries ae2
    where ae2.family_id   = v_family_id
      and ae2.user_id     = v_user_id
      and ae2.category_id = ae.category_id
      and date_trunc('week', ae2.logged_at)::date = v_week_start
  ) cat_sum
  on conflict (family_id, user_id, week_start)
  do update set
    total_minutes = excluded.total_minutes,
    total_points  = excluded.total_points,
    by_category   = excluded.by_category,
    updated_at    = now();

  return null;
end;
$$;

create trigger on_activity_entry_change
  after insert or delete on public.activity_entries
  for each row execute procedure public.update_weekly_summary();

-- =============================================================================
-- REALTIME PUBLICATIONS
-- =============================================================================
alter publication supabase_realtime add table public.activity_entries;
alter publication supabase_realtime add table public.weekly_summaries;
alter publication supabase_realtime add table public.family_members;

-- =============================================================================
-- INDEXES
-- =============================================================================
create index family_members_family_id on public.family_members(family_id);
create index family_members_user_family on public.family_members(id, family_id);
create index category_config_family_id on public.category_config(family_id);
create index family_invites_token on public.family_invites(token);
create index weekly_summaries_family_user_week
  on public.weekly_summaries(family_id, user_id, week_start);

-- =============================================================================
-- HELPER FUNCTIONS (security definer — rufen auth.uid() einmal auf, nicht pro Row)
-- =============================================================================

create or replace function public.is_family_member(p_family_id uuid)
returns boolean
language sql
security definer
set search_path = ''
as $$
  select exists(
    select 1 from public.family_members
    where id        = (select auth.uid())
      and family_id = p_family_id
  );
$$;

create or replace function public.is_family_admin(p_family_id uuid)
returns boolean
language sql
security definer
set search_path = ''
as $$
  select exists(
    select 1 from public.family_members
    where id        = (select auth.uid())
      and family_id = p_family_id
      and role      = 'admin'
  );
$$;

-- =============================================================================
-- RLS: families
-- =============================================================================
alter table public.families enable row level security;

create policy "Familienmitglieder koennen Familie lesen"
  on public.families for select to authenticated
  using (public.is_family_member(id));

create policy "Admin kann Familie aktualisieren"
  on public.families for update to authenticated
  using (public.is_family_admin(id));

create policy "Authentifizierter User kann Familie erstellen"
  on public.families for insert to authenticated
  with check (created_by = (select auth.uid()));

-- =============================================================================
-- RLS: family_members
-- =============================================================================
alter table public.family_members enable row level security;

create policy "Familienmitglieder sehen sich gegenseitig"
  on public.family_members for select to authenticated
  using (
    id = (select auth.uid())
    or
    public.is_family_member(family_id)
  );

create policy "User verwaltet eigenes Profil"
  on public.family_members for update to authenticated
  using ((select auth.uid()) = id);

create policy "Admin verwaltet Familienmitglieder"
  on public.family_members for all to authenticated
  using (public.is_family_admin(family_id));

create policy "User kann eigenes Profil initialisieren"
  on public.family_members for insert to authenticated
  with check ((select auth.uid()) = id);

-- =============================================================================
-- RLS: category_config
-- =============================================================================
alter table public.category_config enable row level security;

create policy "Familienmitglieder lesen Kategorien"
  on public.category_config for select to authenticated
  using (public.is_family_member(family_id));

create policy "Admin verwaltet Kategorien"
  on public.category_config for all to authenticated
  using (public.is_family_admin(family_id));

-- =============================================================================
-- RLS: activity_entries
-- =============================================================================
alter table public.activity_entries enable row level security;

create policy "Familienmitglieder lesen alle Eintraege der Familie"
  on public.activity_entries for select to authenticated
  using (public.is_family_member(family_id));

create policy "User erstellt eigene Eintraege"
  on public.activity_entries for insert to authenticated
  with check (
    (select auth.uid()) = user_id
    and public.is_family_member(family_id)
  );

create policy "User loescht eigene Eintraege"
  on public.activity_entries for delete to authenticated
  using (
    (select auth.uid()) = user_id
  );

create policy "Admin loescht alle Eintraege der Familie"
  on public.activity_entries for delete to authenticated
  using (public.is_family_admin(family_id));

-- =============================================================================
-- RLS: family_invites
-- =============================================================================
alter table public.family_invites enable row level security;

create policy "Admin verwaltet Einladungen"
  on public.family_invites for all to authenticated
  using (public.is_family_admin(family_id));

create policy "Jeder authentifizierte User kann Einladung per Token lesen"
  on public.family_invites for select to authenticated
  using (true);

-- =============================================================================
-- RLS: weekly_summaries
-- =============================================================================
alter table public.weekly_summaries enable row level security;

create policy "Familienmitglieder lesen Weekly Summaries"
  on public.weekly_summaries for select to authenticated
  using (public.is_family_member(family_id));

create policy "Service kann Weekly Summaries upserten"
  on public.weekly_summaries for all to service_role
  using (true);
```

**KRITISCH nach der Datei-Erstellung:**
- RLS muss für ALLE 6 Tabellen aktiviert sein (6x `enable row level security` Statements)
- Trigger-Funktion aggregiert IMMER vollständig, nie inkrementell
- `date_trunc('week', ...)` gibt Montag zurück (ISO 8601) — so gewollt für v1
  </action>
  <verify>
    ```bash
    # Migrationsdatei existiert
    ls FamilyScore/supabase/migrations/20260515_initial_schema.sql
    # Erwartet: Datei gefunden

    # Alle 6 Tabellen definiert
    grep "^create table public\." FamilyScore/supabase/migrations/20260515_initial_schema.sql | wc -l
    # Erwartet: 6

    # RLS für alle 6 Tabellen aktiviert
    grep "enable row level security" FamilyScore/supabase/migrations/20260515_initial_schema.sql | wc -l
    # Erwartet: 6

    # Kein total_score Column (Architektur-Invariante)
    grep "total_score" FamilyScore/supabase/migrations/20260515_initial_schema.sql
    # Erwartet: 0 Zeilen

    # Trigger-Funktion vorhanden
    grep "create trigger on_activity_entry_change" FamilyScore/supabase/migrations/20260515_initial_schema.sql
    # Erwartet: Treffer

    # Helper Functions vorhanden
    grep "create or replace function public.is_family_member" FamilyScore/supabase/migrations/20260515_initial_schema.sql
    # Erwartet: Treffer
    ```
  </verify>
  <acceptance_criteria>
    - `FamilyScore/supabase/migrations/20260515_initial_schema.sql` existiert
    - `grep "^create table public\." ... | wc -l` gibt 6 aus (alle 6 Tabellen)
    - `grep "enable row level security" ... | wc -l` gibt 6 aus (RLS auf allen 6 Tabellen)
    - `grep "total_score" ...` gibt 0 Zeilen aus (kein mutabler Score-Column)
    - `grep "create trigger on_activity_entry_change"` findet Treffer
    - `grep "is_family_member\|is_family_admin"` findet beide Helper Functions
    - SQL enthält `alter publication supabase_realtime add table` für activity_entries, weekly_summaries, family_members
  </acceptance_criteria>
  <done>Vollständige SQL-Migration mit 6 Tabellen, RLS auf allen Tabellen, weekly_summaries Trigger (vollständige Neuberechnung), Helper Functions, Realtime Publications, Indexes.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: [BLOCKING] supabase db push — Schema zu Supabase Cloud pushen</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Abschnitt "Environment Availability" → Supabase CLI)
    - .planning/phases/01-foundation/01-RESEARCH.md (Abschnitt "Assumptions Log" → A3 über supabase_realtime)
  </read_first>
  <what-built>
    Task 2 hat die SQL-Migrationsdatei `supabase/migrations/20260515_initial_schema.sql` erstellt.
    Dieser Schritt pusht die Migration zu Supabase Cloud und bringt alle 6 Tabellen mit RLS live.
  </what-built>
  <how-to-verify>
    **Voraussetzungen prüfen:**
    ```bash
    # Supabase CLI installiert?
    supabase --version
    # Falls nicht: brew install supabase/tap/supabase

    # Eingeloggt?
    supabase projects list
    # Falls Fehler: supabase login
    ```

    **Im FamilyScore/ Verzeichnis ausführen:**
    ```bash
    cd FamilyScore

    # Supabase-Projekt verknüpfen (nur beim ersten Mal nötig)
    supabase link --project-ref <your-project-ref>
    # project-ref findest du in: Supabase Dashboard → Project Settings → General → Reference ID

    # Schema pushen
    supabase db push
    ```

    **Falls `supabase db push` nach Bestätigung fragt:**
    - Mit `y` (yes) bestätigen um die Migration anzuwenden

    **Falls Fehler "publication supabase_realtime does not exist":**
    ```bash
    # Die ALTER PUBLICATION Zeilen manuell im Supabase SQL Editor ausführen:
    # Supabase Dashboard → SQL Editor:
    alter publication supabase_realtime add table public.activity_entries;
    alter publication supabase_realtime add table public.weekly_summaries;
    alter publication supabase_realtime add table public.family_members;
    ```

    **Nach erfolgreichem Push verifizieren:**
    In Supabase Dashboard → Table Editor prüfen ob alle 6 Tabellen erscheinen:
    - families
    - family_members
    - activity_entries
    - category_config
    - family_invites
    - weekly_summaries

    WICHTIG: Dashboard zeigt Tabellen, aber RLS-Test erfordert Swift Client (Task 4).
  </how-to-verify>
  <resume-signal>Gib "Schema live" ein wenn supabase db push erfolgreich war und alle 6 Tabellen im Dashboard sichtbar sind</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: SC-3 + SC-4 Verifikation — RLS + Secrets aus Git</name>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Abschnitt "Validation Architecture" → SC-3, SC-4 Verifikationsschritte)
    - .planning/phases/01-foundation/01-RESEARCH.md (Abschnitt "Common Pitfalls" → Pitfall 2 RLS, Pitfall 4 Secrets)
  </read_first>
  <what-built>
    Task 1 hat Supabase.swift + verifySupabaseConnection() erstellt.
    Task 2 hat das SQL-Schema erstellt.
    Task 3 hat das Schema zu Supabase Cloud gepusht.
    Dieser Schritt führt die finalen SC-3 und SC-4 Checks durch.
  </what-built>
  <how-to-verify>
    **SC-3: Supabase RLS via Swift Client prüfen**

    App auf Simulator (oder Gerät) bauen und starten:
    ```bash
    xcodebuild \
      -project FamilyScore/FamilyScore.xcodeproj \
      -scheme FamilyScore \
      -destination "platform=iOS Simulator,name=iPhone 16" \
      build 2>&1 | tail -3
    ```

    App im Simulator starten. In Xcode Console prüfen:
    ```
    [Phase1] App Group Test: PASS
    [Phase1] Supabase Connection PASS — families returned 0 rows (expected 0 with RLS, no auth)
    [Phase1] weekly_summaries table exists PASS — 0 rows
    [Phase1] RLS appears active — anon user sees 0 rows (correct RLS behavior)
    ```

    Falls `[Phase1] Supabase Connection FAIL: ...` erscheint:
    - Prüfe Config/Secrets.xcconfig — hat echte URL und Key?
    - Prüfe Info.plist — enthält $(SUPABASE_URL) und $(SUPABASE_KEY)?
    - Prüfe Xcode Build Settings — ist Config.xcconfig als Configuration File gesetzt?

    **SC-4: Secrets aus Git verbannen**
    ```bash
    # Kein Supabase-Key in committed Dateien
    git grep -r "supabase.co" -- "*.swift" "*.plist" "*.xcconfig" "*.json"
    # Erwartet: kein Output (Secrets.xcconfig ist gitignored, taucht nicht auf)

    git grep -r "sb_publishable" -- "*.swift" "*.plist" "*.xcconfig"
    # Erwartet: kein Output

    # .gitignore enthält Secrets.xcconfig
    grep "Secrets.xcconfig" FamilyScore/.gitignore || grep "Secrets.xcconfig" .gitignore
    # Erwartet: Treffer

    # Git-History prüfen (stärkster Test)
    git log --all --oneline -S "supabase.co" -- "*.swift" "*.plist" "*.xcconfig"
    # Erwartet: kein Output (nichts in History)
    ```

    **SC-5: Kein Supabase im Widget Extension Target**
    ```bash
    grep -r "import Supabase" FamilyScore/FamilyScoreWidgetExtension/ --include="*.swift"
    # Erwartet: 0 Zeilen

    grep "Supabase" FamilyScore/FamilyScore.xcodeproj/project.pbxproj | grep -i "widget"
    # Erwartet: 0 Zeilen
    ```
  </how-to-verify>
  <resume-signal>Gib "Verifikation bestanden" ein wenn alle Checks grünes Licht geben, oder beschreibe welche Checks fehlschlagen</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| iOS Client (anon JWT) → Supabase PostgREST | RLS filtert alle Queries serverseitig; Client kann nie direkt auf fremde Familiendaten zugreifen |
| Build System → Binary | Supabase Key wird via xcconfig in Info.plist injiziert; nicht in Source Code |
| Git Repository → Supabase Cloud | Kein Secret darf die Git-Grenze überschreiten (Secrets.xcconfig gitignored) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | Config/Secrets.xcconfig | mitigate | `git log --all -S "supabase.co"` als Acceptance Criterion in SC-4-Verifikation; `git check-ignore -v Secrets.xcconfig` als Exit-0-Check |
| T-1-02 | Information Disclosure | Supabase RLS — alle 6 Tabellen | mitigate | `enable row level security` + `is_family_member()` Helper auf jeder Tabelle; SC-3 prüft Swift-Client gibt [] zurück (nicht Dashboard); SQL-Acceptance-Criterion: 6x "enable row level security" |
| T-1-03 | Denial of Service | FamilyScoreWidgetExtension 30MB Limit | mitigate | Supabase.swift hat Kommentar "NIEMALS Widget Extension"; Acceptance Criterion: `grep import Supabase FamilyScoreWidgetExtension/ = 0 Zeilen` |
| T-1-08 | Tampering | weekly_summaries Trigger | mitigate | Trigger aggregiert vollständig (SELECT SUM über alle Rows der Woche), niemals inkrementell; Code verbatim aus RESEARCH.md (Pitfall 5 vermieden) |
| T-1-09 | Elevation of Privilege | service_role Key | mitigate | Nur anon/publishable Key im iOS Client (Secrets.xcconfig); service_role Key nur für Edge Functions (Phase 3+, niemals in iOS Binary) |
| T-1-10 | Information Disclosure | SQL Injection via PostgREST | accept | supabase-swift verwendet parameterisierte Queries; kein raw SQL aus User-Input in Phase 1; explizit validiert in RESEARCH.md Security Domain |
</threat_model>

<verification>
## Phase 1 Plan 03 — Verifikation (alle 5 Success Criteria)

**SC-1: Build auf Gerät/Simulator**
```bash
xcodebuild -project FamilyScore/FamilyScore.xcodeproj -scheme FamilyScore \
  -destination "platform=iOS Simulator,name=iPhone 16" build 2>&1 | grep "BUILD"
# Erwartet: BUILD SUCCEEDED
```

**SC-2: App Group (auf echtem Gerät — Simulator beweist nichts)**
Xcode Console nach Gerät-Deploy:
```
[Phase1] App Group Test: PASS
```

**SC-3: Supabase RLS via Swift Client**
Xcode Console (Simulator oder Gerät):
```
[Phase1] Supabase Connection PASS — families returned 0 rows
[Phase1] weekly_summaries table exists PASS
```

**SC-4: Secrets aus Git**
```bash
git grep -r "supabase.co" -- "*.swift" "*.plist" "*.xcconfig" "*.json"
# Erwartet: kein Output
git log --all --oneline -S "supabase.co" -- "*.swift" "*.plist" "*.xcconfig"
# Erwartet: kein Output
```

**SC-5: FamilyScoreKit + kein Supabase im Widget**
```bash
ls FamilyScore/FamilyScoreKit/Package.swift
grep -r "import Supabase" FamilyScore/FamilyScoreWidgetExtension/ --include="*.swift"
# Letzteres: 0 Zeilen
```
</verification>

<success_criteria>
- App baut (xcodebuild BUILD SUCCEEDED) — SC-1 automatisch
- Xcode Console zeigt [Phase1] Supabase Connection PASS — SC-3 bestätigt
- git grep findet keine Supabase-URL in committed Files — SC-4 bestätigt
- Kein Supabase-Import in Widget Extension — SC-5 bestätigt
- supabase/migrations/20260515_initial_schema.sql: 6 Tabellen, 6x RLS enabled, kein total_score
</success_criteria>

<output>
Nach Abschluss `.planning/phases/01-foundation/01-03-SUMMARY.md` erstellen mit:
- Supabase-Projekt-URL (ohne Key!)
- Tabellen-Status (alle 6 live? Jede mit RLS?)
- Swift-Client-Verbindungstest-Ergebnis (SC-3)
- SC-4 git-grep Ergebnis
- SC-5 Widget Extension check Ergebnis
- Alle 5 Success Criteria: PASS / FAIL / PARTIAL
</output>
