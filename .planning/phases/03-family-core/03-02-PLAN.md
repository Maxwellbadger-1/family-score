---
phase: 03-family-core
plan: "02"
type: execute
wave: 1
depends_on: ["03-01-PLAN"]
files_modified:
  - FamilyScore/Services/FamilyService.swift
  - FamilyScore/Services/AuthService.swift
autonomous: true
requirements:
  - FAM-01
  - FAM-02
  - FAM-03
  - FAM-04
  - FAM-05
  - KID-01

must_haves:
  truths:
    - "supabase db push hat die Phase-3-Migration erfolgreich ausgefuehrt (child_profiles-Tabelle existiert, alle 4 RPCs sind in der Datenbank)"
    - "FamilyService ist ein ObservableObject (@MainActor, @Published members/currentFamily/childProfiles/serviceError) mit allen Protocol-Methoden implementiert"
    - "FamilyService ruft niemals zwei getrennte REST-Aufrufe fuer atomare Operationen — immer supabase.rpc()"
    - "AuthService.refreshFamilyStatus() ist hinzugefuegt und kann nach createFamily/joinFamily aufgerufen werden"
    - "xcodebuild test -scheme FamilyScore gibt 0 Failures fuer FamilyServiceTests aus"
  artifacts:
    - path: "FamilyScore/Services/FamilyService.swift"
      provides: "ObservableObject mit allen Family-Operationen via Supabase"
      exports: ["FamilyService", "FamilyServiceError"]
    - path: "FamilyScore/Services/AuthService.swift"
      provides: "Erweitert um refreshFamilyStatus()"
      exports: ["AuthService"]
  key_links:
    - from: "FamilyScore/Services/FamilyService.swift"
      to: "FamilyScore/FamilyScore/Supabase.swift"
      via: "globaler let supabase"
      pattern: "supabase\\.rpc|supabase\\.from"
    - from: "FamilyScore/Views (Wave 2)"
      to: "FamilyScore/Services/FamilyService.swift"
      via: "@EnvironmentObject var familyService: FamilyService"
      pattern: "@EnvironmentObject.*familyService.*FamilyService"
---

<objective>
[BLOCKING: Schema Push] Fuehrt die Phase-3-SQL-Migration gegen die Live-Supabase-Datenbank aus, dann implementiert FamilyService (ObservableObject) mit allen Family-Operationen, und erweitert AuthService um refreshFamilyStatus(). Nach diesem Plan ist das Backend vollstaendig und alle Wave-1-Tests sind gruen.

Purpose: Die SwiftUI-Views in Plan 03 koennen nicht ohne FamilyService implementiert werden. Der Schema-Push muss vor jedem Swift-Zugriff auf die neuen RPCs abgeschlossen sein.

Output:
- Schema Push: child_profiles-Tabelle + 4 RPCs live in Supabase
- FamilyScore/Services/FamilyService.swift (vollstaendige Implementierung)
- FamilyScore/Services/AuthService.swift (nur refreshFamilyStatus() Ergaenzung)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-family-core/03-RESEARCH.md
@.planning/phases/03-family-core/03-PATTERNS.md
@.planning/phases/03-family-core/03-01-SUMMARY.md

<interfaces>
<!-- Wave 0 (Plan 01) hat geliefert: -->

<!-- FamilyScoreTests/Mocks/MockFamilyService.swift definiert das Protocol: -->
```swift
@MainActor
protocol FamilyServiceProtocol: AnyObject {
    var currentFamily: Family? { get }
    var members: [FamilyMember] { get }
    var childProfiles: [ChildProfile] { get }
    var serviceError: String? { get set }
    func createFamily(name: String) async throws -> UUID
    func joinFamily(token: String) async throws -> UUID
    func fetchFamily(familyId: UUID) async
    func fetchMembers(familyId: UUID) async
    func fetchChildProfiles(familyId: UUID) async
    func generateInvite(familyId: UUID, role: MemberRole) async throws -> String
    func removeMember(memberId: UUID) async throws
    func changeMemberRole(memberId: UUID, role: MemberRole) async throws
    func updateProfile(displayName: String, avatarColor: String) async throws
    func createChildProfile(name: String, avatarColor: String) async throws -> UUID
}
```

<!-- Modell-Structs (Plan 01 erstellt, unveraendert): -->
```swift
// FamilyScore/Models/Family.swift
struct Family: Codable, Identifiable {
    let id: UUID; let name: String; let created_at: Date; let created_by: UUID?
}

// FamilyScore/Models/FamilyMember.swift
struct FamilyMember: Codable, Identifiable {
    let id: UUID; let family_id: UUID?; let display_name: String
    let avatar_color: String; let role: String; let created_at: Date
}

// FamilyScore/Models/ChildProfile.swift
struct ChildProfile: Codable, Identifiable {
    let id: UUID; let family_id: UUID; let display_name: String
    let avatar_color: String; let created_by: UUID; let created_at: Date
}

enum MemberRole: String, Codable, CaseIterable { case admin; case adult; case child }
```

<!-- Phase 1 + 2 (bereits live): -->
```swift
// FamilyScore/Supabase.swift — globaler SupabaseClient (NIEMALS zweiten instanziieren)
let supabase = SupabaseClient(supabaseURL: ..., supabaseKey: ..., options: ...)

// FamilyScore/Services/AuthService.swift (Plan 02-02) — wird nur um refreshFamilyStatus() ergaenzt
// checkFamilyMembership(userId:) ist private; wird in refreshFamilyStatus() intern aufgerufen
```

<!-- KRITISCH: iOS 16.0 Minimum -->
<!-- @Observable ist VERBOTEN — immer ObservableObject + @Published -->
<!-- NIEMALS zwei separate REST-Calls fuer atomare Ops — immer supabase.rpc() -->
<!-- Admin-Check immer serverseitig via SECURITY DEFINER RPC -->
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: [BLOCKING] Supabase Schema Push</name>
  <what-built>Die SQL-Migrationsdatei supabase/migrations/20260515_phase3_family_core.sql wurde in Plan 01 erstellt und enthaelt child_profiles DDL, alle 4 SECURITY DEFINER RPCs, category_config-Seeding und die korrigierte family_members UPDATE-Policy. Sie muss jetzt gegen die Live-Datenbank ausgefuehrt werden.</what-built>
  <how-to-verify>
Folgenden Befehl im Terminal ausfuehren (im Projekt-Stammverzeichnis):

```bash
# Option A: Mit gespeichertem Access Token (empfohlen)
$env:SUPABASE_ACCESS_TOKEN = "dein-supabase-access-token"
supabase db push

# Option B: Interaktiv (benoetigt keine Env-Variable)
supabase db push
```

Erwartete Ausgabe: `Applying migration 20260515_phase3_family_core.sql...` gefolgt von `Done.`

Danach verifizieren:
1. Supabase Dashboard → Table Editor → `child_profiles`-Tabelle sichtbar
2. Supabase Dashboard → Database → Functions → `create_family`, `accept_invite`, `change_member_role`, `remove_member` sichtbar

WICHTIG: Mit echtem JWT (nicht Dashboard-Query) testen — RLS ist nur mit echtem JWT aktiv!
  </how-to-verify>
  <resume-signal>Tippe "pushed" wenn der Schema-Push erfolgreich war, oder beschreibe den Fehler.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FamilyService implementieren (ObservableObject, alle Protocol-Methoden)</name>
  <read_first>
    - .planning/phases/03-family-core/03-RESEARCH.md (Abschnitt "Pattern 3: FamilyService — ObservableObject" — vollstaendiger Code-Block mit generateInvite)
    - .planning/phases/03-family-core/03-RESEARCH.md (Abschnitt "Pattern 1" — createFamily Swift-Code)
    - .planning/phases/03-family-core/03-RESEARCH.md (Abschnitt "Pattern 2" — joinFamily Swift-Code)
    - .planning/phases/03-family-core/03-RESEARCH.md (Abschnitt "Code Examples" — fetchMembers, updateProfile, generateInvite Code-Examples)
    - .planning/phases/03-family-core/03-PATTERNS.md (Abschnitt "FamilyService.swift" — Imports, ObservableObject-Klassen-Pattern, RPC-Aufruf-Pattern, Error-Enum-Pattern)
    - .planning/phases/03-family-core/03-PATTERNS.md (Abschnitt "Shared Patterns" — Supabase-Global-Client, ObservableObject + MainActor)
    - FamilyScore/Services/AuthService.swift (ObservableObject-Pattern als exakter Analog — @MainActor, @Published, async throws Methoden)
    - FamilyScore/FamilyScore/Supabase.swift (globaler let supabase — nur einmal lesen, nicht modifizieren)
    - CLAUDE.md (Abschnitt "Supabase" — SDK nur im App-Target; Score nie mutabler Wert; RLS mit echtem JWT)
  </read_first>
  <files>
    FamilyScore/Services/FamilyService.swift
  </files>
  <behavior>
    - createFamily(name:) → ruft supabase.rpc("create_family") auf → gibt UUID zurueck → wirft FamilyServiceError.alreadyInFamily wenn User schon Familie hat
    - joinFamily(token:) → ruft supabase.rpc("accept_invite") auf → gibt family_id zurueck → wirft FamilyServiceError.invalidToken bei ungueltigem Token
    - fetchMembers(familyId:) → laedt [FamilyMember] aus family_members-Tabelle → setzt members-Property → bei Fehler: setzt serviceError, kein Crash
    - fetchFamily(familyId:) → laedt Family-Struct → setzt currentFamily → bei Fehler: serviceError
    - generateInvite(familyId:, role:) → INSERT in family_invites → gibt 8-stelligen Display-Code zurueck (alphanumerisch, uppercase)
    - removeMember(memberId:) → ruft supabase.rpc("remove_member") auf → kein Rueckgabewert → wirft FamilyServiceError.insufficientPermissions bei Fehler
    - changeMemberRole(memberId:, role:) → ruft supabase.rpc("change_member_role") auf → wirft bei Fehler
    - updateProfile(displayName:, avatarColor:) → direktes UPDATE auf family_members → nur display_name + avatar_color (nie role oder family_id)
    - createChildProfile(name:, avatarColor:) → INSERT in child_profiles → gibt UUID zurueck
    - fetchChildProfiles(familyId:) → laedt [ChildProfile] → setzt childProfiles
  </behavior>
  <action>
Datei `FamilyScore/Services/FamilyService.swift` erstellen.
Target Membership: FamilyScore (App) ONLY — NIEMALS Widget Extension!

```swift
// FamilyScore/Services/FamilyService.swift
// Target Membership: FamilyScore (App) ONLY
// iOS 16.0 Minimum: ObservableObject + @Published (KEIN @Observable -- iOS 17+)
// NIEMALS Supabase SDK im Widget-Target -- gilt auch fuer diesen Service

import Foundation
import Supabase

@MainActor
final class FamilyService: ObservableObject {

    @Published private(set) var currentFamily: Family?
    @Published private(set) var members: [FamilyMember] = []
    @Published private(set) var childProfiles: [ChildProfile] = []
    @Published var serviceError: String? = nil

    // MARK: - Familie laden

    func fetchFamily(familyId: UUID) async {
        do {
            let family: Family = try await supabase
                .from("families")
                .select()
                .eq("id", value: familyId.uuidString)
                .single()
                .execute()
                .value
            currentFamily = family
        } catch {
            serviceError = "Familie konnte nicht geladen werden."
        }
    }

    func fetchMembers(familyId: UUID) async {
        do {
            let fetched: [FamilyMember] = try await supabase
                .from("family_members")
                .select()
                .eq("family_id", value: familyId.uuidString)
                .order("created_at", ascending: true)
                .execute()
                .value
            members = fetched
        } catch {
            serviceError = "Mitglieder konnten nicht geladen werden."
        }
    }

    func fetchChildProfiles(familyId: UUID) async {
        do {
            let fetched: [ChildProfile] = try await supabase
                .from("child_profiles")
                .select()
                .eq("family_id", value: familyId.uuidString)
                .order("created_at", ascending: true)
                .execute()
                .value
            childProfiles = fetched
        } catch {
            serviceError = "Kinder-Profile konnten nicht geladen werden."
        }
    }

    // MARK: - Familie erstellen und beitreten

    func createFamily(name: String) async throws -> UUID {
        struct Params: Encodable {
            let familyName: String
            enum CodingKeys: String, CodingKey { case familyName = "family_name" }
        }
        do {
            let familyId: UUID = try await supabase
                .rpc("create_family", params: Params(familyName: name))
                .execute()
                .value
            return familyId
        } catch {
            let msg = error.localizedDescription.lowercased()
            if msg.contains("bereits einer familie") || msg.contains("already") {
                throw FamilyServiceError.alreadyInFamily
            }
            throw FamilyServiceError.unknown(error.localizedDescription)
        }
    }

    func joinFamily(token: String) async throws -> UUID {
        struct Params: Encodable {
            let inviteToken: String
            enum CodingKeys: String, CodingKey { case inviteToken = "invite_token" }
        }
        do {
            let familyId: UUID = try await supabase
                .rpc("accept_invite", params: Params(inviteToken: token))
                .execute()
                .value
            return familyId
        } catch {
            let msg = error.localizedDescription.lowercased()
            if msg.contains("ungültig") || msg.contains("abgelaufen") || msg.contains("invalid") {
                throw FamilyServiceError.invalidToken
            }
            if msg.contains("bereits einer familie") {
                throw FamilyServiceError.alreadyInFamily
            }
            throw FamilyServiceError.unknown(error.localizedDescription)
        }
    }

    // MARK: - Einladung generieren (Admin only)

    func generateInvite(familyId: UUID, role: MemberRole) async throws -> String {
        // Token-Generierung IMMER serverseitig (gen_random_bytes DB-Default)
        // Admin-only via RLS Policy "Admin verwaltet Einladungen" (Phase 1)
        struct NewInvite: Encodable {
            let family_id: String
            let created_by: String
            let role: String
        }
        struct InviteResponse: Decodable { let token: String }

        let currentUserId: UUID
        do {
            currentUserId = try await supabase.auth.session.user.id
        } catch {
            throw FamilyServiceError.notAuthenticated
        }

        let response: InviteResponse = try await supabase
            .from("family_invites")
            .insert(NewInvite(
                family_id: familyId.uuidString,
                created_by: currentUserId.uuidString,
                role: role.rawValue
            ))
            .select("token")
            .single()
            .execute()
            .value

        // Nur alphanumerische Zeichen, 8 Stellen, Uppercase
        // Vermeidet Base64-Sonderzeichen (+, /, =) die schlecht abtippbar sind
        let displayCode = response.token
            .filter { $0.isLetter || $0.isNumber }
            .prefix(8)
            .uppercased()
        return String(displayCode)
    }

    // MARK: - Admin-Aktionen (via SECURITY DEFINER RPCs)

    func removeMember(memberId: UUID) async throws {
        struct Params: Encodable {
            let targetMemberId: UUID
            enum CodingKeys: String, CodingKey { case targetMemberId = "target_member_id" }
        }
        do {
            try await supabase
                .rpc("remove_member", params: Params(targetMemberId: memberId))
                .execute()
        } catch {
            let msg = error.localizedDescription.lowercased()
            if msg.contains("keine admin") || msg.contains("berechtigung") {
                throw FamilyServiceError.insufficientPermissions
            }
            if msg.contains("mindestens einen admin") {
                throw FamilyServiceError.lastAdminProtection
            }
            throw FamilyServiceError.unknown(error.localizedDescription)
        }
    }

    func changeMemberRole(memberId: UUID, role: MemberRole) async throws {
        struct Params: Encodable {
            let targetMemberId: UUID
            let newRole: String
            enum CodingKeys: String, CodingKey {
                case targetMemberId = "target_member_id"
                case newRole = "new_role"
            }
        }
        do {
            try await supabase
                .rpc("change_member_role", params: Params(targetMemberId: memberId, newRole: role.rawValue))
                .execute()
        } catch {
            let msg = error.localizedDescription.lowercased()
            if msg.contains("keine admin") || msg.contains("berechtigung") {
                throw FamilyServiceError.insufficientPermissions
            }
            if msg.contains("mindestens einen admin") {
                throw FamilyServiceError.lastAdminProtection
            }
            throw FamilyServiceError.unknown(error.localizedDescription)
        }
    }

    // MARK: - Eigenes Profil bearbeiten (direktes REST -- kein RPC noetig)

    func updateProfile(displayName: String, avatarColor: String) async throws {
        // Direktes UPDATE: safe weil die RLS-Policy nur display_name + avatar_color zulaesst.
        // role + family_id koennen NICHT ueber diesen Pfad veraendert werden (Policy-Design).
        struct ProfileUpdate: Encodable {
            let display_name: String
            let avatar_color: String
            let updated_at: Date
        }
        let currentUserId: UUID
        do {
            currentUserId = try await supabase.auth.session.user.id
        } catch {
            throw FamilyServiceError.notAuthenticated
        }

        try await supabase
            .from("family_members")
            .update(ProfileUpdate(
                display_name: displayName,
                avatar_color: avatarColor,
                updated_at: Date()
            ))
            .eq("id", value: currentUserId.uuidString)
            .execute()
    }

    // MARK: - Kinder-Profile

    func createChildProfile(name: String, avatarColor: String) async throws -> UUID {
        // Admin-only via RLS Policy "Admin erstellt Kind-Profile"
        struct NewChildProfile: Encodable {
            let family_id: String
            let display_name: String
            let avatar_color: String
            let created_by: String
        }
        struct ProfileResponse: Decodable { let id: UUID }

        guard let familyId = currentFamily?.id else {
            throw FamilyServiceError.familyNotFound
        }
        let currentUserId: UUID
        do {
            currentUserId = try await supabase.auth.session.user.id
        } catch {
            throw FamilyServiceError.notAuthenticated
        }

        let response: ProfileResponse = try await supabase
            .from("child_profiles")
            .insert(NewChildProfile(
                family_id: familyId.uuidString,
                display_name: name,
                avatar_color: avatarColor,
                created_by: currentUserId.uuidString
            ))
            .select("id")
            .single()
            .execute()
            .value

        // Nach Erstellen die Liste neu laden
        await fetchChildProfiles(familyId: familyId)
        return response.id
    }
}

// MARK: - Error-Enum

enum FamilyServiceError: Error, LocalizedError {
    case notAuthenticated
    case familyNotFound
    case invalidToken
    case alreadyInFamily
    case insufficientPermissions
    case lastAdminProtection
    case unknown(String)

    var errorDescription: String? {
        switch self {
        case .notAuthenticated:       return "Bitte zuerst einloggen."
        case .familyNotFound:         return "Familie nicht gefunden."
        case .invalidToken:           return "Ungültiger oder abgelaufener Einladungscode."
        case .alreadyInFamily:        return "Du bist bereits Mitglied einer Familie."
        case .insufficientPermissions: return "Keine Berechtigung für diese Aktion."
        case .lastAdminProtection:    return "Die Familie muss mindestens einen Admin haben."
        case .unknown(let msg):       return msg
        }
    }
}
```
  </action>
  <verify>
    <automated>
xcodebuild test -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" -only-testing FamilyScoreTests/FamilyServiceTests 2>&1 | Select-String "TEST PASSED|TEST FAILED|PASS|FAIL|error:"
    </automated>
  </verify>
  <acceptance_criteria>
    - `FamilyScore/Services/FamilyService.swift` existiert
    - `grep "@MainActor" FamilyScore/Services/FamilyService.swift` gibt Treffer (keine alternative Position)
    - `grep "final class FamilyService: ObservableObject" FamilyScore/Services/FamilyService.swift` gibt Treffer
    - `grep "@Observable" FamilyScore/Services/FamilyService.swift` gibt 0 Zeilen (iOS 17+ verboten)
    - `grep "supabase.rpc" FamilyScore/Services/FamilyService.swift` gibt mindestens 4 Treffer (create_family, accept_invite, remove_member, change_member_role)
    - `grep "supabase.from.*UPDATE.*role" FamilyScore/Services/FamilyService.swift` gibt 0 Zeilen (role nie via direktem UPDATE)
    - `grep "enum FamilyServiceError" FamilyScore/Services/FamilyService.swift` gibt Treffer
    - `grep "case alreadyInFamily" FamilyScore/Services/FamilyService.swift` gibt Treffer
    - `grep "case lastAdminProtection" FamilyScore/Services/FamilyService.swift` gibt Treffer
    - `xcodebuild test -only-testing FamilyScoreTests/FamilyServiceTests` gibt 0 FAIL-Zeilen
  </acceptance_criteria>
  <done>FamilyService implementiert FamilyServiceProtocol vollstaendig. Alle atomaren Operationen laufen via supabase.rpc(). Keine direkten role/family_id-Updates. FamilyServiceTests alle gruen.</done>
</task>

<task type="auto">
  <name>Task 3: AuthService um refreshFamilyStatus() erweitern</name>
  <read_first>
    - .planning/phases/03-family-core/03-RESEARCH.md (Abschnitt "Pattern 7: AuthService.refreshFamilyStatus()" — vollstaendiger Code-Block und Erklaerung warum Views den Aufruf machen, nicht FamilyService direkt)
    - .planning/phases/03-family-core/03-PATTERNS.md (Abschnitt "AuthService.swift (service, event-driven) — MODIFY" — exaktes Code-Snippet der Ergaenzung)
    - FamilyScore/Services/AuthService.swift (MUSS gelesen werden bevor modifiziert wird — checkFamilyMembership() ist bereits private definiert)
  </read_first>
  <files>
    FamilyScore/Services/AuthService.swift
  </files>
  <action>
AuthService.swift lesen, dann eine einzige Methode am Ende der Klasse ergaenzen (vor der letzten `}`):

```swift
// MARK: - Family Status Refresh (Phase 3)

/// Wird von Views nach createFamily() und joinFamily() aufgerufen.
/// FamilyService gibt die neue family_id zurueck; die View ruft danach refreshFamilyStatus()
/// auf AuthService auf. So bleibt die Dependency-Richtung sauber:
/// Views → Services (nie Services → Services direkt).
///
/// Pitfall 3 (RESEARCH.md): authStateChanges feuert kein neues Event wenn
/// family_members.family_id sich aendert — daher manueller refresh noetig.
func refreshFamilyStatus() async {
    guard let userId = currentUser?.id else { return }
    let hasFamily = await checkFamilyMembership(userId: userId)
    appState = .authenticated(hasFamily: hasFamily)
}
```

ACHTUNG: Alle bestehenden Methoden in AuthService.swift unveraendert lassen.
Nur diese eine Methode hinzufuegen.
  </action>
  <verify>
    <automated>
xcodebuild build -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" -quiet 2>&1 | Select-String "BUILD SUCCEEDED|error:"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "func refreshFamilyStatus" FamilyScore/Services/AuthService.swift` gibt Treffer
    - `grep "await checkFamilyMembership" FamilyScore/Services/AuthService.swift` gibt mindestens 2 Treffer (bestehender Aufruf in startObserving + neuer in refreshFamilyStatus)
    - `grep "appState = .authenticated(hasFamily:" FamilyScore/Services/AuthService.swift` gibt mindestens 2 Treffer
    - Keine anderen Methoden in AuthService.swift wurden modifiziert (diff zeigt nur die neue Methode)
    - `xcodebuild build -scheme FamilyScore` gibt `BUILD SUCCEEDED` aus
  </acceptance_criteria>
  <done>AuthService hat refreshFamilyStatus(). Bestehende Methoden unveraendert. Build gruen.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| SwiftUI View → FamilyService | Views rufen FamilyService-Methoden auf; Admin-Checks laufen serverseitig in SECURITY DEFINER RPCs, nie im Swift-Client |
| iOS Client → Supabase RPC | JWT im Authorization-Header; SECURITY DEFINER RPCs pruefen auth.uid() serverseitig |
| FamilyService → family_members UPDATE | Nur display_name + avatar_color ueber direktes UPDATE erlaubt; role + family_id nur via RPCs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-01 | Elevation of Privilege | FamilyService.changeMemberRole / direkte DB-Updates | mitigate | changeMemberRole ruft ausschliesslich `change_member_role` RPC auf (SECURITY DEFINER + Admin-Check); updateProfile() veraendert niemals role/family_id |
| T-3-02 | Spoofing | FamilyService.joinFamily / Invite-Token Replay | mitigate | accept_invite RPC prueft `used_by is null` serverseitig; FamilyService gibt Token 1:1 an RPC weiter ohne Client-Validierung |
| T-3-03 | Spoofing | Ablaufdatum-Pruefung Client vs. Server | mitigate | Ablaufdatum-Pruefung ausschliesslich in accept_invite RPC mit `expires_at > now()` -- kein Client-Clock-Vergleich |
| T-3-04 | Information Disclosure | FamilyService.@Published members | mitigate | `private(set)` verhindert direktes Schreiben aus Views; nur fetchMembers() setzt members |
| T-3-05 | Tampering | FamilyService.createChildProfile / Nicht-Admin | mitigate | RLS Policy "Admin erstellt Kind-Profile" WITH CHECK blockiert serverseitig; kein client-seitiger Admin-Check |
| T-3-06 | Denial of Service | FamilyService.removeMember / letzter Admin | mitigate | remove_member RPC prueft Admin-Count; FamilyService wirft FamilyServiceError.lastAdminProtection |
| T-3-07 | Denial of Service | FamilyService.createFamily / bereits in Familie | mitigate | create_family RPC prueft family_id IS NOT NULL; FamilyService wirft FamilyServiceError.alreadyInFamily |
</threat_model>

<verification>
## Plan 02 Verifikation

**Schema-Push erfolgreich:**
```bash
# Supabase CLI muss im Projektverzeichnis verfuegbar sein
supabase db status
# Erwartet: Migration 20260515_phase3_family_core.sql als "applied" gelistet
```

**FamilyService ist ObservableObject (iOS 16 kompatibel):**
```bash
Select-String "ObservableObject" FamilyScore/Services/FamilyService.swift
# Erwartet: Treffer

Select-String "@Observable" FamilyScore/Services/FamilyService.swift
# Erwartet: Keine Treffer
```

**Alle atomaren Ops via RPC (niemals zwei getrennte REST-Calls):**
```bash
Select-String "supabase.rpc" FamilyScore/Services/FamilyService.swift
# Erwartet: Mindestens 4 Treffer
```

**AuthService hat refreshFamilyStatus():**
```bash
Select-String "func refreshFamilyStatus" FamilyScore/Services/AuthService.swift
# Erwartet: Treffer
```

**Build:**
```bash
xcodebuild build -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" -quiet 2>&1 | Select-String "BUILD SUCCEEDED|error:"
# Erwartet: BUILD SUCCEEDED
```

**FamilyServiceTests gruen:**
```bash
xcodebuild test -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" -only-testing FamilyScoreTests/FamilyServiceTests 2>&1 | Select-String "TEST PASSED|FAIL"
# Erwartet: TEST PASSED (0 FAIL)
```
</verification>

<success_criteria>
- supabase db push: Migration 20260515_phase3_family_core.sql als applied
- FamilyService.swift: ObservableObject, @Published currentFamily/members/childProfiles/serviceError
- Alle 4 atomaren Operationen via supabase.rpc() (create_family, accept_invite, remove_member, change_member_role)
- FamilyServiceError-Enum mit 7 Cases (inkl. lastAdminProtection)
- AuthService.swift: nur refreshFamilyStatus() hinzugefuegt, alles andere unveraendert
- xcodebuild build → BUILD SUCCEEDED
- FamilyServiceTests alle gruen (10/10)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/03-family-core/03-02-SUMMARY.md` erstellen mit:
- Schema-Push-Status (applied / error)
- FamilyService: Pfad + implementierte Methoden-Liste
- AuthService: Ergaenzung (refreshFamilyStatus hinzugefuegt)
- iOS 16 Kompatibilitaet: ObservableObject bestaetigt
- Build-Status: PASS / FAIL
- Test-Ergebnis: X/10 Tests gruengeschaltet
</output>
