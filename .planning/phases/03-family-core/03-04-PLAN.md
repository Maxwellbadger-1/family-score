---
phase: 03-family-core
plan: "04"
type: execute
wave: 3
depends_on: ["03-03-PLAN"]
files_modified:
  - FamilyScore/Views/RootView.swift
  - FamilyScore/FamilyScore/FamilyScoreApp.swift
autonomous: false
requirements:
  - FAM-01
  - FAM-02
  - FAM-03
  - FAM-04
  - FAM-05
  - KID-01
  - SETTINGS-03

must_haves:
  truths:
    - "RootView.authenticated(hasFamily: false) zeigt FamilyOnboardingView statt OnboardingPlaceholderView"
    - "FamilyService wird in FamilyScoreApp.swift als @StateObject instanziiert und via .environmentObject() injiziert"
    - "MemberListView ist von MainTabView (Placeholder) erreichbar — Tab oder Navigation"
    - "Invite-Fluss auf zwei Geraeten funktioniert: Admin generiert Code auf Geraet A; zweiter User gibt Code auf Geraet B ein und sieht dieselbe Familie"
    - "xcodebuild test gibt 0 Failures aus"
  artifacts:
    - path: "FamilyScore/Views/RootView.swift"
      provides: "Routing zu FamilyOnboardingView statt OnboardingPlaceholderView"
      contains: "FamilyOnboardingView"
    - path: "FamilyScore/FamilyScore/FamilyScoreApp.swift"
      provides: "FamilyService als @StateObject + .environmentObject Injektion"
      contains: "@StateObject.*familyService.*FamilyService"
  key_links:
    - from: "FamilyScore/FamilyScore/FamilyScoreApp.swift"
      to: "FamilyScore/Services/FamilyService.swift"
      via: "@StateObject private var familyService = FamilyService()"
      pattern: "@StateObject.*familyService"
    - from: "FamilyScore/Views/RootView.swift"
      to: "FamilyScore/Views/Family/FamilyOnboardingView.swift"
      via: "case .authenticated(hasFamily: false): FamilyOnboardingView()"
      pattern: "FamilyOnboardingView"
---

<objective>
Verkabelt alle Phase-3-Komponenten in der App: RootView zeigt FamilyOnboardingView statt Placeholder, FamilyService wird als @StateObject instanziiert und global injiziert. Abgeschlossen durch einen Geraete-Checkpoint der den vollstaendigen Invite-Fluss auf zwei Geraeten verifiziert.

Purpose: Ohne diese Verdrahtung sind alle Phase-3-Views vorhanden aber nicht erreichbar. Der Geraete-Checkpoint sichert die kritischste Success Criterion (SC-2: zwei Geraete, echter Cross-Device-Invite).

Output:
- RootView.swift (OnboardingPlaceholderView → FamilyOnboardingView)
- FamilyScoreApp.swift (FamilyService @StateObject + .environmentObject)
- Geraete-Checkpoint: Invite-Fluss verifiziert
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-family-core/03-RESEARCH.md
@.planning/phases/03-family-core/03-03-SUMMARY.md

<interfaces>
<!-- Wave 3 konsumiert alles aus Wellen 0-2: -->

```swift
// FamilyScore/Views/RootView.swift (Plan 02-02, wird modifiziert)
// Aktuelle Implementierung:
case .authenticated(hasFamily: false):
    OnboardingPlaceholderView()   // ← NUR DIESE ZEILE ersetzen

// Nachher:
case .authenticated(hasFamily: false):
    FamilyOnboardingView()
    // FamilyService muss als @EnvironmentObject in FamilyScoreApp.swift bereitstehen
```

```swift
// FamilyScore/FamilyScore/FamilyScoreApp.swift (Plan 02-03, wird modifiziert)
// Aktuelle Implementierung hat:
@StateObject private var authService = AuthService()
// Ergaenzung: FamilyService als weiteren @StateObject hinzufuegen
@StateObject private var familyService = FamilyService()
// und in body: .environmentObject(familyService)
```

```swift
// Authenticated-Placeholder braucht MemberListView-Zugang fuer Phase-3-Tests
// Vorlaeufige Loesung: AuthenticatedPlaceholderView um NavigationLink zu MemberListView erweitern
// (wird in Phase 4 durch echte MainTabView ersetzt)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App-Verdrahtung (RootView + FamilyScoreApp)</name>
  <read_first>
    - FamilyScore/Views/RootView.swift (MUSS gelesen werden -- nur OnboardingPlaceholderView → FamilyOnboardingView ersetzen, alles andere unveraendert)
    - FamilyScore/FamilyScore/FamilyScoreApp.swift (MUSS gelesen werden -- FamilyService @StateObject + environmentObject hinzufuegen, alles andere unveraendert)
    - .planning/phases/03-family-core/03-PATTERNS.md (Abschnitt "RootView.swift — MODIFY" und "AuthService.swift — MODIFY" fuer exaktes Aenderungs-Pattern)
    - .planning/phases/02-authentication/02-03-PLAN.md (FamilyScoreApp.swift Struktur aus Phase 2 — um welche Stellen im File es geht)
  </read_first>
  <files>
    FamilyScore/Views/RootView.swift,
    FamilyScore/FamilyScore/FamilyScoreApp.swift
  </files>
  <action>
**Schritt 1: RootView.swift modifizieren**

RootView.swift lesen. Dann genau EINE Zeile aendern:

```swift
// Vorher:
case .authenticated(hasFamily: false):
    OnboardingPlaceholderView()

// Nachher:
case .authenticated(hasFamily: false):
    FamilyOnboardingView()
```

Alle anderen Teile von RootView.swift UNVERAENDERT lassen.
Import-Check: `FamilyOnboardingView` liegt im gleichen App-Target, kein zusaetzlicher Import noetig.

**Schritt 2: AuthenticatedPlaceholderView um MemberListView-Zugang erweitern**

In RootView.swift, innerhalb von `struct AuthenticatedPlaceholderView`, einen NavigationLink zu MemberListView hinzufuegen:

```swift
struct AuthenticatedPlaceholderView: View {
    @EnvironmentObject private var authService: AuthService
    @EnvironmentObject private var familyService: FamilyService   // Ergaenzen

    var body: some View {
        NavigationStack {          // NavigationStack hinzufuegen
            ZStack {
                Color.black.ignoresSafeArea()
                VStack(spacing: 16) {
                    Image(systemName: "checkmark.circle.fill")
                        .font(.system(size: 48))
                        .foregroundColor(.green)
                    Text("Eingeloggt!")
                        .font(.title.bold())
                        .foregroundColor(.white)
                    Text("Dashboard kommt in Phase 4.")
                        .font(.body)
                        .foregroundColor(.secondary)

                    // Phase 3: Mitgliederliste erreichbar machen (fuer Testing)
                    NavigationLink(destination: MemberListView()) {
                        Label("Familie verwalten", systemImage: "person.2.fill")
                            .font(.headline)
                            .frame(maxWidth: .infinity, minHeight: 50)
                            .background(Color.white.opacity(0.15))
                            .foregroundColor(.white)
                            .cornerRadius(12)
                    }
                    .padding(.horizontal, 24)

                    Button("Ausloggen") {
                        Task { try? await authService.signOut() }
                    }
                    .foregroundColor(.red)
                    .padding(.top, 32)
                }
            }
        }
    }
}
```

**Schritt 3: FamilyScoreApp.swift modifizieren**

FamilyScoreApp.swift lesen. Dann:

1. Nach der bestehenden `@StateObject private var authService = AuthService()` Zeile einfuegen:
```swift
@StateObject private var familyService = FamilyService()
```

2. Im `body`-Abschnitt, nach dem bestehenden `.environmentObject(authService)` einfuegen:
```swift
.environmentObject(familyService)
```

Alle anderen Teile von FamilyScoreApp.swift UNVERAENDERT lassen.
`startObserving()` ist bereits konfiguriert (Plan 02-03) — nicht nochmals hinzufuegen.

Ergebnis-Struktur (Beispiel):
```swift
@main
struct FamilyScoreApp: App {
    @StateObject private var authService = AuthService()
    @StateObject private var familyService = FamilyService()  // Neu

    var body: some Scene {
        WindowGroup {
            RootView()
                .environmentObject(authService)
                .environmentObject(familyService)              // Neu
        }
        .task { await authService.startObserving() }
    }
}
```
  </action>
  <verify>
    <automated>
xcodebuild build -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" -quiet 2>&1 | Select-String "BUILD SUCCEEDED|error:"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "FamilyOnboardingView" FamilyScore/Views/RootView.swift` gibt Treffer
    - `grep "OnboardingPlaceholderView()" FamilyScore/Views/RootView.swift | grep -v struct | grep -v Preview` gibt 0 Zeilen (ersetzt durch FamilyOnboardingView)
    - `grep "@StateObject.*familyService.*FamilyService" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer
    - `grep "\.environmentObject(familyService)" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer
    - `grep "MemberListView" FamilyScore/Views/RootView.swift` gibt Treffer (NavigationLink in AuthenticatedPlaceholderView)
    - `xcodebuild build -scheme FamilyScore` gibt `BUILD SUCCEEDED` aus
  </acceptance_criteria>
  <done>App-Verdrahtung vollstaendig. FamilyService ist global verfuegbar. RootView zeigt FamilyOnboardingView. MemberListView ist von AuthenticatedPlaceholderView erreichbar.</done>
</task>

<task type="auto">
  <name>Task 2: Vollstaendige Test-Suite gruen schalten</name>
  <read_first>
    - FamilyScoreTests/FamilyServiceTests.swift (Teststubs aus Plan 01 -- pruefen ob alle 10 Tests kompilieren und mit MockFamilyService laufen)
    - FamilyScoreTests/Mocks/MockFamilyService.swift (Mock-Implementierung -- sicherstellen dass Protocol vollstaendig implementiert ist)
    - FamilyScoreTests/AuthServiceTests.swift (Phase 2 Tests -- sicherstellen dass durch Phase 3 Aenderungen nichts gebrochen wurde)
  </read_first>
  <files>
    FamilyScoreTests/FamilyServiceTests.swift
  </files>
  <action>
**Schritt 1: Test-Suite ausfuehren und Fehler beheben**

```bash
xcodebuild test -scheme FamilyScore \
  -destination "platform=iOS Simulator,name=iPhone 16" \
  -only-testing FamilyScoreTests/FamilyServiceTests \
  2>&1 | tail -30
```

Bei Kompilierungsfehlern: Ursache analysieren und beheben.

Haeufige Ursachen:
- MockFamilyService implementiert FamilyServiceProtocol nicht vollstaendig → fehlende Methode ergaenzen
- Model-Struct-Felder stimmen nicht mit Test-Initialisierern ueberein → Struct-Definition pruefen
- @MainActor-Isolation-Fehler → sicherstellen dass alle Testmethoden `async throws` sind

**Schritt 2: AuthServiceTests ebenfalls gruen halten**

```bash
xcodebuild test -scheme FamilyScore \
  -destination "platform=iOS Simulator,name=iPhone 16" \
  -only-testing FamilyScoreTests/AuthServiceTests \
  2>&1 | tail -30
```

Phase 3 Aenderungen an AuthService.swift (nur refreshFamilyStatus() hinzugefuegt) duerften keinen Test brechen. Falls doch: analysieren und beheben.

**Schritt 3: Vollstaendige Test-Suite**

```bash
xcodebuild test -scheme FamilyScore \
  -destination "platform=iOS Simulator,name=iPhone 16" \
  2>&1 | grep -E "TEST PASSED|TEST FAILED|error:|FAIL"
```

Erwartet: Alle Tests PASS, 0 Failures.
  </action>
  <verify>
    <automated>
xcodebuild test -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" 2>&1 | Select-String "TEST PASSED|TEST FAILED|error:" | Select-Object -Last 5
    </automated>
  </verify>
  <acceptance_criteria>
    - `xcodebuild test -scheme FamilyScore` gibt `TEST PASSED` aus (oder: 0 FAIL-Zeilen)
    - `xcodebuild test -only-testing FamilyScoreTests/FamilyServiceTests` gibt 0 FAIL-Zeilen
    - `xcodebuild test -only-testing FamilyScoreTests/AuthServiceTests` gibt 0 FAIL-Zeilen
    - Gesamtzahl der Tests: mindestens 18 (10 FamilyServiceTests + 8 AuthServiceTests)
  </acceptance_criteria>
  <done>Alle Tests gruen. Phase 3 hat keine Phase-2-Tests gebrochen. Test-Suite ist vollstaendig und zuverlaessig.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Geraete-Checkpoint — Invite-Fluss auf echten Geraeten</name>
  <what-built>
Phase 3 ist vollstaendig implementiert:
- FamilyOnboardingView (Erstellen + Beitreten)
- FamilyService mit allen SECURITY DEFINER RPCs
- MemberListView mit Admin-Aktionen (Entfernen, Rolle aendern, Invite generieren)
- AddChildView fuer parent-managed Kind-Profile
- Schema mit child_profiles, create_family, accept_invite, change_member_role, remove_member RPCs

Alle Unit-Tests laufen gruen (MockFamilyService-basiert).
Dieser Checkpoint verifiziert den echten Cross-Device-Invite-Fluss (SC-2) und die Admin-UI-Sichtbarkeit (SC-3/SC-4).
  </what-built>
  <how-to-verify>
**Mindestvoraussetzungen:**
- Zwei iOS-Geraete (oder: ein Geraet + Simulator mit zwei verschiedenen Supabase-Accounts)
- Zwei separate Supabase-Test-Accounts (unterschiedliche E-Mail-Adressen)

**Checkliste — Success Criteria Phase 3:**

SC-1: Familie erstellen
1. App auf Geraet A oeffnen, mit Account A einloggen
2. FamilyOnboardingView erscheint → "Neue Familie erstellen" antippen
3. Familienname eingeben → "Familie erstellen" → Erwartung: App zeigt AuthenticatedPlaceholderView (hasFamily: true)

SC-2: Invite-Fluss (Kerntest)
4. Auf Geraet A: "Familie verwalten" antippen → MemberListView erscheint
5. "+" antippen → "Einladungscode generieren" → Code wird generiert (8 Zeichen, alphanumerisch)
6. Code notieren oder in Zwischenablage kopieren
7. App auf Geraet B oeffnen, mit Account B einloggen
8. FamilyOnboardingView → "Mit Code beitreten" → Code eingeben → "Beitreten"
9. Erwartung: Geraet B zeigt AuthenticatedPlaceholderView + beide Geraete zeigen die gleiche Familie in MemberListView

SC-3: Mitglied entfernen
10. Auf Geraet A (Admin): MemberListView → Account B nach links swipen → "Entfernen" antippen
11. Erwartung: Account B verschwindet aus Liste auf Geraet A
12. Auf Geraet B: App neu starten → Erwartung: FamilyOnboardingView erscheint (kein Familienzugang mehr)

SC-4: Rolle aendern
13. Neuen zweiten User einladen (Schritte 4-9 wiederholen)
14. Auf Geraet A: Mitglied nach rechts swipen → "Rolle" → Admin auswaehlen → Bestaetigen
15. Erwartung: Mitglied hat jetzt Admin-Rolle in der Liste

SC-5: Kind-Profil
16. Auf Geraet A (Admin): "+" → "Kind-Profil hinzufuegen"
17. Name + Farbe eingeben → "Profil erstellen"
18. Erwartung: Kind-Profil erscheint in MemberListView im "Kinder"-Abschnitt ohne Login-Anforderung

**Simulator-Fallback (wenn nur 1 Geraet):**
- SC-1, SC-3, SC-4, SC-5 mit einem Account testbar
- SC-2 mit Simulator (simuliert zwei Accounts nicht perfekt — Invite-Code manuell eingeben)

**Bekannte Einschraenkung:** RLS nur mit echtem JWT aktiv (CLAUDE.md). Dashboard-Tests bypassen RLS — nur echte Swift-Client-Tests zaehlen.
  </how-to-verify>
  <resume-signal>
Tippe eine der folgenden Optionen:
- "passed" — alle 5 Success Criteria bestanden
- "partial: SC-1 SC-2 SC-5" — welche SC bestanden haben (Nummer angeben)
- Beschreibe aufgetretene Fehler fuer Diagnose
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| FamilyScoreApp @StateObject → Views | FamilyService-Instanz ist App-Singleton; kein mehfaches Instanziieren moeglich via @StateObject |
| Physisches Geraet → Supabase | Echter JWT im Authorization-Header; RLS ist aktiv (nicht bypassed wie im Dashboard) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-11 | Elevation of Privilege | FamilyScoreApp.environmentObject Injektion | accept | @StateObject erzeugt eine einzige Instanz; kein Code-Pfad erlaubt das Ersetzen durch manipulierten Service |
| T-3-12 | Information Disclosure | MemberListView currentUserIsAdmin Heuristik | accept | Admin-Check ist nur fuer UI-Sichtbarkeit; Server-seitiger SECURITY DEFINER Check ist die echte Sicherheitsgrenze; ein falscher Client-Admin-Wert fuehrt zu RPC-Fehler, nicht zu Datenverlust |
</threat_model>

<verification>
## Plan 04 Verifikation

**RootView zeigt FamilyOnboardingView:**
```bash
Select-String "FamilyOnboardingView" FamilyScore/Views/RootView.swift
# Erwartet: Treffer (im .authenticated(hasFamily: false) Case)
```

**FamilyService in App-Entry-Point:**
```bash
Select-String "familyService" FamilyScore/FamilyScore/FamilyScoreApp.swift
# Erwartet: Mindestens 2 Treffer (@StateObject + .environmentObject)
```

**Vollstaendige Test-Suite gruen:**
```bash
xcodebuild test -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" 2>&1 | Select-String "TEST PASSED|FAIL"
# Erwartet: TEST PASSED, keine FAIL-Zeilen
```

**Phase-3-Requirements abgedeckt:**
- FAM-01: createFamily() RPC + Admin-Zuweisung (Plan 02 Task 2, Plan 04 Checkpoint SC-1)
- FAM-02: generateInvite() + accept_invite RPC + Cross-Device-Fluss (Plan 02 Task 2, Plan 04 Checkpoint SC-2)
- FAM-03: removeMember() RPC + Last-Admin-Schutz (Plan 02 Task 2, Plan 04 Checkpoint SC-3)
- FAM-04: changeMemberRole() RPC (Plan 02 Task 2, Plan 04 Checkpoint SC-4)
- FAM-05: MemberListView + MemberRow mit display_name + avatar_color + role (Plan 03 Task 2)
- KID-01: child_profiles-Tabelle + AddChildView + createChildProfile() (Plan 03 Task 2, Plan 04 Checkpoint SC-5)
- SETTINGS-03: MemberListView Admin-Aktionen (entfernen + Rolle + Invite + Kind hinzufuegen) (Plan 03 Task 2)
</verification>

<success_criteria>
- RootView.swift: FamilyOnboardingView im .authenticated(hasFamily: false) Case
- FamilyScoreApp.swift: @StateObject familyService + .environmentObject(familyService)
- xcodebuild test → TEST PASSED (0 Failures, mindestens 18 Tests)
- Geraete-Checkpoint: SC-1 bis SC-5 bestanden (oder: SC-2/SC-3 mit Simulator-Fallback als akzeptiert markiert)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/03-family-core/03-04-SUMMARY.md` erstellen mit:
- App-Verdrahtung: RootView + FamilyScoreApp Status
- Test-Ergebnis: X/18 Tests gruen
- Geraete-Checkpoint: Bestanden / Teilweise / Ausstehend
  - SC-1 Familie erstellen: PASS/FAIL
  - SC-2 Cross-Device Invite: PASS/FAIL/Simulator
  - SC-3 Mitglied entfernen: PASS/FAIL
  - SC-4 Rolle aendern: PASS/FAIL
  - SC-5 Kind-Profil: PASS/FAIL
- Phase 3 Status: COMPLETE / PARTIAL
</output>
