---
phase: 05-realtime-widgets
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift
  - FamilyScore/FamilyScoreWidgetExtension/FamilyScoreWidget.swift
  - FamilyScore/FamilyScore/project.yml
  - FamilyScore/FamilyScoreTests/RealtimeServiceTests.swift
  - FamilyScore/FamilyScoreTests/Mocks/MockRealtimeService.swift
  - FamilyScore/FamilyScoreTests/WidgetDataWriterTests.swift
  - FamilyScore/FamilyScoreTests/LogActivityIntentTests.swift
  - FamilyScore/FamilyScoreTests/WidgetViewTests.swift
autonomous: false
requirements:
  - SYNC-01
  - SYNC-02
  - WIDGET-01
  - WIDGET-02
  - WIDGET-03
  - WIDGET-04
  - WIDGET-05

must_haves:
  truths:
    - "WidgetData.swift enthaelt alle 5 neuen Felder (myDutyProgress, myLeisureProgress, myScoreProgress, myTodayScore, myTodayScoreGoal) plus avatarColor in MemberScore — mit Default-Werten fuer Rueckwaertskompatibilitaet"
    - "FamilyScoreEntry hat ein data: WidgetData Feld — alle Widget-Views koennen darauf zugreifen"
    - "URL-Scheme familyscore:// ist in project.yml eingetragen und damit in der generierten Info.plist verfuegbar"
    - "5 Test-Dateien existieren (RealtimeServiceTests, MockRealtimeService, WidgetDataWriterTests, LogActivityIntentTests, WidgetViewTests) und bauen ohne Fehler"
    - "App Group Verifikation auf physischem Geraet bestaetigt (verifyAppGroup() debug function aufgerufen via Sideloadly)"
    - "Supabase Realtime-Publikation enthaelt activity_entries und weekly_summaries (Dashboard verifiziert)"
  artifacts:
    - path: "FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift"
      provides: "Erweiterter WidgetData-Struct mit allen Phase-5-Feldern"
      contains: "myTodayScore"
    - path: "FamilyScore/FamilyScoreWidgetExtension/FamilyScoreWidget.swift"
      provides: "FamilyScoreEntry mit data: WidgetData Feld"
      contains: "data: WidgetData"
    - path: "FamilyScore/FamilyScore/project.yml"
      provides: "CFBundleURLSchemes familyscore registriert"
      contains: "familyscore"
    - path: "FamilyScore/FamilyScoreTests/Mocks/MockRealtimeService.swift"
      provides: "RealtimeServiceProtocol + MockRealtimeService"
      exports: ["RealtimeServiceProtocol", "MockRealtimeService"]
    - path: "FamilyScore/FamilyScoreTests/RealtimeServiceTests.swift"
      provides: "5 Test-Stubs fuer SYNC-01/02"
      contains: "RealtimeServiceTests"
    - path: "FamilyScore/FamilyScoreTests/WidgetDataWriterTests.swift"
      provides: "Tests fuer App Group Lesen/Schreiben"
      contains: "WidgetDataWriterTests"
    - path: "FamilyScore/FamilyScoreTests/LogActivityIntentTests.swift"
      provides: "Tests fuer LogActivityIntent.perform() App-Group-Schreiben"
      contains: "LogActivityIntentTests"
    - path: "FamilyScore/FamilyScoreTests/WidgetViewTests.swift"
      provides: "Tests fuer Widget-View-Logik (Sortierung, Progress)"
      contains: "WidgetViewTests"
  key_links:
    - from: "FamilyScore/FamilyScoreTests/Mocks/MockRealtimeService.swift"
      to: "FamilyScore/FamilyScore/Services/RealtimeService.swift"
      via: "@testable import FamilyScore"
      pattern: "RealtimeServiceProtocol"
    - from: "FamilyScore/FamilyScoreWidgetExtension/FamilyScoreWidget.swift"
      to: "FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift"
      via: "import FamilyScoreKit"
      pattern: "FamilyScoreEntry.*data.*WidgetData"
---

<objective>
Wave 0: Infrastruktur-Fundament fuer Phase 5 aufbauen.

Purpose: Alle nachfolgenden Waves (1-3) koennen ohne Vertragsunsicherheiten arbeiten. Identisches Muster wie Phase 2 Wave 0 (Protokoll-first, Mock, Test-Stubs) und Phase 4 Wave 0 (Protokoll + SQL). Dieser Plan stellt die Datenfundamente sicher: WidgetData-Erweiterung, FamilyScoreEntry-Erweiterung, URL-Scheme, und die vollstaendige Test-Infrastruktur.

Output:
- WidgetData.swift mit 5 neuen Feldern (rueckwaertskompatibel via Default-Werte)
- FamilyScoreEntry erweitert um data: WidgetData
- URL-Scheme familyscore:// in project.yml
- RealtimeServiceProtocol + MockRealtimeService (Test-Target)
- 5 Test-Dateien mit ~15 Stubs fuer SYNC-01/02 und WIDGET-01..05
- App Group Checkpoint (physisches Geraet, Sideloadly)
- Supabase Realtime Publikation Checkpoint
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-realtime-widgets/05-RESEARCH.md
@.planning/phases/05-realtime-widgets/05-PATTERNS.md

<interfaces>
<!-- Bestehende Contracts — Executor MUSS diese vor der Implementierung lesen -->

Aus FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift (MODIFY — bestehendes Struct erweitern):
```swift
// Bestehendes Struct Phase 1 — NICHT loeschen, nur erweitern
public struct WidgetData: Codable, Sendable {
    public struct MemberScore: Codable, Sendable {
        public let displayName: String
        public let avatarInitial: String
        public let weeklyPoints: Double
        public let weeklyMinutes: Int
        // Phase 5 NEU: avatarColor: String = "#007AFF"
    }
    public let familyName: String
    public let members: [MemberScore]
    public let lastUpdated: Date
    // Phase 5 NEU: myDutyProgress, myLeisureProgress, myScoreProgress, myTodayScore, myTodayScoreGoal
}
public let appGroupIdentifier = "group.com.familyscore"
```

Aus FamilyScoreWidgetExtension/FamilyScoreWidget.swift (MODIFY — FamilyScoreEntry erweitern):
```swift
// Phase 1 Placeholder — data-Feld hinzufuegen
struct FamilyScoreEntry: TimelineEntry {
    let date: Date
    // Phase 5 NEU: let data: WidgetData
}
struct FamilyScoreProvider: TimelineProvider {
    func placeholder(in context: Context) -> FamilyScoreEntry {
        FamilyScoreEntry(date: Date())  // Phase 5: data: .placeholder hinzufuegen
    }
    // getSnapshot und getTimeline ebenfalls mit data befuellen
}
```

Aus FamilyScoreTests/Mocks/MockFamilyService.swift (exakter Analog fuer MockRealtimeService):
```swift
@MainActor
protocol FamilyServiceProtocol: AnyObject { ... }
@MainActor
final class MockFamilyService: ObservableObject, FamilyServiceProtocol {
    var shouldFailOn[Action]: Bool = false
    var [action]CallCount: Int = 0
}
```

Aus FamilyScoreTests/FamilyServiceTests.swift (exakter Analog fuer RealtimeServiceTests):
```swift
@MainActor
final class FamilyServiceTests: XCTestCase {
    var mock: MockFamilyService!
    override func setUp() async throws { mock = MockFamilyService() }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: WidgetData.swift erweitern + FamilyScoreEntry um data: WidgetData ergaenzen</name>
  <files>
    FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift
    FamilyScore/FamilyScoreWidgetExtension/FamilyScoreWidget.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift (VOLLSTAENDIG lesen — bestehende Felder, init(), placeholder extension — NICHTS loeschen)
    - FamilyScore/FamilyScoreWidgetExtension/FamilyScoreWidget.swift (VOLLSTAENDIG lesen — FamilyScoreEntry, FamilyScoreProvider, FamilyScoreWidgetEntryView)
    - .planning/phases/05-realtime-widgets/05-PATTERNS.md (Abschnitt WidgetData.swift und FamilyScoreEntry — exakte neue Felder und Default-Werte)
    - .planning/phases/05-realtime-widgets/05-RESEARCH.md (Pattern 2: WidgetData-Felder; Pattern 3: getTimeline-Logik)
  </read_first>
  <behavior>
    - WidgetData.MemberScore bekommt ein neues Feld avatarColor: String mit Default "#007AFF" in init()
    - WidgetData bekommt 5 neue Felder: myDutyProgress, myLeisureProgress, myScoreProgress (alle Double), myTodayScore, myTodayScoreGoal (beide Int)
    - Alle neuen init()-Parameter haben Default-Werte (0 oder 0.0) fuer Rueckwaertskompatibilitaet
    - WidgetData.placeholder wird aktualisiert: avatarColor, myDutyProgress: 0.6, myLeisureProgress: 0.4, myScoreProgress: 0.7, myTodayScore: 42, myTodayScoreGoal: 60
    - FamilyScoreEntry.data: WidgetData wird hinzugefuegt (KEIN optional — immer befuellt)
    - FamilyScoreProvider.placeholder() gibt FamilyScoreEntry(date: Date(), data: .placeholder) zurueck
    - FamilyScoreProvider.getSnapshot() und getTimeline() geben FamilyScoreEntry(date: .now, data: .placeholder) zurueck (echter Daten-Read kommt in Wave 1)
    - KEIN import Supabase in WidgetData.swift oder FamilyScoreWidget.swift
    - FamilyScoreKit hat kein Supabase-SDK als Abhaengigkeit
  </behavior>
  <action>
Modifiziere `FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift`:

1. In `MemberScore` das Feld `public let avatarColor: String` hinzufuegen.
2. Im `MemberScore.init()` den Parameter `avatarColor: String = "#007AFF"` hinzufuegen und zuweisen.
3. In `WidgetData` nach `lastUpdated: Date` die 5 neuen Felder hinzufuegen:
```swift
// NEU Phase 5: WIDGET-05 Persoenliche Ringe
public let myDutyProgress: Double      // 0.0-1.0+ (Pflicht-Ring Tagesfortschritt)
public let myLeisureProgress: Double   // 0.0-1.0+ (Freizeit-Ring Tagesfortschritt)
public let myScoreProgress: Double     // 0.0-1.0+ (Score-Ring Tagesfortschritt)
// NEU Phase 5: WIDGET-01 Lock-Screen-Score-Ring
public let myTodayScore: Int           // heutiger Score in Punkten
public let myTodayScoreGoal: Int       // Tagesziel (Standard: 60 Punkte)
```
4. Im `WidgetData.init()` alle 5 neuen Parameter mit Default-Werten hinzufuegen:
```swift
myDutyProgress: Double = 0,
myLeisureProgress: Double = 0,
myScoreProgress: Double = 0,
myTodayScore: Int = 0,
myTodayScoreGoal: Int = 60
```
5. Im `placeholder` die neuen Felder besetzen: avatarColor auf "#007AFF" bzw. "#FF9500", myDutyProgress: 0.6, myLeisureProgress: 0.4, myScoreProgress: 0.7, myTodayScore: 42, myTodayScoreGoal: 60.

Modifiziere `FamilyScore/FamilyScoreWidgetExtension/FamilyScoreWidget.swift`:

6. `FamilyScoreEntry` erweitern: `let data: WidgetData` hinzufuegen.
7. `FamilyScoreProvider.placeholder()` aendern auf: `FamilyScoreEntry(date: Date(), data: .placeholder)`
8. `FamilyScoreProvider.getSnapshot()` aendern: `completion(FamilyScoreEntry(date: .now, data: .placeholder))`
9. `FamilyScoreProvider.getTimeline()` aendern: entry mit `data: .placeholder` erstellen. Stundlicher Fallback bleibt.
10. `FamilyScoreWidgetEntryView` zeigt vorlaeufig `Text("\(entry.data.familyName)")` — echter Widget-Inhalt kommt in Wave 1.
  </action>
  <verify>
    <automated>grep "myTodayScore" "FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift" && grep "data: WidgetData" "FamilyScore/FamilyScoreWidgetExtension/FamilyScoreWidget.swift"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "myTodayScore" FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift` gibt Treffer
    - `grep "myDutyProgress" FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift` gibt Treffer
    - `grep "myLeisureProgress" FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift` gibt Treffer
    - `grep "myScoreProgress" FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift` gibt Treffer
    - `grep "myTodayScoreGoal" FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift` gibt Treffer
    - `grep "avatarColor" FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift` gibt Treffer
    - `grep "data: WidgetData" FamilyScore/FamilyScoreWidgetExtension/FamilyScoreWidget.swift` gibt Treffer
    - `grep "import Supabase" FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift` gibt KEINEN Treffer
    - `grep "weeklyPoints" FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift` gibt Treffer (bestehendes Feld NICHT geloescht)
  </acceptance_criteria>
  <done>WidgetData.swift hat 5 neue Felder mit Default-Werten. FamilyScoreEntry.data ist vorhanden. Rueckwaertskompatibilitaet durch Default-Parameter erhalten. Kein Supabase-Import.</done>
</task>

<task type="auto">
  <name>Task 2: URL-Scheme in project.yml + Test-Infrastruktur (MockRealtimeService + 5 Test-Dateien)</name>
  <files>
    FamilyScore/FamilyScore/project.yml
    FamilyScore/FamilyScoreTests/Mocks/MockRealtimeService.swift
    FamilyScore/FamilyScoreTests/RealtimeServiceTests.swift
    FamilyScore/FamilyScoreTests/WidgetDataWriterTests.swift
    FamilyScore/FamilyScoreTests/LogActivityIntentTests.swift
    FamilyScore/FamilyScoreTests/WidgetViewTests.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/project.yml (VOLLSTAENDIG lesen — bestehende CFBundleURLTypes-Eintraege pruefen; Schluesselpfad fuer URL-Scheme-Eintrag finden)
    - FamilyScore/FamilyScoreTests/Mocks/MockFamilyService.swift (exakter Analog fuer MockRealtimeService — Verhaltens-Flags, Call-Count, Protocol-Definition)
    - FamilyScore/FamilyScoreTests/FamilyServiceTests.swift (exakter Analog fuer RealtimeServiceTests — setUp/tearDown Muster)
    - .planning/phases/05-realtime-widgets/05-PATTERNS.md (Abschnitte MockRealtimeService, RealtimeServiceTests, WidgetDataWriterTests, LogActivityIntentTests, WidgetViewTests — vollstaendige Code-Fragmente)
  </read_first>
  <action>
**Teil A — URL-Scheme in project.yml:**

Lese project.yml und fuege unter dem `FamilyScore`-App-Target in der `info`-Sektion folgendes ein (falls CFBundleURLTypes noch nicht existiert):

```yaml
CFBundleURLTypes:
  - CFBundleURLSchemes:
      - familyscore
    CFBundleURLName: com.familyscore
```

Das genaue YAML-Einrueckungsniveau muss zum bestehenden `info:`-Block passen. Pruefe zuerst ob `CFBundleURLTypes` bereits existiert — wenn ja, nur `familyscore` zum Schemes-Array hinzufuegen.

**Teil B — MockRealtimeService.swift (Target: FamilyScoreTests ONLY):**

```swift
// FamilyScoreTests/Mocks/MockRealtimeService.swift
// Target Membership: FamilyScoreTests ONLY
import Foundation
import Combine
@testable import FamilyScore

@MainActor
protocol RealtimeServiceProtocol: AnyObject {
    var isListening: Bool { get }
    func startListening(familyId: UUID) async
    func stopListening() async
    func refetchAndResubscribe(familyId: UUID, activityService: ActivityService) async
}

@MainActor
final class MockRealtimeService: ObservableObject, RealtimeServiceProtocol {
    @Published private(set) var isListening: Bool = false

    var startListeningCallCount: Int = 0
    var stopListeningCallCount: Int = 0
    var refetchCallCount: Int = 0
    var lastFamilyId: UUID? = nil

    func startListening(familyId: UUID) async {
        startListeningCallCount += 1
        lastFamilyId = familyId
        isListening = true
    }

    func stopListening() async {
        stopListeningCallCount += 1
        isListening = false
    }

    func refetchAndResubscribe(familyId: UUID, activityService: ActivityService) async {
        refetchCallCount += 1
        await startListening(familyId: familyId)
    }
}
```

**Teil C — RealtimeServiceTests.swift (Target: FamilyScoreTests):**

```swift
// FamilyScoreTests/RealtimeServiceTests.swift
// STUBS — Wave 0; MockRealtimeService-basiert, kein echter Supabase-Channel
// Anforderungen: SYNC-01, SYNC-02

import XCTest
@testable import FamilyScore

@MainActor
final class RealtimeServiceTests: XCTestCase {

    var mock: MockRealtimeService!

    override func setUp() async throws {
        try await super.setUp()
        mock = MockRealtimeService()
    }

    override func tearDown() async throws {
        mock = nil
        try await super.tearDown()
    }

    // SYNC-01: startListening() setzt isListening = true
    func testStartListeningSetsIsListening() async throws {
        XCTAssertFalse(mock.isListening)
        await mock.startListening(familyId: UUID())
        XCTAssertTrue(mock.isListening)
    }

    // SYNC-01: stopListening() setzt isListening = false
    func testStopListeningSetsNotListening() async throws {
        await mock.startListening(familyId: UUID())
        await mock.stopListening()
        XCTAssertFalse(mock.isListening)
    }

    // SYNC-01: XCTestConfigurationFilePath-Guard verhindert echten Connect
    // Echter RealtimeService hat diesen Guard als ERSTE Zeile in startListening()
    func testXCTestGuardPreventsSupabaseConnect() async throws {
        // Mock macht NIE echten Supabase-Call — Guard-Existenz im echten Service dokumentiert
        let envValue = ProcessInfo.processInfo.environment["XCTestConfigurationFilePath"]
        XCTAssertNotNil(envValue, "XCTest-Umgebung muss XCTestConfigurationFilePath setzen")
    }

    // SYNC-01: refetchAndResubscribe ruft startListening auf
    func testRefetchAndResubscribeCallsStartListening() async throws {
        let familyId = UUID()
        // ActivityService Stub brauchen wir nicht — Mock ignoriert ihn
        await mock.startListening(familyId: familyId)  // Direkter Mock-Call als Proxy
        XCTAssertEqual(mock.startListeningCallCount, 1)
        XCTAssertEqual(mock.lastFamilyId, familyId)
    }

    // SYNC-02: Mehrfach-Subscribe ohne Fehler (idempotent)
    func testMultipleStartListeningCallsAreIdempotent() async throws {
        await mock.startListening(familyId: UUID())
        await mock.startListening(familyId: UUID())
        XCTAssertEqual(mock.startListeningCallCount, 2)
        XCTAssertTrue(mock.isListening)
    }
}
```

**Teil D — WidgetDataWriterTests.swift (Target: FamilyScoreTests):**

```swift
// FamilyScoreTests/WidgetDataWriterTests.swift
// Zweck: App Group Lesen/Schreiben ohne echte WidgetKit-Runtime verifizieren
import XCTest
@testable import FamilyScore
import FamilyScoreKit

@MainActor
final class WidgetDataWriterTests: XCTestCase {

    // WIDGET-01..05: WidgetData kann JSON-kodiert und dekodiert werden
    func testWidgetDataEncodesAndDecodesCorrectly() throws {
        let testData = WidgetData.placeholder
        let encoded = try JSONEncoder().encode(testData)
        let decoded = try JSONDecoder().decode(WidgetData.self, from: encoded)
        XCTAssertEqual(decoded.familyName, testData.familyName)
        XCTAssertEqual(decoded.myTodayScore, testData.myTodayScore)
        XCTAssertEqual(decoded.myDutyProgress, testData.myDutyProgress, accuracy: 0.001)
    }

    // WIDGET-05: myDutyProgress korrekt kodiert/dekodiert
    func testMyDutyProgressDecodesCorrectly() throws {
        let data = WidgetData(
            familyName: "Test",
            members: [],
            lastUpdated: .now,
            myDutyProgress: 0.75,
            myLeisureProgress: 0.5,
            myScoreProgress: 0.6,
            myTodayScore: 45,
            myTodayScoreGoal: 60
        )
        let encoded = try JSONEncoder().encode(data)
        let decoded = try JSONDecoder().decode(WidgetData.self, from: encoded)
        XCTAssertEqual(decoded.myDutyProgress, 0.75, accuracy: 0.001)
        XCTAssertEqual(decoded.myLeisureProgress, 0.5, accuracy: 0.001)
    }

    // WIDGET-01: myTodayScore und myTodayScoreGoal korrekt gesetzt
    func testTodayScoreFieldsCorrect() throws {
        let data = WidgetData(familyName: "F", members: [], lastUpdated: .now,
                              myTodayScore: 42, myTodayScoreGoal: 60)
        XCTAssertEqual(data.myTodayScore, 42)
        XCTAssertEqual(data.myTodayScoreGoal, 60)
    }

    // App Group: readFromAppGroup() liefert nil wenn nichts geschrieben
    func testReadFromAppGroupReturnsNilWhenEmpty() {
        let defaults = UserDefaults(suiteName: appGroupIdentifier)
        defaults?.removeObject(forKey: "widgetData")
        let result = WidgetData.readFromAppGroup()
        // In CI (Simulator ohne App Group) liefert readFromAppGroup nil — akzeptabel
        // Auf Geraet mit konfigurierter App Group wird nil erwartet wenn leer
        _ = result  // Kein XCTAssertNil — App Group Verfuegbarkeit ist Geraet-abhaengig
        XCTAssertTrue(true, "Kein Crash wenn App Group leer oder nicht verfuegbar")
    }
}
```

**Teil E — LogActivityIntentTests.swift (Target: FamilyScoreTests):**

```swift
// FamilyScoreTests/LogActivityIntentTests.swift
// Zweck: LogActivityIntent.perform() ohne Widget-Runtime testen
import XCTest
@testable import FamilyScore
import FamilyScoreKit

@MainActor
final class LogActivityIntentTests: XCTestCase {

    override func setUp() async throws {
        try await super.setUp()
        UserDefaults(suiteName: appGroupIdentifier)?.removeObject(forKey: "pendingLogs")
    }

    // WIDGET-04: perform() schreibt pending_log in App Group
    func testPerformWritesPendingLog() async throws {
        let intent = LogActivityIntent(categorySlug: "haushalt")
        _ = try await intent.perform()
        let defaults = UserDefaults(suiteName: appGroupIdentifier)
        let pending = defaults?.array(forKey: "pendingLogs") as? [[String: String]]
        XCTAssertNotNil(pending)
        XCTAssertEqual(pending?.count, 1)
        XCTAssertEqual(pending?.first?["category"], "haushalt")
    }

    // WIDGET-04: perform() bei leerem Array erstellt neues Array
    func testPerformCreatesArrayWhenEmpty() async throws {
        let intent = LogActivityIntent(categorySlug: "freizeit")
        _ = try await intent.perform()
        let pending = UserDefaults(suiteName: appGroupIdentifier)?
            .array(forKey: "pendingLogs") as? [[String: String]]
        XCTAssertEqual(pending?.count, 1)
        XCTAssertEqual(pending?.first?["category"], "freizeit")
    }

    // WIDGET-04: Zwei perform()-Aufrufe ergeben zwei Eintraege
    func testMultiplePerformCallsAppendEntries() async throws {
        let intent1 = LogActivityIntent(categorySlug: "haushalt")
        let intent2 = LogActivityIntent(categorySlug: "arbeit")
        _ = try await intent1.perform()
        _ = try await intent2.perform()
        let pending = UserDefaults(suiteName: appGroupIdentifier)?
            .array(forKey: "pendingLogs") as? [[String: String]]
        XCTAssertEqual(pending?.count, 2)
    }
}
```

**Teil F — WidgetViewTests.swift (Target: FamilyScoreTests):**

```swift
// FamilyScoreTests/WidgetViewTests.swift
// Zweck: Widget-View-Logik (Sortierung, Progress-Berechnungen) ohne Widget-Runtime
import XCTest
@testable import FamilyScore
import FamilyScoreKit

@MainActor
final class WidgetViewTests: XCTestCase {

    // WIDGET-03: Familienranking sortiert Members nach weeklyPoints absteigend
    func testFamilyRankingViewSortsMembersDescending() {
        let members = [
            WidgetData.MemberScore(displayName: "Max", avatarInitial: "M",
                                   weeklyPoints: 50, weeklyMinutes: 40, avatarColor: "#007AFF"),
            WidgetData.MemberScore(displayName: "Anna", avatarInitial: "A",
                                   weeklyPoints: 80, weeklyMinutes: 60, avatarColor: "#FF9500")
        ]
        let sorted = members.sorted { $0.weeklyPoints > $1.weeklyPoints }
        XCTAssertEqual(sorted.first?.displayName, "Anna")
        XCTAssertEqual(sorted.last?.displayName, "Max")
    }

    // WIDGET-05: Progress kann > 1.0 sein (zweite Runde)
    func testRingProgressCanExceedOneForSecondLap() {
        let progress = 1.5  // zweite Runde
        XCTAssertGreaterThan(progress, 1.0)
        let secondLap = progress - 1.0
        XCTAssertEqual(secondLap, 0.5, accuracy: 0.001)
    }

    // WIDGET-01: Gauge-Berechnung score/goal
    func testGaugeValueCalculation() {
        let score = 42
        let goal = 60
        let gaugeValue = Double(score) / Double(goal)
        XCTAssertEqual(gaugeValue, 0.7, accuracy: 0.001)
    }

    // WIDGET-01: Goal 0 verursacht keinen Division-by-Zero — max(1, goal) Pattern
    func testGaugeValueWithZeroGoalUsesMaxSafeguard() {
        let score = 10
        let goal = max(1, 0)  // Safeguard aus ScoreRingView
        let gaugeValue = Double(score) / Double(goal)
        XCTAssertEqual(gaugeValue, 10.0)  // 10/1 = 10 (Ueberlauf, aber kein Crash)
    }

    // WIDGET-05: Default-Werte aus WidgetData.init() sind 0
    func testWidgetDataDefaultsToZeroProgress() {
        let data = WidgetData(familyName: "Test", members: [], lastUpdated: .now)
        XCTAssertEqual(data.myDutyProgress, 0.0)
        XCTAssertEqual(data.myLeisureProgress, 0.0)
        XCTAssertEqual(data.myScoreProgress, 0.0)
        XCTAssertEqual(data.myTodayScore, 0)
        XCTAssertEqual(data.myTodayScoreGoal, 60)
    }
}
```
  </action>
  <verify>
    <automated>grep "familyscore" "FamilyScore/FamilyScore/project.yml" && grep "RealtimeServiceProtocol" "FamilyScore/FamilyScoreTests/Mocks/MockRealtimeService.swift"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "familyscore" FamilyScore/FamilyScore/project.yml` gibt Treffer
    - `grep "CFBundleURLSchemes\|CFBundleURLTypes" FamilyScore/FamilyScore/project.yml` gibt Treffer
    - `grep "protocol RealtimeServiceProtocol" FamilyScore/FamilyScoreTests/Mocks/MockRealtimeService.swift` gibt Treffer
    - `grep "class MockRealtimeService" FamilyScore/FamilyScoreTests/Mocks/MockRealtimeService.swift` gibt Treffer
    - `grep "testStartListeningSetsIsListening\|testStopListeningSetsNotListening\|testXCTestGuardPreventsSupabaseConnect" FamilyScore/FamilyScoreTests/RealtimeServiceTests.swift` gibt 3 Treffer
    - `grep "testWidgetDataEncodesAndDecodesCorrectly" FamilyScore/FamilyScoreTests/WidgetDataWriterTests.swift` gibt Treffer
    - `grep "testPerformWritesPendingLog" FamilyScore/FamilyScoreTests/LogActivityIntentTests.swift` gibt Treffer
    - `grep "testFamilyRankingViewSortsMembersDescending" FamilyScore/FamilyScoreTests/WidgetViewTests.swift` gibt Treffer
    - Alle 6 Dateien existieren im Dateisystem
  </acceptance_criteria>
  <done>URL-Scheme familyscore:// in project.yml eingetragen. MockRealtimeService mit RealtimeServiceProtocol, 5 Stub-Test-Klassen mit insgesamt 15 Tests erstellt. Alle Dateien kompilieren ohne Fehler (Build verifizieren via CI).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>[BLOCKING] App Group SC-2 + Supabase Realtime-Publikation verifizieren</name>
  <what-built>
    Wave 0 hat WidgetData.swift erweitert, FamilyScoreEntry um data: WidgetData ergaenzt, URL-Scheme eingetragen und die gesamte Test-Infrastruktur erstellt. Jetzt muss die physische App Group (SC-2 aus Phase 1) und die Supabase Realtime-Publikation verifiziert werden, bevor Widget-UI in Wave 1 gebaut wird.
  </what-built>
  <how-to-verify>
    **Schritt 1 — CI-Build gruenhalten:**
    Nach git push: `gh run list --limit 3` pruefen. Build muss gruen sein.

    **Schritt 2 — App Group auf physischem iPhone (Sideloadly, Windows):**
    1. CI-Artifact (`.ipa`) herunterladen aus GitHub Actions Artifacts
    2. Sideloadly oeffnen → `.ipa` auf iPhone sideloaden
    3. App starten → in den Logs (Xcode Organizer oder Appetize) nach `[AppGroup]` suchen
    4. ODER: In FamilyScoreApp.swift ist `verifyAppGroup()` bereits vorhanden — diese Funktion loggt "App Group OK" oder "App Group FEHLER"
    5. Erwartetes Ergebnis: "App Group OK" — falls "FEHLER": Apple Developer Portal pruefen (App Group muss fuer BEIDE App-IDs registriert sein: com.familyscore + com.familyscore.widget)

    **Schritt 3 — Supabase Realtime Publikation pruefen:**
    Im Supabase Dashboard → Database → Replication → Publications:
    Pruefen ob `supabase_realtime` die Tabellen `activity_entries` UND `weekly_summaries` enthaelt.
    Falls NICHT: Im SQL-Editor ausfuehren:
    ```sql
    ALTER PUBLICATION supabase_realtime ADD TABLE public.activity_entries;
    ALTER PUBLICATION supabase_realtime ADD TABLE public.weekly_summaries;
    ```
    Dann pruefen: `SELECT tablename FROM pg_publication_tables WHERE pubname = 'supabase_realtime';`
    Muss beide Tabellen zeigen.
  </how-to-verify>
  <resume-signal>Tippe "wave0 ok" wenn: (1) CI-Build gruen, (2) App Group auf Geraet verifiziert (OK oder Fehler dokumentiert), (3) Realtime-Publikation enthaelt activity_entries + weekly_summaries</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Widget Extension → App Group UserDefaults | Widget liest nur Display-Daten (Score, Name) — niemals Auth-Tokens oder APNs-Tokens |
| App → App Group UserDefaults | App schreibt JSON-kodiertes WidgetData — kein Netzwerkzugriff im Widget-Prozess |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|------------|-------------|-----------------|
| T-5-01 | Information Disclosure | App Group UserDefaults | mitigate | Nur Display-Daten (familyName, weeklyPoints, myTodayScore) in App Group schreiben; NIEMALS auth tokens, apns_token, user passwords |
| T-5-02 | Spoofing | URL-Scheme familyscore:// | accept | iOS oeffnet nur die registrierte App fuer das Scheme; custom URL scheme ist App-spezifisch; kein PII in URL-Parametern (nur categorySlug wie "haushalt") |
| T-5-03 | Tampering | pendingLogs in App Group | accept | pendingLogs enthalten nur categorySlug + Zeitstempel; kein user_id oder family_id — kein Sicherheitsrisiko; App validiert Slug vor Log-Erstellung |
</threat_model>

<verification>
Nach Abschluss aller Tasks und Checkpoint:

1. `grep "myTodayScore" FamilyScore/FamilyScoreKit/Sources/FamilyScoreKit/WidgetData.swift` — gibt Treffer
2. `grep "data: WidgetData" FamilyScore/FamilyScoreWidgetExtension/FamilyScoreWidget.swift` — gibt Treffer
3. `grep "familyscore" FamilyScore/FamilyScore/project.yml` — gibt Treffer
4. CI-Build gruen nach git push
5. App Group auf physischem Geraet verifiziert (Checkpoint)
6. Supabase Realtime Publikation enthaelt activity_entries + weekly_summaries (Checkpoint)
</verification>

<success_criteria>
- WidgetData.swift hat 5 neue Felder mit Default-Werten; avatarColor in MemberScore vorhanden
- FamilyScoreEntry.data: WidgetData ist vorhanden; Placeholder-Provider nutzt .placeholder
- URL-Scheme familyscore:// in project.yml eingetragen
- 5 Test-Dateien (MockRealtimeService, RealtimeServiceTests, WidgetDataWriterTests, LogActivityIntentTests, WidgetViewTests) kompilieren ohne Fehler
- App Group SC-2 auf physischem iPhone verifiziert
- Supabase Realtime Publikation enthaelt beide Tabellen
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/05-realtime-widgets/05-01-SUMMARY.md` mit:
- Welche Dateien erstellt/modifiziert wurden
- Ergebnis App Group Checkpoint (OK / Fehler + Massnahme)
- Ergebnis Supabase Realtime Publikation (vorhanden / hinzugefuegt)
- CI-Build Status
</output>
