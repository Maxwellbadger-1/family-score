---
phase: 06-settings-polish
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - FamilyScore/FamilyScore/Models/CategoryConfig.swift
  - FamilyScoreTests/Mocks/MockCategoryService.swift
  - FamilyScoreTests/CategoryServiceTests.swift
  - FamilyScore/supabase/migrations/20260517_phase6_point_weight_constraint.sql
autonomous: true
requirements:
  - SETTINGS-01
  - SETTINGS-02
must_haves:
  truths:
    - "CategoryConfig-Struct dekodiert alle Felder aus category_config-Tabelle korrekt"
    - "MockCategoryService implementiert CategoryServiceProtocol mit korrekten Call-Countern"
    - "CategoryServiceTests enthalten Stubs fuer testToggleCategoryUpdatesLocalState, testEnabledCategoriesFilter, testUpdateWeightUpdatesLocalState"
    - "SQL-Migration mit CHECK-Constraint (point_weight > 0 AND point_weight <= 10) existiert und wurde gepusht"
  artifacts:
    - path: "FamilyScore/FamilyScore/Models/CategoryConfig.swift"
      provides: "Decodable-Struct fuer category_config-Tabelle mit mutablen is_enabled und point_weight"
      min_lines: 20
    - path: "FamilyScoreTests/Mocks/MockCategoryService.swift"
      provides: "CategoryServiceProtocol + MockCategoryService mit Behavior-Flags und Call-Countern"
      exports: ["CategoryServiceProtocol", "MockCategoryService"]
    - path: "FamilyScoreTests/CategoryServiceTests.swift"
      provides: "XCTest-Stubs fuer alle SETTINGS-01/02 Szenarien"
      contains: "testToggleCategoryUpdatesLocalState"
    - path: "FamilyScore/supabase/migrations/20260517_phase6_point_weight_constraint.sql"
      provides: "CHECK-Constraint fuer point_weight"
      contains: "category_config_point_weight_range"
  key_links:
    - from: "FamilyScoreTests/CategoryServiceTests.swift"
      to: "FamilyScoreTests/Mocks/MockCategoryService.swift"
      via: "@testable import + MockCategoryService-Instanz"
      pattern: "MockCategoryService"
    - from: "FamilyScoreTests/Mocks/MockCategoryService.swift"
      to: "FamilyScore/FamilyScore/Models/CategoryConfig.swift"
      via: "CategoryConfig-Struct in Mock-State"
      pattern: "CategoryConfig"
---

<objective>
Wave 0 legt das Fundament fuer alle SETTINGS-01/02-Funktionen: das Datenmodell, die Test-Infrastruktur (Protocol + Mock + Test-Stubs) und die fehlende SQL-Constraint auf point_weight.

Purpose: Ohne CategoryConfig.swift koennen weder CategoryService noch Tests kompilieren. Ohne MockCategoryService koennen alle weiteren Waves nicht getestet werden. Die SQL-Constraint ist ein Security-Blocking-Item (ASVS V5).
Output: CategoryConfig.swift, MockCategoryService.swift, CategoryServiceTests.swift (Stubs), SQL-Migration, gepushte Constraint in Supabase.
</objective>

<execution_context>
@C:/Users/maxfe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxfe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-settings-polish/06-RESEARCH.md
@.planning/phases/06-settings-polish/06-PATTERNS.md
@.planning/phases/06-settings-polish/06-VALIDATION.md

<interfaces>
<!-- Analog-Muster aus bestehender Codebase — KEIN Erkunden noetig -->

Aus FamilyScore/FamilyScore/Models/FamilyMember.swift (Zeilen 1-13):
```swift
struct FamilyMember: Codable, Identifiable {
    let id: UUID
    let family_id: UUID?
    let display_name: String
    let avatar_color: String
    let role: String
    let created_at: Date
}
```

Aus FamilyScoreTests/Mocks/MockFamilyService.swift (Zeilen 10-27) — Protocol-Pattern:
```swift
@MainActor
protocol FamilyServiceProtocol: AnyObject {
    var currentFamily: Family? { get }
    var members: [FamilyMember] { get }
    var serviceError: String? { get set }
    func fetchMembers(familyId: UUID) async
    func changeMemberRole(memberId: UUID, role: MemberRole) async throws
}
@MainActor
final class MockFamilyService: ObservableObject, FamilyServiceProtocol {
    @Published private(set) var currentFamily: Family?
    var shouldThrowOnCreateFamily: Bool = false
    var createFamilyCallCount: Int = 0
}
```

Aus FamilyScoreTests/FamilyServiceTests.swift — Test-Class-Pattern:
```swift
@MainActor
final class FamilyServiceTests: XCTestCase {
    var mock: MockFamilyService!
    override func setUp() async throws {
        try await super.setUp()
        mock = MockFamilyService()
    }
    override func tearDown() async throws {
        mock = nil
        try await super.tearDown()
    }
    func testCreateFamilySetsCurrentFamily() async throws {
        XCTAssertNil(mock.currentFamily)
        let familyId = try await mock.createFamily(name: "Muster-Familie")
        XCTAssertNotNil(mock.currentFamily)
        XCTAssertEqual(mock.createFamilyCallCount, 1)
    }
}
```

category_config DB-Schema (aus 20260515_initial_schema.sql):
- id UUID PRIMARY KEY DEFAULT gen_random_uuid()
- family_id UUID NOT NULL REFERENCES families(id)
- name TEXT NOT NULL
- icon TEXT NOT NULL
- is_enabled BOOLEAN NOT NULL DEFAULT true
- point_weight DOUBLE PRECISION NOT NULL DEFAULT 1.0
- sort_order INTEGER NOT NULL DEFAULT 0
- created_at TIMESTAMPTZ DEFAULT now()
- updated_at TIMESTAMPTZ DEFAULT now()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CategoryConfig-Model + CategoryServiceProtocol + MockCategoryService</name>
  <files>
    FamilyScore/FamilyScore/Models/CategoryConfig.swift
    FamilyScoreTests/Mocks/MockCategoryService.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/Models/FamilyMember.swift — Struct-Pattern (Codable, Identifiable, snake_case-Felder)
    - FamilyScoreTests/Mocks/MockFamilyService.swift — Protocol + Mock Vollstruktur (Zeilen 1-161)
    - FamilyScore/FamilyScore/Models/MemberRole.swift — MemberRole-Enum (Zeilen 1-20)
  </read_first>
  <behavior>
    - CategoryConfig.id ist UUID und Identifiable
    - CategoryConfig.is_enabled ist var (mutable fuer optimistische Updates)
    - CategoryConfig.point_weight ist var (mutable fuer optimistische Updates)
    - MockCategoryService.enabledCategories gibt nur Eintraege mit is_enabled == true zurueck
    - MockCategoryService.toggleCategory(id:isEnabled:) erhoeht toggleCallCount, wirft wenn shouldThrowOnToggle == true
    - MockCategoryService.updateWeight(id:weight:) erhoeht updateWeightCallCount, speichert lastUpdatedWeight
    - CategoryServiceProtocol schliesst fetchCategories, toggleCategory, updateWeight, enabledCategories ein
  </behavior>
  <action>
**CategoryConfig.swift** — exakter Klon von FamilyMember.swift-Pattern mit diesen Aenderungen:
- Struct-Name: `CategoryConfig`
- Felder: `id: UUID`, `family_id: UUID`, `name: String`, `icon: String`, `is_enabled: Bool` (var!), `point_weight: Double` (var!), `sort_order: Int`, `created_at: Date`
- Kein `updated_at`-Feld im Struct noetig (wird nur beim Schreiben gesendet)
- Header-Kommentar: `// Target Membership: FamilyScore (App) ONLY`
- CodingKeys explizit deklarieren fuer snake_case-zu-camelCase-Mapping: `isEnabled` ↔ `is_enabled`, `pointWeight` ↔ `point_weight`, `sortOrder` ↔ `sort_order`, `familyId` ↔ `family_id`, `createdAt` ↔ `created_at`

**MockCategoryService.swift** — exakter Klon von MockFamilyService.swift-Pattern:
1. `CategoryServiceProtocol` (@MainActor, AnyObject) mit:
   - `var categories: [CategoryConfig] { get }`
   - `var enabledCategories: [CategoryConfig] { get }`
   - `var serviceError: String? { get set }`
   - `func fetchCategories(familyId: UUID) async`
   - `func toggleCategory(id: UUID, isEnabled: Bool) async throws`
   - `func updateWeight(id: UUID, weight: Double) async throws`
2. `MockCategoryService` (@MainActor final class, ObservableObject, CategoryServiceProtocol):
   - `@Published private(set) var categories: [CategoryConfig] = []`
   - `@Published var serviceError: String? = nil`
   - `var enabledCategories: [CategoryConfig] { categories.filter { $0.isEnabled } }`
   - Behavior-Flags: `shouldThrowOnToggle: Bool = false`, `shouldThrowOnUpdateWeight: Bool = false`
   - Call-Counter: `toggleCallCount: Int = 0`, `updateWeightCallCount: Int = 0`, `fetchCategoriesCallCount: Int = 0`
   - Letzte Parameter: `lastToggledId: UUID? = nil`, `lastToggledValue: Bool? = nil`, `lastUpdatedId: UUID? = nil`, `lastUpdatedWeight: Double? = nil`
   - `init(initialCategories: [CategoryConfig] = [])` — setzt categories
   - Hilfsmethode: `func setCategories(_ c: [CategoryConfig]) { categories = c }`
   - `toggleCategory`: setzt lastToggledId/lastToggledValue, erhoeht toggleCallCount, wirft `MockCategoryError.toggleFailed` wenn shouldThrowOnToggle, sonst aktualisiert `categories[idx].isEnabled`
   - `updateWeight`: setzt lastUpdatedId/lastUpdatedWeight, erhoeht updateWeightCallCount, wirft `MockCategoryError.updateFailed` wenn shouldThrowOnUpdateWeight, sonst aktualisiert `categories[idx].pointWeight`
   - `fetchCategories`: erhoeht fetchCategoriesCallCount, belaesst categories unveraendert (Test setzt via setCategories)
3. `MockCategoryError` enum: cases `toggleFailed`, `updateFailed` mit deutschen errorDescription-Strings
- Import: `import Foundation`, `import Combine`, `@testable import FamilyScore`
- KEIN `import Supabase` in der Test-Datei
  </action>
  <verify>
    <automated>cd '/mnt/c/Users/maxfe/Maxflow Software/Projekte/Family Score' && grep -n "CategoryServiceProtocol" FamilyScoreTests/Mocks/MockCategoryService.swift && grep -n "isEnabled" FamilyScore/FamilyScore/Models/CategoryConfig.swift && grep -n "pointWeight" FamilyScore/FamilyScore/Models/CategoryConfig.swift</automated>
  </verify>
  <done>
    - CategoryConfig.swift existiert mit `var isEnabled: Bool` und `var pointWeight: Double`
    - MockCategoryService.swift existiert mit CategoryServiceProtocol, MockCategoryService, MockCategoryError
    - `enabledCategories` filtert korrekt nach `isEnabled == true`
    - Alle Behavior-Flags und Call-Counter vorhanden
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CategoryServiceTests-Stubs</name>
  <files>
    FamilyScoreTests/CategoryServiceTests.swift
  </files>
  <read_first>
    - FamilyScoreTests/Mocks/MockCategoryService.swift — gerade erstellt in Task 1; alle Mock-Methoden und Behavior-Flags
    - FamilyScoreTests/FamilyServiceTests.swift — Test-Klassen-Shell (setUp/tearDown, @MainActor, assert-Muster)
  </read_first>
  <behavior>
    - testToggleCategoryUpdatesLocalState: startet mit enabled=true, toggleCategory(isEnabled: false), prueft categories[0].isEnabled == false und toggleCallCount == 1
    - testEnabledCategoriesFilter: 3 Kategorien (2 enabled, 1 disabled), prueft enabledCategories.count == 2
    - testUpdateWeightUpdatesLocalState: startet mit weight=1.0, updateWeight(weight: 2.5), prueft categories[0].pointWeight == 2.5 und updateWeightCallCount == 1
    - testToggleThrowsOnError: shouldThrowOnToggle=true, prueft dass toggleCategory() throws
    - testUpdateWeightThrowsOnError: shouldThrowOnUpdateWeight=true, prueft dass updateWeight() throws
  </behavior>
  <action>
Erstelle FamilyScoreTests/CategoryServiceTests.swift als exakten Klon des FamilyServiceTests.swift-Patterns:
- `@MainActor final class CategoryServiceTests: XCTestCase`
- `var mock: MockCategoryService!`
- setUp/tearDown identisch zu FamilyServiceTests
- Hilfsmethode: `private func makeCategoryConfig(id: UUID = UUID(), isEnabled: Bool = true, pointWeight: Double = 1.0) -> CategoryConfig` — erstellt einen CategoryConfig mit Testdaten (family_id: UUID(), name: "Haushalt", icon: "house.fill", sort_order: 0, created_at: Date())
- Alle 5 Testmethoden implementieren (RED-Phase — sie testen gegen Mock, nicht gegen echten Service)
- Jeder Test-Name beginnt mit `test`
- Imports: `import XCTest`, `@testable import FamilyScore`
- KEIN echter Supabase-Call — alle Tests laufen gegen MockCategoryService
  </action>
  <verify>
    <automated>cd '/mnt/c/Users/maxfe/Maxflow Software/Projekte/Family Score' && grep -c "func test" FamilyScoreTests/CategoryServiceTests.swift</automated>
  </verify>
  <done>
    - CategoryServiceTests.swift existiert mit 5 Testmethoden
    - Alle Tests laufen gegen MockCategoryService (kein echter Supabase-Call)
    - File ist Teil des FamilyScoreTests-Targets (in project.yml automatisch durch sources-path eingeschlossen)
  </done>
</task>

<task type="auto">
  <name>Task 3: SQL-Migration point_weight-Constraint + [BLOCKING] Schema Push</name>
  <files>
    FamilyScore/supabase/migrations/20260517_phase6_point_weight_constraint.sql
  </files>
  <read_first>
    - FamilyScore/supabase/migrations/20260515_initial_schema.sql — vorhandene category_config-Tabellendefinition und Constraint-Syntax-Muster
  </read_first>
  <action>
1. Erstelle FamilyScore/supabase/migrations/20260517_phase6_point_weight_constraint.sql mit exakt diesem Inhalt:
```sql
-- Phase 6: Security-Constraint fuer point_weight
-- Verhindert manipulative Gewichtungen (T-6-02: ASVS V5 Input Validation)
-- point_weight = 0 nicht erlaubt (Kategorie wuerde Punkte vernichten)
-- point_weight > 10 nicht erlaubt (verhindert Score-Inflation durch Admin)

ALTER TABLE public.category_config
  ADD CONSTRAINT category_config_point_weight_range
  CHECK (point_weight > 0 AND point_weight <= 10);
```

2. [BLOCKING] Wende die Migration via MCP-Tool an:
   - Tool: `mcp__supabase__apply_migration`
   - name: `20260517_phase6_point_weight_constraint`
   - sql: (Inhalt der Migration oben)
   
   HINWEIS: NICHT `supabase db push` verwenden — kein CLI auf Windows verfuegbar. Immer MCP-Tool!
   
3. Verifiziere nach dem Push: Query gegen Supabase information_schema um zu pruefen dass die Constraint existiert.
  </action>
  <verify>
    <automated>cd '/mnt/c/Users/maxfe/Maxflow Software/Projekte/Family Score' && grep -n "category_config_point_weight_range" FamilyScore/supabase/migrations/20260517_phase6_point_weight_constraint.sql</automated>
  </verify>
  <done>
    - Migrationsdatei existiert mit CHECK-Constraint
    - Migration wurde via mcp__supabase__apply_migration erfolgreich gepusht
    - Constraint category_config_point_weight_range in Supabase vorhanden (verifiziert via MCP oder information_schema-Query)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| Test-Code → Produktions-Code | MockCategoryService implementiert Protocol; echte Service-Implementierung kommt in Wave 1 |
| Client → Supabase | CHECK-Constraint erzwingt point_weight-Validierung serverseitig — kein Client-Trust |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Elevation of Privilege | category_config UPDATE | mitigate | RLS-Policy "Admin verwaltet Kategorien" aus Phase 1 bereits aktiv; kein neuer Code noetig |
| T-6-02 | Tampering | category_config.point_weight | mitigate | CHECK (point_weight > 0 AND point_weight <= 10) via SQL-Migration in diesem Plan; zusaetzlich Stepper-Range 0.5-10.0 in Wave-1-UI |
| T-6-03 | Elevation of Privilege | Settings-Navigation (DeepLink) | accept | Wird in Wave 1 via Admin-Guard im MainTabView mitigiert; kein Risiko in Wave 0 (kein UI vorhanden) |
| T-6-04 | Information Disclosure | Supabase Anon Key im Binary | accept | Secrets.xcconfig gitignored seit Phase 1; CI injiziert via Secrets; kein Wave-0-Risiko |
</threat_model>

<verification>
Nach Wave 0 gilt Phase 6 als bereit fuer Wave 1 wenn:
1. CI-Build gruen nach `git push` (CategoryConfig.swift + MockCategoryService.swift kompilieren)
2. CategoryServiceTests laufen im CI durch (alle 5 Tests gruene im FamilyScoreTests-Target)
3. SQL-Constraint in Supabase verifiziert

```powershell
# CI-Status pruefen (nach git push):
& "C:\Program Files\GitHub CLI\gh.exe" run list --limit 3
# Bei Fehler:
& "C:\Program Files\GitHub CLI\gh.exe" run view <ID> --log-failed
```
</verification>

<success_criteria>
- CategoryConfig.swift: struct mit `var isEnabled: Bool`, `var pointWeight: Double`, CodingKeys fuer snake_case-Mapping
- MockCategoryService.swift: CategoryServiceProtocol + MockCategoryService + MockCategoryError, kein echter Supabase-Call
- CategoryServiceTests.swift: 5 Test-Stubs, alle gegen MockCategoryService, CI-gruen
- SQL-Migration: CHECK (point_weight > 0 AND point_weight <= 10) in Supabase aktiv
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-settings-polish/06-01-SUMMARY.md` erstellen.
</output>
