---
phase: 06-settings-polish
plan: 02
type: execute
wave: 1
depends_on:
  - 06-01
files_modified:
  - FamilyScore/FamilyScore/Services/CategoryService.swift
  - FamilyScore/FamilyScore/Views/Settings/SettingsView.swift
  - FamilyScore/FamilyScore/Views/Settings/CategorySettingsView.swift
  - FamilyScore/FamilyScore/Views/Settings/MemberSettingsView.swift
  - FamilyScore/FamilyScore/Views/MainTabView.swift
  - FamilyScore/FamilyScore/FamilyScoreApp.swift
autonomous: true
requirements:
  - SETTINGS-01
  - SETTINGS-02
  - KID-03
must_haves:
  truths:
    - "Admin kann Kategorie-Toggle bedienen; Supabase-UPDATE wird ausgefuehrt; Widget-Reload folgt"
    - "Admin kann Punkt-Multiplikator mit Stepper (0.5-10.0, Step 0.5) aendern"
    - "Settings-Tab erscheint nur fuer Admin in MainTabView; fuer Adult/Child-Rolle ist er nicht vorhanden"
    - "MemberSettingsView zeigt Picker 'Modus' pro Mitglied; Aenderung ruft FamilyService.changeMemberRole() auf"
    - "CategoryService ist als @StateObject in FamilyScoreApp injiziert"
  artifacts:
    - path: "FamilyScore/FamilyScore/Services/CategoryService.swift"
      provides: "ObservableObject mit fetchCategories, toggleCategory, updateWeight, enabledCategories"
      exports: ["CategoryService", "CategoryServiceError"]
    - path: "FamilyScore/FamilyScore/Views/Settings/SettingsView.swift"
      provides: "Root-Settings-View mit Admin-Guard, drei Sektionen"
      min_lines: 40
    - path: "FamilyScore/FamilyScore/Views/Settings/CategorySettingsView.swift"
      provides: "Toggle + Stepper pro Kategorie, optimistische UI, Error-Banner"
      min_lines: 60
    - path: "FamilyScore/FamilyScore/Views/Settings/MemberSettingsView.swift"
      provides: "MemberRow + Modus-Picker, changeMemberRole-Integration"
      min_lines: 50
    - path: "FamilyScore/FamilyScore/Views/MainTabView.swift"
      provides: "TabView mit conditionalem Settings-Tab fuer Admin"
      contains: "role == .admin"
    - path: "FamilyScore/FamilyScore/FamilyScoreApp.swift"
      provides: "CategoryService als @StateObject, .environmentObject(categoryService)"
      contains: "@StateObject private var categoryService"
  key_links:
    - from: "FamilyScore/FamilyScore/Views/MainTabView.swift"
      to: "FamilyScore/FamilyScore/Views/Settings/SettingsView.swift"
      via: "NavigationStack + .environmentObject(categoryService)"
      pattern: "SettingsView"
    - from: "FamilyScore/FamilyScore/Views/Settings/CategorySettingsView.swift"
      to: "FamilyScore/FamilyScore/Services/CategoryService.swift"
      via: "@EnvironmentObject var categoryService: CategoryService"
      pattern: "categoryService.toggleCategory"
    - from: "FamilyScore/FamilyScore/FamilyScoreApp.swift"
      to: "FamilyScore/FamilyScore/Services/CategoryService.swift"
      via: "@StateObject + .environmentObject"
      pattern: "categoryService"
---

<objective>
Wave 1 implementiert den vollstaendigen SETTINGS-01/02-Stack: CategoryService (echter Supabase-CRUD), alle drei Settings-Views (SettingsView, CategorySettingsView, MemberSettingsView) und die App-Verdrahtung (MainTabView + FamilyScoreApp).

Purpose: Admins koennen nach dieser Wave Kategorien toggeln, Multiplikatoren aendern und Mitglieder-Modi umschalten — vollstaendig mit Supabase verbunden und Widget-aktualisiert.
Output: CategoryService.swift, 3 Settings-Views, modifiziertes MainTabView.swift, modifiziertes FamilyScoreApp.swift.
</objective>

<execution_context>
@C:/Users/maxfe/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/maxfe/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-settings-polish/06-RESEARCH.md
@.planning/phases/06-settings-polish/06-PATTERNS.md
@.planning/phases/06-settings-polish/06-UI-SPEC.md
@.planning/phases/06-settings-polish/06-01-SUMMARY.md

<interfaces>
<!-- Aus bestehender Codebase extrahiert — KEIN Erkunden noetig -->

Aus FamilyScore/FamilyScore/Services/FamilyService.swift (vollstaendige Struktur):
```swift
// Header-Muster:
import Foundation
@preconcurrency import Supabase

@MainActor
final class FamilyService: ObservableObject {
    @Published private(set) var members: [FamilyMember] = []
    @Published var serviceError: String? = nil

    // Fetch-Pattern:
    func fetchMembers(familyId: UUID) async {
        do {
            let fetched: [FamilyMember] = try await supabase
                .from("family_members")
                .select()
                .eq("family_id", value: familyId.uuidString)
                .order("created_at", ascending: true)
                .execute()
                .value
            members = fetched
        } catch {
            serviceError = "Mitglieder konnten nicht geladen werden."
        }
    }

    // UPDATE-Pattern (aus updateProfile):
    func updateProfile(displayName: String, avatarColor: String) async throws {
        struct ProfileUpdate: Encodable {
            let display_name: String
            let avatar_color: String
            let updated_at: Date
        }
        try await supabase
            .from("family_members")
            .update(ProfileUpdate(...))
            .eq("id", value: currentUserId.uuidString)
            .execute()
    }

    // Error-Enum-Pattern:
    enum FamilyServiceError: Error, LocalizedError {
        case notAuthenticated
        case unknown(String)
        var errorDescription: String? { ... }
    }
}
```

Aus FamilyScore/FamilyScore/Views/Family/MemberListView.swift (Shell-Pattern):
```swift
// ZStack + List-Shell:
var body: some View {
    ZStack {
        Color.black.ignoresSafeArea()
        List {
            Section { /* rows */ } header: {
                Text("Abschnittsname")
                    .foregroundColor(.secondary)
                    .font(.caption)
                    .textCase(nil)
            }
        }
        .listStyle(.grouped)
        .scrollContentBackground(.hidden)
    }
    .navigationTitle("Titel")
    .navigationBarTitleDisplayMode(.large)
}

// Error-Overlay (Zeilen 116-133):
.overlay {
    if let error = familyService.serviceError {
        VStack {
            Spacer()
            HStack {
                Text(error).font(.subheadline).foregroundColor(.white)
                Spacer()
                Button { familyService.serviceError = nil } label: {
                    Image(systemName: "xmark").foregroundColor(.white)
                }
            }
            .padding()
            .background(Color.red.opacity(0.85))
            .cornerRadius(12)
            .padding()
        }
    }
}

// Async-Task-Pattern (Admin-Aktionen):
Button(role: .destructive) {
    Task {
        do {
            try await familyService.removeMember(memberId: member.id)
        } catch {
            familyService.serviceError = error.localizedDescription
        }
    }
}

// Admin-Check:
private var currentUserIsAdmin: Bool {
    familyService.members.contains { $0.role == "admin" }
}

// Row-Background:
.listRowBackground(Color.white.opacity(0.05))

// Task fuer initiales Laden:
.task {
    if let familyId = familyService.currentFamily?.id {
        await familyService.fetchMembers(familyId: familyId)
    }
}
```

Aus FamilyScore/FamilyScore/FamilyScoreApp.swift (Injektions-Pattern):
```swift
@StateObject private var authService = AuthService()
@StateObject private var familyService = FamilyService()

WindowGroup {
    RootView()
        .environmentObject(authService)
        .environmentObject(familyService)
        .task { ... }
}
```

Aus FamilyScore/FamilyScore/Models/MemberRole.swift:
```swift
enum MemberRole: String, Codable, CaseIterable {
    case admin; case adult; case child
    var displayName: String { /* "Admin", "Erwachsen", "Kind-vereinfacht" */ }
}
```

Aus FamilyScore/FamilyScore/Views/Family/RolePickerSheet.swift (changeMemberRole-Aufruf-Pattern):
```swift
private func confirmRoleChange() async {
    isLoading = true
    defer { isLoading = false }
    do {
        try await familyService.changeMemberRole(memberId: member.id, role: selectedRole)
        if let familyId = familyService.currentFamily?.id {
            await familyService.fetchMembers(familyId: familyId)
        }
    } catch {
        familyService.serviceError = error.localizedDescription
    }
}
```

Aus 06-01 erstellt: CategoryServiceProtocol (fetchCategories, toggleCategory, updateWeight, enabledCategories)
Aus 06-01 erstellt: CategoryConfig-Struct mit `isEnabled: Bool`, `pointWeight: Double`, `sortOrder: Int`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: CategoryService implementieren</name>
  <files>
    FamilyScore/FamilyScore/Services/CategoryService.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/Services/FamilyService.swift — vollstaendiges Service-Muster (Header, Klassen-Deklaration, Fetch, Update, Error-Enum) — VOLLSTAENDIG lesen
    - FamilyScoreTests/Mocks/MockCategoryService.swift — CategoryServiceProtocol-Definition aus Wave 0 — lesen um Protocol-Konformanz sicherzustellen
    - FamilyScore/FamilyScore/Models/CategoryConfig.swift — Felder und CodingKeys — lesen fuer korrekte Patch-Struct-Felder
  </read_first>
  <action>
Erstelle FamilyScore/FamilyScore/Services/CategoryService.swift als exakten Klon von FamilyService.swift mit folgenden Anpassungen:

**Header-Kommentare:**
```swift
// FamilyScore/Services/CategoryService.swift
// Target Membership: FamilyScore (App) ONLY
// iOS 16.0 Minimum: ObservableObject + @Published (KEIN @Observable -- iOS 17+)
// NIEMALS Supabase SDK im Widget-Target -- gilt auch fuer diesen Service
// Threat mitigations: T-6-01 (RLS), T-6-02 (Stepper-Range + DB-Constraint)
```

**Imports:**
```swift
import Foundation
import WidgetKit        // fuer WidgetCenter.shared.reloadAllTimelines()
@preconcurrency import Supabase
```

**Klassen-Deklaration:**
```swift
@MainActor
final class CategoryService: ObservableObject {
    @Published private(set) var categories: [CategoryConfig] = []
    @Published var serviceError: String? = nil
```

**fetchCategories(familyId:)** — Klon von fetchMembers, mit:
- `.from("category_config")`
- `.eq("family_id", value: familyId.uuidString)`
- `.order("sort_order", ascending: true)`
- Fehlertext: `"Kategorien konnten nicht geladen werden."`

**enabledCategories computed property:**
```swift
var enabledCategories: [CategoryConfig] {
    categories.filter { $0.isEnabled }
}
```

**toggleCategory(id: UUID, isEnabled: Bool) async throws:**
```swift
struct Patch: Encodable {
    let is_enabled: Bool
    let updated_at: Date
}
// Supabase UPDATE:
try await supabase
    .from("category_config")
    .update(Patch(is_enabled: isEnabled, updated_at: Date()))
    .eq("id", value: id.uuidString)
    .execute()
// Optimistisches lokales Update:
if let idx = categories.firstIndex(where: { $0.id == id }) {
    categories[idx].isEnabled = isEnabled
}
// Widget-Reload IMMER nach kategorie_config-Mutationen:
WidgetCenter.shared.reloadAllTimelines()
```
Im catch-Block: `throw CategoryServiceError.updateFailed(error.localizedDescription)`

**updateWeight(id: UUID, weight: Double) async throws:**
```swift
struct Patch: Encodable {
    let point_weight: Double
    let updated_at: Date
}
try await supabase
    .from("category_config")
    .update(Patch(point_weight: weight, updated_at: Date()))
    .eq("id", value: id.uuidString)
    .execute()
if let idx = categories.firstIndex(where: { $0.id == id }) {
    categories[idx].pointWeight = weight
}
// Kein WidgetCenter-Call bei weight-Aenderung (kein Widget-Display benoetigt)
```
Im catch-Block: `throw CategoryServiceError.updateFailed(error.localizedDescription)`

**CategoryServiceError enum** (nach dem Klassen-Ende):
```swift
enum CategoryServiceError: Error, LocalizedError {
    case fetchFailed(String)
    case updateFailed(String)
    case unknown(String)

    var errorDescription: String? {
        switch self {
        case .fetchFailed(let msg):  return "Laden fehlgeschlagen: \(msg)"
        case .updateFailed(let msg): return "Speichern fehlgeschlagen: \(msg)"
        case .unknown(let msg):      return msg
        }
    }
}
```

WICHTIG: CategoryService implementiert CategoryServiceProtocol NICHT explizit (Duck-Typing wie FamilyService/FamilyServiceProtocol). Structural conformance ueber Interface-Matching.
  </action>
  <verify>
    <automated>cd '/mnt/c/Users/maxfe/Maxflow Software/Projekte/Family Score' && grep -n "WidgetCenter.shared.reloadAllTimelines" FamilyScore/FamilyScore/Services/CategoryService.swift && grep -n "enabledCategories" FamilyScore/FamilyScore/Services/CategoryService.swift</automated>
  </verify>
  <done>
    - CategoryService.swift existiert mit @MainActor, ObservableObject, fetchCategories, toggleCategory, updateWeight, enabledCategories, CategoryServiceError
    - WidgetCenter.shared.reloadAllTimelines() wird in toggleCategory aufgerufen (nicht in updateWeight)
    - Kein echter Test in diesem Task (Unit-Tests laufen gegen Mock aus Wave 0)
  </done>
</task>

<task type="auto">
  <name>Task 2: Settings-Views (SettingsView + CategorySettingsView + MemberSettingsView)</name>
  <files>
    FamilyScore/FamilyScore/Views/Settings/SettingsView.swift
    FamilyScore/FamilyScore/Views/Settings/CategorySettingsView.swift
    FamilyScore/FamilyScore/Views/Settings/MemberSettingsView.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/Views/Family/MemberListView.swift — VOLLSTAENDIG lesen: ZStack+List-Shell (Zeilen 20-83), Error-Overlay (116-133), Admin-Check, .task-Pattern, MemberRow-Struct (139-174), Async-Task-Fehlerbehandlung (33-44)
    - FamilyScore/FamilyScore/Views/Family/RolePickerSheet.swift — changeMemberRole-Aufruf-Pattern (Zeilen 102-115)
    - FamilyScore/FamilyScore/Models/CategoryConfig.swift — Felder fuer View-Binding
    - FamilyScore/FamilyScore/Models/MemberRole.swift — displayName fuer Picker-Optionen
  </read_first>
  <action>
**SettingsView.swift** — Root-Settings mit Admin-Guard und drei Sektionen:
- `@EnvironmentObject private var categoryService: CategoryService`
- `@EnvironmentObject private var familyService: FamilyService`
- ZStack + Color.black.ignoresSafeArea() + List(.grouped) + .scrollContentBackground(.hidden)
- Drei Sektionen mit Section-Header-Stil (foregroundColor(.secondary) + .font(.caption) + .textCase(nil)):
  1. "Kategorien" → NavigationLink(destination: CategorySettingsView())
  2. "Mitglieder" → NavigationLink(destination: MemberSettingsView())
  3. "App" → zwei Rows:
     - NavigationLink("Datenschutz") → leere `PrivacyInfoView` Stub (inline struct mit Text "Die Family Score App sammelt Aktivitaetsdaten ausschliesslich zur App-Funktion.")
     - Text("Version \(Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "–")")
         .foregroundColor(.secondary)
- Alle List-Rows: .listRowBackground(Color.white.opacity(0.05))
- .navigationTitle("Einstellungen") + .navigationBarTitleDisplayMode(.large)

**CategorySettingsView.swift** — Toggle + Stepper pro Kategorie:
- `@EnvironmentObject private var categoryService: CategoryService`
- `@EnvironmentObject private var familyService: FamilyService`
- `@State private var errorMessage: String? = nil`
- ZStack + Color.black.ignoresSafeArea() + List(.grouped) + .scrollContentBackground(.hidden)
- Sektion "Kategorien" mit Section-Header: "Gilt nur fuer neue Eintraege — bestehende Eintraege bleiben unveraendert." (foregroundColor(.secondary) + .font(.caption) + .textCase(nil))
- ForEach(categoryService.categories) { category in:
  ```swift
  VStack(alignment: .leading, spacing: 4) {
      HStack {
          Image(systemName: category.icon)
              .foregroundColor(.white)
              .frame(width: 24)
          Text(category.name)
              .foregroundColor(.white)
              .font(.body)
          Spacer()
          Toggle("", isOn: Binding(
              get: { category.isEnabled },
              set: { newValue in
                  Task {
                      do {
                          try await categoryService.toggleCategory(id: category.id, isEnabled: newValue)
                      } catch {
                          errorMessage = "Kategorie konnte nicht geaendert werden. Bitte versuche es erneut."
                      }
                  }
              }
          ))
          .tint(Color.white.opacity(0.9))
          .labelsHidden()
      }
      if category.isEnabled {
          HStack {
              Text("Punkte-Gewicht")
                  .foregroundColor(.secondary)
                  .font(.caption)
              Spacer()
              Stepper(
                  value: Binding(
                      get: { category.pointWeight },
                      set: { newValue in
                          Task {
                              do {
                                  try await categoryService.updateWeight(id: category.id, weight: newValue)
                              } catch {
                                  errorMessage = "Gewichtung konnte nicht gespeichert werden. Bitte versuche es erneut."
                              }
                          }
                      }
                  ),
                  in: 0.5...10.0,
                  step: 0.5
              ) {
                  Text(String(format: "%.1f×", category.pointWeight))
                      .foregroundColor(.white)
                      .font(.body)
              }
          }
      }
  }
  .padding(.vertical, 4)
  ```
- .listRowBackground(Color.white.opacity(0.05)) auf ForEach-Rows
- Error-Banner: exakt wie MemberListView-Overlay (VStack+Spacer+HStack+Text+Button, Color.red.opacity(0.85), cornerRadius(12)) — benutze `$errorMessage`
- .task: `if let familyId = familyService.currentFamily?.id { await categoryService.fetchCategories(familyId: familyId) }`
- .navigationTitle("Kategorien") + .navigationBarTitleDisplayMode(.large)

WICHTIG: Da CategoryConfig.categories ein `@Published private(set) var` ist, muss das `Binding` via Closure die Index-Suche machen (wie oben mit `category.id`). Die `Toggle(isOn: Binding(...))` Implementierung ist korrekt.

**MemberSettingsView.swift** — Mitglied-Modus-Picker (KID-03):
- `@EnvironmentObject private var familyService: FamilyService`
- `@State private var errorMessage: String? = nil`
- ZStack + Color.black.ignoresSafeArea() + List(.grouped) + .scrollContentBackground(.hidden)
- Sektion "Mitglieder" mit Section-Header (gleicher Stil)
- ForEach(familyService.members) { member in → MemberSettingsRow(member:) }
  - `MemberSettingsRow` ist ein innerer struct in MemberSettingsView.swift:
    ```swift
    struct MemberSettingsRow: View {
        let member: FamilyMember
        @EnvironmentObject private var familyService: FamilyService
        @State private var selectedRole: MemberRole
        @State private var errorMessage: String? = nil
        
        init(member: FamilyMember) {
            self.member = member
            _selectedRole = State(initialValue: MemberRole(rawValue: member.role) ?? .adult)
        }
        
        var body: some View {
            HStack(spacing: 12) {
                Circle()
                    .fill(Color(hex: member.avatar_color) ?? .blue)
                    .frame(width: 44, height: 44)
                    .overlay {
                        Text(String(member.display_name.prefix(1)).uppercased())
                            .font(.headline)
                            .foregroundColor(.white)
                    }
                VStack(alignment: .leading, spacing: 2) {
                    Text(member.display_name)
                        .foregroundColor(.white)
                        .font(.body)
                    Text(selectedRole.displayName)
                        .font(.caption)
                        .foregroundColor(.secondary)
                }
                Spacer()
                Picker("Modus", selection: $selectedRole) {
                    ForEach(MemberRole.allCases, id: \.self) { role in
                        Text(role.displayName).tag(role)
                    }
                }
                .pickerStyle(.menu)
                .tint(.white)
                .onChange(of: selectedRole) { newRole in
                    Task {
                        do {
                            try await familyService.changeMemberRole(memberId: member.id, role: newRole)
                            if let familyId = familyService.currentFamily?.id {
                                await familyService.fetchMembers(familyId: familyId)
                            }
                        } catch {
                            errorMessage = "Modus konnte nicht geaendert werden. Bitte versuche es erneut."
                            selectedRole = MemberRole(rawValue: member.role) ?? .adult
                        }
                    }
                }
            }
            .padding(.vertical, 4)
        }
    }
    ```
- Error-Banner auf MemberSettingsView-Level (gleicher Overlay-Stil)
- .task: `if let familyId = familyService.currentFamily?.id { await familyService.fetchMembers(familyId: familyId) }`
- .navigationTitle("Mitglieder") + .navigationBarTitleDisplayMode(.large)
- Alle Rows: .listRowBackground(Color.white.opacity(0.05))

HINWEIS zu Color(hex:): Die MemberListView verwendet bereits `Color(hex:)` — pruefe ob eine Color-Extension existiert, bevor du eine neue anlegst.
  </action>
  <verify>
    <automated>cd '/mnt/c/Users/maxfe/Maxflow Software/Projekte/Family Score' && grep -rn "categoryService.toggleCategory" FamilyScore/FamilyScore/Views/Settings/ && grep -n "Stepper" FamilyScore/FamilyScore/Views/Settings/CategorySettingsView.swift && grep -n "changeMemberRole" FamilyScore/FamilyScore/Views/Settings/MemberSettingsView.swift</automated>
  </verify>
  <done>
    - SettingsView.swift: drei List-Sektionen, NavigationLinks zu Category- und MemberSettingsView
    - CategorySettingsView.swift: ForEach ueber categoryService.categories, Toggle mit Optimistic-Update, Stepper (0.5-10.0 step 0.5), Error-Banner
    - MemberSettingsView.swift: ForEach ueber familyService.members, Picker(.menu) fuer Modus, .onChange ruft changeMemberRole auf, Error-Banner
    - Alle Views: ZStack + Color.black + List(.grouped) + .scrollContentBackground(.hidden) + .listRowBackground(0.05)
  </done>
</task>

<task type="auto">
  <name>Task 3: MainTabView + FamilyScoreApp verdrahten</name>
  <files>
    FamilyScore/FamilyScore/Views/MainTabView.swift
    FamilyScore/FamilyScore/FamilyScoreApp.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/FamilyScoreApp.swift — bestehende @StateObject-Injektionen und WindowGroup-Struktur — VOLLSTAENDIG lesen
    - FamilyScore/FamilyScore/Views/RootView.swift — bestehende Struktur; Phase 6 modifiziert hier NICHT (Kind-Routing kommt in Plan 06-03)
    - FamilyScore/FamilyScore/Views/Family/MemberListView.swift — Admin-Check-Pattern und Tab-Struktur (falls MainTabView noch nicht existiert, lesen fuer Strukturreferenz)
  </read_first>
  <action>
**FamilyScoreApp.swift** — CategoryService hinzufuegen:
1. BESTEHENDE Zeilen LESEN (vollstaendige Datei ist in Interfaces-Block oben)
2. Einzige Aenderung: Neue @StateObject-Deklaration und .environmentObject hinzufuegen:
   ```swift
   @StateObject private var categoryService = CategoryService()
   ```
   Nach `@StateObject private var familyService = FamilyService()`
3. In WindowGroup, nach `.environmentObject(familyService)`:
   ```swift
   .environmentObject(categoryService)
   ```
4. Alle bestehenden Zeilen (XCTest-Guard, Debug-Funktionen, Kommentare) UNVERAENDERT lassen

**MainTabView.swift** — Tab-View mit conditionalem Settings-Tab:

Falls MainTabView noch nicht existiert (Phase 4 nicht abgeschlossen): Erstelle eine neue Datei.
Falls MainTabView bereits existiert: Zeile fuer conditionale Settings-Tab hinzufuegen.

Struktur (iOS 16-kompatibel mit `TabView` + `.tag`-Pattern):
```swift
// FamilyScore/Views/MainTabView.swift
// Target Membership: FamilyScore (App) ONLY
// iOS 16.0 Minimum: TabView mit .tabItem (kein Tab()-Initialisierungstyp -- das ist iOS 18+)
// Threat mitigation: T-6-03 — Settings-Tab nur sichtbar fuer Admin

import SwiftUI

struct MainTabView: View {
    @EnvironmentObject private var authService: AuthService
    @EnvironmentObject private var familyService: FamilyService
    @EnvironmentObject private var categoryService: CategoryService

    var body: some View {
        TabView {
            // Tab 1: Home/Dashboard (Phase 4 liefert echten DashboardView)
            Text("Dashboard")
                .tabItem {
                    Label("Home", systemImage: "house.fill")
                }

            // Tab 2: Aktivitaeten (Phase 4)
            Text("Aktivitaeten")
                .tabItem {
                    Label("Aktivitaeten", systemImage: "list.bullet")
                }

            // Tab 3: Familie
            NavigationStack {
                MemberListView()
            }
            .tabItem {
                Label("Familie", systemImage: "person.2.fill")
            }

            // Tab 4: Einstellungen — NUR fuer Admin sichtbar (T-6-03)
            // Per UI-SPEC: Tab fehlt komplett fuer Non-Admins (kein Lock, kein Disable)
            if familyService.members.first(where: {
                $0.role == MemberRole.admin.rawValue &&
                $0.id == (try? UUID(uuidString: (try? await authService.currentUserId()) ?? "")) ?? UUID()
            }) != nil {
                NavigationStack {
                    SettingsView()
                        .environmentObject(categoryService)
                        .environmentObject(familyService)
                }
                .tabItem {
                    Label("Einstellungen", systemImage: "gear")
                }
            }
        }
        .tint(.white)
    }
}
```

WICHTIG: Die Admin-Pruefung in MainTabView braucht die eingeloggte User-ID. Da AuthService.currentUserId() async ist und TabView-Rendering synchron, verwende stattdessen:
- Lies die eingeloggte User-ID aus `AppState` oder pruefe ob FamilyService ein `currentMember`-Property hat (gemaess PATTERNS.md Assumption A3 ist das noch unbekannt)
- FALLBACK-Loesung (iOS 16-kompatibel, keine async-Probleme): Pruefe ob irgendein Member mit der User-ID des eingeloggten Users die Admin-Rolle hat

Da Phase 4 MainTabView noch nicht existiert oder nur als Placeholder, nutze diese einfache Loesung:
```swift
// Vereinfachter Admin-Check via appState (korrekte Loesung wenn Phase 4 fertig):
// In FamilyScoreApp werden nur Admin-User an MainTabView weitergeleitet wenn Phase 6 vollstaendig ist.
// Fuer jetzt: zeige Settings-Tab nur wenn familyService.members eine Admin-Rolle enthaelt
// die dem eingeloggten User gehoert.
// Da wir currentUserId hier nicht direkt haben: nutze Task + @State.

// Implementierung mit @State fuer currentUserId:
@State private var currentUserId: UUID? = nil

// In .task{}: currentUserId aus authService.currentSession holen
.task {
    if let session = try? await supabase.auth.session {
        currentUserId = session.user.id
    }
}

// Admin-Check:
private var isAdmin: Bool {
    guard let uid = currentUserId else { return false }
    return familyService.members.contains { $0.id == uid && $0.role == MemberRole.admin.rawValue }
}
```

Dann in TabView: `if isAdmin { /* Settings-Tab */ }`

Falls das aufgrund fehlender Phase-4-Implementation zu komplex ist: Erstelle MainTabView als saubere neue Datei mit dem obigen Muster — Phase 4 wird sie erweitern.
  </action>
  <verify>
    <automated>cd '/mnt/c/Users/maxfe/Maxflow Software/Projekte/Family Score' && grep -n "categoryService" FamilyScore/FamilyScore/FamilyScoreApp.swift && grep -n "isAdmin\|MemberRole.admin" FamilyScore/FamilyScore/Views/MainTabView.swift</automated>
  </verify>
  <done>
    - FamilyScoreApp.swift: `@StateObject private var categoryService = CategoryService()` und `.environmentObject(categoryService)` vorhanden
    - MainTabView.swift: Settings-Tab ist konditional (nur wenn isAdmin == true), nicht fuer Adult/Child sichtbar
    - Kein Settings-Tab-Lock oder -Disable — Tab fehlt einfach (gemaess UI-SPEC)
    - CI-Build gruen nach `git push`
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| SwiftUI View → CategoryService | Jeder Supabase-Call geht durch CategoryService; kein direkter Supabase-Aufruf aus Views |
| CategoryService → Supabase REST | RLS-Policy "Admin verwaltet Kategorien" erzwingt Berechtigung serverseitig |
| MainTabView Admin-Guard → Settings-Tab | Oberflaechlicher Guard (nur UI-Sicherheit); RLS ist die echte Schicht |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Elevation of Privilege | CategoryService.toggleCategory / updateWeight | mitigate | RLS-Policy "Admin verwaltet Kategorien" mit `USING (is_family_admin())` bereits in Phase 1 aktiv; jeder UPDATE-Call wird serverseitig geprueft |
| T-6-02 | Tampering | category_config.point_weight | mitigate | DB-Constraint aus Wave 0 (point_weight > 0 AND <= 10) aktiv; Stepper-Range in UI (0.5-10.0) als zusaetzliche Eingabevalidierung |
| T-6-03 | Elevation of Privilege | MainTabView Settings-Tab | mitigate | Tab wird nicht gerendert fuer Non-Admins (isAdmin-Check); RPC change_member_role prueft Admin serverseitig (Phase 3); doppelte Sicherheitsschicht |
| T-6-04 | Information Disclosure | Supabase Anon Key | accept | Secrets.xcconfig gitignored seit Phase 1; CI-Injection via GitHub Secrets; keine Aktion in Phase 6 noetig |
</threat_model>

<verification>
Nach Wave 1 gilt:
1. CI-Build gruen (`git push` → `gh run list`)
2. CategoryServiceTests im CI gruen (laufen gegen Mock)
3. grep-Checks bestanden (s. Task-Verify)

```powershell
& "C:\Program Files\GitHub CLI\gh.exe" run list --limit 3
```
</verification>

<success_criteria>
- CategoryService.swift: `@MainActor final class`, `@Published private(set) var categories`, `enabledCategories`, `toggleCategory` mit WidgetCenter-Call, `updateWeight` ohne WidgetCenter-Call, CategoryServiceError
- SettingsView.swift: drei Sektionen (Kategorien/Mitglieder/App), ZStack+List-Shell, .listRowBackground(0.05)
- CategorySettingsView.swift: ForEach+Toggle (Optimistic) + Stepper (0.5-10.0) + Error-Banner
- MemberSettingsView.swift: ForEach+MemberSettingsRow+Picker(.menu)+changeMemberRole-Integration
- FamilyScoreApp.swift: categoryService als @StateObject injiziert
- MainTabView.swift: Settings-Tab konditional (isAdmin), nicht fuer Adult/Child vorhanden
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/06-settings-polish/06-02-SUMMARY.md` erstellen.
</output>
