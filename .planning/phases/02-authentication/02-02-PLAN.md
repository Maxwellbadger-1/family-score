---
phase: 02-authentication
plan: "02"
type: execute
wave: 1
depends_on: ["02-01-PLAN"]
files_modified:
  - FamilyScore/Services/AuthService.swift
  - FamilyScore/Views/RootView.swift
  - FamilyScore/Views/Auth/AuthFlowView.swift
  - FamilyScore/Views/Auth/LoginView.swift
  - FamilyScore/Views/Auth/RegisterView.swift
autonomous: true
requirements:
  - AUTH-01
  - AUTH-03
  - AUTH-04

must_haves:
  truths:
    - "AuthService ist ObservableObject + @Published — kompiliert auf iOS 16.0"
    - "authStateChanges-Schleife handhabt initialSession, signedIn und signedOut Events"
    - "RootView liest authService.appState und zeigt den korrekten Screen fuer jeden der 4 States"
    - "LoginView enthaelt E-Mail + Passwort Felder + Sign-In-Button; RegisterView hat zusaetzlich Name-Feld"
    - "Falsches Passwort zeigt Fehlermeldung in der UI; App crasht nicht"
    - "Nach signOut landet User auf dem Login-Screen ohne Residualdaten"
    - "xcodebuild test -scheme FamilyScore gibt 0 Failures aus fuer testSignIn*, testSignOut* Tests"
  artifacts:
    - path: "FamilyScore/Services/AuthService.swift"
      provides: "ObservableObject mit authStateChanges-Schleife, signUp/signIn/signOut"
      exports: ["AuthService"]
    - path: "FamilyScore/Views/RootView.swift"
      provides: "AppState-basiertes View-Routing"
      exports: ["RootView"]
    - path: "FamilyScore/Views/Auth/AuthFlowView.swift"
      provides: "TabView-Container fuer Login + Register"
    - path: "FamilyScore/Views/Auth/LoginView.swift"
      provides: "E-Mail + Passwort Login-Screen"
    - path: "FamilyScore/Views/Auth/RegisterView.swift"
      provides: "E-Mail + Passwort + Name Registrierungs-Screen"
  key_links:
    - from: "FamilyScore/Services/AuthService.swift"
      to: "FamilyScore/FamilyScore/Supabase.swift"
      via: "supabase.auth.authStateChanges (global let supabase)"
      pattern: "supabase.auth.authStateChanges"
    - from: "FamilyScore/Views/RootView.swift"
      to: "FamilyScore/Services/AuthService.swift"
      via: "@EnvironmentObject var authService: AuthService"
      pattern: "@EnvironmentObject.*authService.*AuthService"
    - from: "FamilyScore/Views/Auth/LoginView.swift"
      to: "FamilyScore/Services/AuthService.swift"
      via: "@EnvironmentObject authService.signIn(email:password:)"
      pattern: "authService.signIn"
---

<objective>
Implementiert AuthService (ObservableObject, iOS 16 kompatibel), RootView (AppState-Routing), und die drei Auth-UI-Views (AuthFlowView, LoginView, RegisterView). Nach diesem Plan sind E-Mail+Passwort-Registration, Login und Logout vollstaendig funktionsfaehig.

Purpose: AUTH-01 (E-Mail+PW), AUTH-03 (Persistenz via authStateChanges/INITIAL_SESSION) und AUTH-04 (signOut) werden in diesem Plan geliefert. Sign in with Apple kommt in Plan 03 (Wave 2).

Output:
- FamilyScore/Services/AuthService.swift (vollstaendige Implementierung)
- FamilyScore/Views/RootView.swift
- FamilyScore/Views/Auth/AuthFlowView.swift
- FamilyScore/Views/Auth/LoginView.swift
- FamilyScore/Views/Auth/RegisterView.swift
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-01-SUMMARY.md

<interfaces>
<!-- Wave 0 (Plan 01) hat geliefert: -->

<!-- FamilyScore/Models/AppState.swift -->
```swift
enum AppState: Equatable {
    case loading
    case unauthenticated
    case authenticated(hasFamily: Bool)
}
```

<!-- FamilyScoreTests/Mocks/MockAuthService.swift definiert das Protocol: -->
```swift
protocol AuthServiceProtocol: AnyObject {
    var appState: AppState { get }
    var authError: String? { get set }
}
```

<!-- Phase 1 (01-PLAN-03) hat geliefert — globaler SupabaseClient: -->
```swift
// FamilyScore/Supabase.swift
import Supabase
let supabase = SupabaseClient(
    supabaseURL: URL(string: Bundle.main.object(forInfoDictionaryKey: "SUPABASE_URL") as! String)!,
    supabaseKey: Bundle.main.object(forInfoDictionaryKey: "SUPABASE_KEY") as! String
)
```

<!-- KRITISCH: iOS 16.0 Minimum → @Observable ist VERBOTEN hier -->
<!-- Verwende: final class AuthService: ObservableObject -->
<!-- Verwende: @Published private(set) var appState: AppState -->
<!-- NICHT: @Observable class AuthService -->
<!-- Begruendung: @Observable ist iOS 17+; CLAUDE.md "iOS 16.0 Minimum" -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: AuthService implementieren (ObservableObject, authStateChanges-Schleife)</name>
  <read_first>
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Pattern 2: AuthService — @Observable mit authStateChanges" — der vollstaendige Code-Block ist die Implementierungsvorlage)
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Pattern 5: SupabaseClient mit KeychainLocalStorage" — Supabase.swift muss KeychainLocalStorage hinzufuegen)
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Anti-Patterns vermeiden" — alle 5 Anti-Patterns)
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Common Pitfalls" — Pitfall 1 Apple fullName, Pitfall 2 startObserving zu spaet, Pitfall 4 Silent Logout)
    - CLAUDE.md (Abschnitt "Supabase" → Score-Regel + RLS)
    - FamilyScore/FamilyScore/Supabase.swift (aktuelle Implementierung lesen um KeychainLocalStorage hinzuzufuegen)
    - FamilyScore/Models/AppState.swift (Wave 0 Ergebnis — nicht modifizieren)
  </read_first>
  <files>
    FamilyScore/Services/AuthService.swift,
    FamilyScore/FamilyScore/Supabase.swift
  </files>
  <behavior>
    - signUp(email:password:displayName:) → wirft keinen Fehler → appState wird zu .authenticated(hasFamily: false)
    - signIn(email:password:) → wirft keinen Fehler → appState wird zu .authenticated(hasFamily: false)
    - signIn mit falschen Credentials → wirft AuthError → appState bleibt .unauthenticated, authError ist gesetzt
    - signOut() → appState wird zu .unauthenticated, currentUser wird nil
    - checkFamilyMembership() mit vorhandener family_id → gibt true zurueck
    - checkFamilyMembership() mit family_id = NULL → gibt false zurueck
  </behavior>
  <action>
**Schritt 1: FamilyScore/FamilyScore/Supabase.swift um KeychainLocalStorage erweitern**

Die bestehende Supabase.swift aus Phase 1 LESEN, dann KeychainLocalStorage hinzufuegen:

```swift
// FamilyScore/Supabase.swift
// Target Membership: FamilyScore (App) ONLY — NIEMALS Widget Extension!
import Supabase

let supabase = SupabaseClient(
    supabaseURL: URL(string: Bundle.main.object(
        forInfoDictionaryKey: "SUPABASE_URL") as! String)!,
    supabaseKey: Bundle.main.object(
        forInfoDictionaryKey: "SUPABASE_KEY") as! String,
    options: SupabaseClientOptions(
        auth: SupabaseClientOptions.AuthOptions(
            storage: KeychainLocalStorage(service: "com.familyscore")
            // Expliziter Service-Name verhindert Keychain-Prompt-Bug auf macOS/iOS
            // Source: github.com/orgs/supabase/discussions/28132
        )
    )
)
```

**Schritt 2: Services/-Verzeichnis erstellen und AuthService.swift erstellen**

`FamilyScore/Services/AuthService.swift`:

```swift
// FamilyScore/Services/AuthService.swift
// Target Membership: FamilyScore (App) ONLY
// iOS 16.0 Minimum: ObservableObject + @Published (NICHT @Observable — iOS 17+)
// Source: RESEARCH.md Pattern 2 (adaptiert fuer ObservableObject)

import Foundation
import Supabase

@MainActor
final class AuthService: ObservableObject {

    @Published private(set) var appState: AppState = .loading
    @Published private(set) var currentUser: User? = nil

    // Fehler-State fuer UI-Feedback (settable aus View fuer Error-Banner-Dismiss)
    @Published var authError: String? = nil

    // MARK: - Auth State Observation

    /// Muss beim App-Start gestartet werden (via .task{} in FamilyScoreApp oder RootView).
    /// Das erste Event ist IMMER INITIAL_SESSION — mit oder ohne vorhandene Session.
    /// Anti-Pattern vermeiden: NIEMALS supabase.auth.currentSession synchron beim Start lesen.
    func startObserving() async {
        for await (event, session) in await supabase.auth.authStateChanges {
            switch event {
            case .initialSession, .signedIn:
                if let session {
                    currentUser = session.user
                    let hasFamily = await checkFamilyMembership(userId: session.user.id)
                    appState = .authenticated(hasFamily: hasFamily)
                } else {
                    appState = .unauthenticated
                }
            case .signedOut:
                currentUser = nil
                appState = .unauthenticated
            case .tokenRefreshed:
                // Kein UI-State-Change noetig — Token laeuft still im Hintergrund
                // Pitfall 4 (Silent Logout Bug #486): Falls SIGNED_OUT nach Refresh kommt, fangt .signedOut-Case ab
                break
            default:
                break
            }
        }
    }

    // MARK: - E-Mail + Passwort

    func signUp(email: String, password: String, displayName: String) async throws {
        authError = nil
        // data["full_name"] wird von handle_new_user-Trigger in family_members.display_name geschrieben
        // Source: Phase 1 SQL-Migration — handle_new_user(): coalesce(new.raw_user_meta_data->>'full_name', 'New Member')
        try await supabase.auth.signUp(
            email: email,
            password: password,
            data: ["full_name": .string(displayName)]
        )
        // authStateChanges feuert SIGNED_IN → startObserving() aktualisiert appState
        // Kein manueller State-Update hier — Anti-Pattern vermeiden
    }

    func signIn(email: String, password: String) async throws {
        authError = nil
        try await supabase.auth.signInWithPassword(email: email, password: password)
        // authStateChanges feuert SIGNED_IN
    }

    func signOut() async throws {
        try await supabase.auth.signOut()
        // authStateChanges feuert SIGNED_OUT → startObserving() setzt appState = .unauthenticated
        // Keychain wird von supabase-swift automatisch geleert
    }

    // MARK: - Family-Membership-Check

    /// Prueft ob der eingeloggte User eine family_id in family_members hat.
    /// Wird nach SIGNED_IN aufgerufen um authenticated(hasFamily:) zu bestimmen.
    /// RLS: family_members-Policy erlaubt id = auth.uid() → eigenes Profil lesbar.
    private func checkFamilyMembership(userId: UUID) async -> Bool {
        do {
            struct FamilyCheck: Decodable { let family_id: UUID? }
            let result: [FamilyCheck] = try await supabase
                .from("family_members")
                .select("family_id")
                .eq("id", value: userId.uuidString)
                .limit(1)
                .execute()
                .value
            return result.first?.family_id != nil
        } catch {
            // DB-Fehler → kein Crash; User landet in OnboardingPlaceholder (Phase 3 repariert)
            return false
        }
    }

    // MARK: - Error Lokalisierung

    /// Supabase Auth-Fehler in nutzerfreundliche deutsche Meldungen uebersetzen.
    func localizedError(from error: Error) -> String {
        let msg = error.localizedDescription.lowercased()
        if msg.contains("invalid login") || msg.contains("invalid credentials") || msg.contains("email not confirmed") {
            return "E-Mail-Adresse oder Passwort falsch."
        } else if msg.contains("network") || msg.contains("connection") || msg.contains("offline") {
            return "Keine Internetverbindung. Bitte pruefen."
        } else if msg.contains("already registered") || msg.contains("already exists") || msg.contains("user already") {
            return "Diese E-Mail-Adresse ist bereits registriert."
        } else if msg.contains("password") && msg.contains("short") {
            return "Passwort muss mindestens 6 Zeichen lang sein."
        }
        return "Ein Fehler ist aufgetreten. Bitte erneut versuchen."
    }
}
```
  </action>
  <verify>
    <automated>
xcodebuild build -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" -quiet 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `FamilyScore/Services/AuthService.swift` existiert
    - `grep "final class AuthService: ObservableObject" FamilyScore/Services/AuthService.swift` gibt Treffer
    - `grep "@Observable" FamilyScore/Services/AuthService.swift` gibt 0 Zeilen (kein @Observable — iOS 17+ verboten)
    - `grep "@Published" FamilyScore/Services/AuthService.swift` gibt mindestens 3 Treffer (appState, currentUser, authError)
    - `grep "authStateChanges" FamilyScore/Services/AuthService.swift` gibt Treffer
    - `grep "case .initialSession, .signedIn" FamilyScore/Services/AuthService.swift` gibt Treffer
    - `grep "case .signedOut" FamilyScore/Services/AuthService.swift` gibt Treffer
    - `grep 'KeychainLocalStorage(service: "com.familyscore")' FamilyScore/FamilyScore/Supabase.swift` gibt Treffer
    - `xcodebuild build -scheme FamilyScore` gibt `BUILD SUCCEEDED` aus
  </acceptance_criteria>
  <done>AuthService ist ObservableObject mit vollstaendiger authStateChanges-Schleife. Supabase.swift hat KeychainLocalStorage. Projekt baut fehlerfrei.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RootView + Auth-UI implementieren (AuthFlowView, LoginView, RegisterView)</name>
  <read_first>
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Pattern 4: RootView — AppState-basiertes Routing" — vollstaendiger Code-Block)
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Empfohlene Projektstruktur" — Dateistruktur)
    - CLAUDE.md (Abschnitt "Design-Prinzipien" — Apple Health Aesthetik, Dark Mode, 3 Taps Maximum)
    - FamilyScore/Services/AuthService.swift (frisch erstellt — lesen um @Published Felder zu kennen)
    - FamilyScore/Models/AppState.swift (Enum-Cases)
    - FamilyScore/FamilyScore/FamilyScoreApp.swift (wird in Task 2 von Plan 03 modifiziert — hier NICHT anfassen)
  </read_first>
  <files>
    FamilyScore/Views/RootView.swift,
    FamilyScore/Views/Auth/AuthFlowView.swift,
    FamilyScore/Views/Auth/LoginView.swift,
    FamilyScore/Views/Auth/RegisterView.swift
  </files>
  <behavior>
    - RootView mit appState=.loading → ProgressView auf schwarzem Hintergrund sichtbar
    - RootView mit appState=.unauthenticated → AuthFlowView sichtbar
    - RootView mit appState=.authenticated(hasFamily: false) → OnboardingPlaceholderView sichtbar
    - RootView mit appState=.authenticated(hasFamily: true) → AuthenticatedPlaceholderView sichtbar
    - LoginView: Leere Felder + Submit-Tap → authService.signIn wird NICHT aufgerufen (Validation greift)
    - LoginView: Fehlermeldung aus authService.authError wird in rotem Text angezeigt
    - RegisterView: Name-Feld ist required; leerer Name verhindert Submit
  </behavior>
  <action>
**Schritt 1: Views/-Verzeichnis-Struktur anlegen**

Verzeichnisse erstellen: `FamilyScore/Views/` und `FamilyScore/Views/Auth/`

**Schritt 2: RootView.swift erstellen**

```swift
// FamilyScore/Views/RootView.swift
// Target Membership: FamilyScore (App) ONLY
// iOS 16: @EnvironmentObject statt @Environment fuer ObservableObject

import SwiftUI

struct RootView: View {
    @EnvironmentObject private var authService: AuthService

    var body: some View {
        Group {
            switch authService.appState {
            case .loading:
                // Splash waehrend INITIAL_SESSION-Event aussteht
                ProgressView()
                    .tint(.white)
                    .frame(maxWidth: .infinity, maxHeight: .infinity)
                    .background(Color.black)

            case .unauthenticated:
                AuthFlowView()

            case .authenticated(hasFamily: false):
                // Phase 3 liefert echten Onboarding-Flow
                OnboardingPlaceholderView()

            case .authenticated(hasFamily: true):
                // Phase 4 liefert MainTabView
                AuthenticatedPlaceholderView()
            }
        }
    }
}

// MARK: - Placeholder Views (werden in spaeteren Phasen ersetzt)

struct OnboardingPlaceholderView: View {
    @EnvironmentObject private var authService: AuthService

    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            VStack(spacing: 16) {
                Image(systemName: "person.2.fill")
                    .font(.system(size: 48))
                    .foregroundColor(.white)
                Text("Willkommen!")
                    .font(.title.bold())
                    .foregroundColor(.white)
                Text("Familiengruppe einrichten kommt bald (Phase 3).")
                    .font(.body)
                    .foregroundColor(.secondary)
                    .multilineTextAlignment(.center)
                    .padding(.horizontal)
                Button("Ausloggen") {
                    Task { try? await authService.signOut() }
                }
                .foregroundColor(.red)
                .padding(.top, 32)
            }
        }
    }
}

struct AuthenticatedPlaceholderView: View {
    @EnvironmentObject private var authService: AuthService

    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()
            VStack(spacing: 16) {
                Image(systemName: "checkmark.circle.fill")
                    .font(.system(size: 48))
                    .foregroundColor(.green)
                Text("Eingeloggt!")
                    .font(.title.bold())
                    .foregroundColor(.white)
                Text("Dashboard kommt in Phase 4.")
                    .font(.body)
                    .foregroundColor(.secondary)
                Button("Ausloggen") {
                    Task { try? await authService.signOut() }
                }
                .foregroundColor(.red)
                .padding(.top, 32)
            }
        }
    }
}

#Preview {
    // Preview mit Mock-State
    RootView()
        .environmentObject(MockPreviewAuthService(state: .unauthenticated))
}

// Lightweight Preview-Helper (nicht MockAuthService aus Tests — die ist im Test-Target)
private final class MockPreviewAuthService: AuthService {
    init(state: AppState) {
        super.init()
        // Direkt state setzen via Reflection wuerde private(set) brechen;
        // stattdessen: eigene init ueberschreiben ist hier NICHT moeglich da AuthService final ist.
        // Preview-Alternative: PreviewProvider mit @State in separatem Preview-Wrapper.
    }
}
```

HINWEIS fuer Preview: Da AuthService `final` ist, kann der Preview-Wrapper nicht subclassen. Stattdessen einen einfacheren Preview-Ansatz verwenden:

```swift
// Am Ende von RootView.swift, Preview ersetzen durch:
#Preview("Unauthenticated") {
    // AuthService wird injiziert; .loading State ist Default
    // Fuer echte Preview muss AuthService mock-state haben
    // Temporaer: ContentView zeigt einfach den Loading-State
    ProgressView()
        .tint(.white)
        .frame(maxWidth: .infinity, maxHeight: .infinity)
        .background(Color.black)
}
```

**Schritt 3: AuthFlowView.swift erstellen**

```swift
// FamilyScore/Views/Auth/AuthFlowView.swift
// TabView mit Login und Registrierung als zwei Tabs

import SwiftUI

struct AuthFlowView: View {
    @State private var selectedTab: AuthTab = .login

    enum AuthTab {
        case login, register
    }

    var body: some View {
        ZStack {
            Color.black.ignoresSafeArea()

            VStack(spacing: 0) {
                // Header
                VStack(spacing: 8) {
                    Image(systemName: "figure.2.and.child.holdinghands")
                        .font(.system(size: 52))
                        .foregroundColor(.white)
                        .padding(.top, 60)
                    Text("Family Score")
                        .font(.system(size: 32, weight: .bold))
                        .foregroundColor(.white)
                    Text("Gemeinsam mehr erreichen")
                        .font(.subheadline)
                        .foregroundColor(.secondary)
                }
                .padding(.bottom, 32)

                // Tab-Switcher
                Picker("", selection: $selectedTab) {
                    Text("Einloggen").tag(AuthTab.login)
                    Text("Registrieren").tag(AuthTab.register)
                }
                .pickerStyle(.segmented)
                .padding(.horizontal, 24)
                .padding(.bottom, 24)

                // Content
                Group {
                    switch selectedTab {
                    case .login:
                        LoginView()
                    case .register:
                        RegisterView()
                    }
                }

                Spacer()
            }
        }
    }
}

#Preview {
    AuthFlowView()
        .environmentObject(AuthService())
}
```

**Schritt 4: LoginView.swift erstellen**

```swift
// FamilyScore/Views/Auth/LoginView.swift
// Apple Health Aesthetik: dunkler Hintergrund, klare Felder, kein Farb-Bloat
// 3 Taps Maximum: Felder ausfuellen + Login-Button antippen = 1 Tap (Tastatur + Button)

import SwiftUI

struct LoginView: View {
    @EnvironmentObject private var authService: AuthService

    @State private var email: String = ""
    @State private var password: String = ""
    @State private var isLoading: Bool = false
    @FocusState private var focusedField: Field?

    enum Field { case email, password }

    private var canSubmit: Bool {
        !email.trimmingCharacters(in: .whitespaces).isEmpty &&
        password.count >= 6
    }

    var body: some View {
        VStack(spacing: 16) {
            // Fehler-Banner
            if let error = authService.authError {
                HStack {
                    Image(systemName: "exclamationmark.circle.fill")
                        .foregroundColor(.red)
                    Text(error)
                        .font(.subheadline)
                        .foregroundColor(.red)
                    Spacer()
                    Button {
                        authService.authError = nil
                    } label: {
                        Image(systemName: "xmark")
                            .foregroundColor(.secondary)
                    }
                }
                .padding(12)
                .background(Color.red.opacity(0.1))
                .cornerRadius(8)
                .padding(.horizontal, 24)
            }

            // E-Mail Feld
            VStack(alignment: .leading, spacing: 4) {
                Text("E-Mail")
                    .font(.caption)
                    .foregroundColor(.secondary)
                TextField("", text: $email)
                    .keyboardType(.emailAddress)
                    .textContentType(.emailAddress)
                    .autocapitalization(.none)
                    .autocorrectionDisabled()
                    .focused($focusedField, equals: .email)
                    .submitLabel(.next)
                    .onSubmit { focusedField = .password }
                    .padding(12)
                    .background(Color.white.opacity(0.08))
                    .cornerRadius(8)
                    .foregroundColor(.white)
            }
            .padding(.horizontal, 24)

            // Passwort Feld
            VStack(alignment: .leading, spacing: 4) {
                Text("Passwort")
                    .font(.caption)
                    .foregroundColor(.secondary)
                SecureField("", text: $password)
                    .textContentType(.password)
                    .focused($focusedField, equals: .password)
                    .submitLabel(.go)
                    .onSubmit { if canSubmit { Task { await submitLogin() } } }
                    .padding(12)
                    .background(Color.white.opacity(0.08))
                    .cornerRadius(8)
                    .foregroundColor(.white)
            }
            .padding(.horizontal, 24)

            // Login Button
            Button {
                Task { await submitLogin() }
            } label: {
                Group {
                    if isLoading {
                        ProgressView()
                            .tint(.black)
                    } else {
                        Text("Einloggen")
                            .font(.headline)
                    }
                }
                .frame(maxWidth: .infinity)
                .frame(height: 50)
                .background(canSubmit ? Color.white : Color.white.opacity(0.3))
                .foregroundColor(.black)
                .cornerRadius(12)
            }
            .disabled(!canSubmit || isLoading)
            .padding(.horizontal, 24)
            .padding(.top, 8)
        }
    }

    private func submitLogin() async {
        guard canSubmit else { return }
        isLoading = true
        defer { isLoading = false }
        do {
            try await authService.signIn(email: email.trimmingCharacters(in: .whitespaces),
                                         password: password)
        } catch {
            authService.authError = authService.localizedError(from: error)
        }
    }
}

#Preview {
    ZStack {
        Color.black.ignoresSafeArea()
        LoginView()
            .environmentObject(AuthService())
    }
}
```

**Schritt 5: RegisterView.swift erstellen**

```swift
// FamilyScore/Views/Auth/RegisterView.swift

import SwiftUI

struct RegisterView: View {
    @EnvironmentObject private var authService: AuthService

    @State private var displayName: String = ""
    @State private var email: String = ""
    @State private var password: String = ""
    @State private var passwordConfirm: String = ""
    @State private var isLoading: Bool = false
    @FocusState private var focusedField: Field?

    enum Field { case name, email, password, passwordConfirm }

    private var passwordsMatch: Bool { password == passwordConfirm || passwordConfirm.isEmpty }

    private var canSubmit: Bool {
        !displayName.trimmingCharacters(in: .whitespaces).isEmpty &&
        !email.trimmingCharacters(in: .whitespaces).isEmpty &&
        password.count >= 6 &&
        password == passwordConfirm
    }

    var body: some View {
        VStack(spacing: 16) {
            // Fehler-Banner
            if let error = authService.authError {
                HStack {
                    Image(systemName: "exclamationmark.circle.fill")
                        .foregroundColor(.red)
                    Text(error)
                        .font(.subheadline)
                        .foregroundColor(.red)
                    Spacer()
                    Button { authService.authError = nil } label: {
                        Image(systemName: "xmark").foregroundColor(.secondary)
                    }
                }
                .padding(12)
                .background(Color.red.opacity(0.1))
                .cornerRadius(8)
                .padding(.horizontal, 24)
            }

            // Name
            VStack(alignment: .leading, spacing: 4) {
                Text("Dein Name")
                    .font(.caption).foregroundColor(.secondary)
                TextField("", text: $displayName)
                    .textContentType(.name)
                    .focused($focusedField, equals: .name)
                    .submitLabel(.next)
                    .onSubmit { focusedField = .email }
                    .padding(12)
                    .background(Color.white.opacity(0.08))
                    .cornerRadius(8)
                    .foregroundColor(.white)
            }
            .padding(.horizontal, 24)

            // E-Mail
            VStack(alignment: .leading, spacing: 4) {
                Text("E-Mail")
                    .font(.caption).foregroundColor(.secondary)
                TextField("", text: $email)
                    .keyboardType(.emailAddress)
                    .textContentType(.emailAddress)
                    .autocapitalization(.none)
                    .autocorrectionDisabled()
                    .focused($focusedField, equals: .email)
                    .submitLabel(.next)
                    .onSubmit { focusedField = .password }
                    .padding(12)
                    .background(Color.white.opacity(0.08))
                    .cornerRadius(8)
                    .foregroundColor(.white)
            }
            .padding(.horizontal, 24)

            // Passwort
            VStack(alignment: .leading, spacing: 4) {
                Text("Passwort (min. 6 Zeichen)")
                    .font(.caption).foregroundColor(.secondary)
                SecureField("", text: $password)
                    .textContentType(.newPassword)
                    .focused($focusedField, equals: .password)
                    .submitLabel(.next)
                    .onSubmit { focusedField = .passwordConfirm }
                    .padding(12)
                    .background(Color.white.opacity(0.08))
                    .cornerRadius(8)
                    .foregroundColor(.white)
            }
            .padding(.horizontal, 24)

            // Passwort bestaetigen
            VStack(alignment: .leading, spacing: 4) {
                Text("Passwort bestaetigen")
                    .font(.caption).foregroundColor(.secondary)
                SecureField("", text: $passwordConfirm)
                    .textContentType(.newPassword)
                    .focused($focusedField, equals: .passwordConfirm)
                    .submitLabel(.go)
                    .onSubmit { if canSubmit { Task { await submitRegister() } } }
                    .padding(12)
                    .background(passwordsMatch ? Color.white.opacity(0.08) : Color.red.opacity(0.15))
                    .cornerRadius(8)
                    .foregroundColor(.white)
                if !passwordsMatch {
                    Text("Passwoerter stimmen nicht ueberein")
                        .font(.caption2)
                        .foregroundColor(.red)
                }
            }
            .padding(.horizontal, 24)

            // Registrieren Button
            Button {
                Task { await submitRegister() }
            } label: {
                Group {
                    if isLoading {
                        ProgressView().tint(.black)
                    } else {
                        Text("Konto erstellen").font(.headline)
                    }
                }
                .frame(maxWidth: .infinity)
                .frame(height: 50)
                .background(canSubmit ? Color.white : Color.white.opacity(0.3))
                .foregroundColor(.black)
                .cornerRadius(12)
            }
            .disabled(!canSubmit || isLoading)
            .padding(.horizontal, 24)
            .padding(.top, 8)
        }
    }

    private func submitRegister() async {
        guard canSubmit else { return }
        isLoading = true
        defer { isLoading = false }
        do {
            try await authService.signUp(
                email: email.trimmingCharacters(in: .whitespaces),
                password: password,
                displayName: displayName.trimmingCharacters(in: .whitespaces)
            )
        } catch {
            authService.authError = authService.localizedError(from: error)
        }
    }
}

#Preview {
    ZStack {
        Color.black.ignoresSafeArea()
        RegisterView()
            .environmentObject(AuthService())
    }
}
```
  </action>
  <verify>
    <automated>
xcodebuild build -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" -quiet 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `FamilyScore/Views/RootView.swift` existiert
    - `grep "switch authService.appState" FamilyScore/Views/RootView.swift` gibt Treffer
    - `grep "case .loading" FamilyScore/Views/RootView.swift` gibt Treffer
    - `grep "AuthFlowView" FamilyScore/Views/RootView.swift` gibt Treffer
    - `grep "OnboardingPlaceholderView" FamilyScore/Views/RootView.swift` gibt Treffer
    - `FamilyScore/Views/Auth/AuthFlowView.swift` existiert
    - `FamilyScore/Views/Auth/LoginView.swift` existiert
    - `grep "authService.signIn" FamilyScore/Views/Auth/LoginView.swift` gibt Treffer
    - `FamilyScore/Views/Auth/RegisterView.swift` existiert
    - `grep "authService.signUp" FamilyScore/Views/Auth/RegisterView.swift` gibt Treffer
    - `grep "@EnvironmentObject" FamilyScore/Views/RootView.swift` gibt Treffer (NICHT @Environment — iOS 16)
    - `grep "@Environment\b" FamilyScore/Views/RootView.swift | grep -v "EnvironmentObject"` gibt 0 Zeilen
    - `xcodebuild build -scheme FamilyScore` gibt `BUILD SUCCEEDED` aus
  </acceptance_criteria>
  <done>RootView routet korrekt auf alle 4 AppState-Cases. LoginView und RegisterView sind einsatzbereit mit Validierung und Fehlerdarstellung. Alle Views verwenden @EnvironmentObject (iOS 16 kompatibel).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| SwiftUI View → AuthService | Views rufen authService.signIn/signUp/signOut auf; AuthService validiert nie User-Inputs direkt — Supabase Backend macht Validierung |
| iOS Client → Supabase Auth API | E-Mail + Passwort gehen ueber TLS; nie im Keychain gespeichert (nur temporaer im @State) |
| Keychain → App | supabase-swift schreibt Tokens automatisch; App liest nie direkt Keychain-Werte |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-02 | Information Disclosure | AuthService.appState | mitigate | `private(set)` verhindert direktes Schreiben von Views; nur authStateChanges-Schleife setzt den State |
| T-2-03 | Spoofing | signIn mit falschen Credentials | mitigate | Supabase gibt `400 Invalid login credentials` zurueck; `localizedError()` zeigt nutzbare Meldung; appState bleibt .unauthenticated |
| T-2-04 | Information Disclosure | Passwort in @State Variable | accept | @State ist nur im RAM waehrend View sichtbar; iOS-App-Sandbox verhindert Memory-Dump durch andere Apps; kein Logging der @State-Werte |
| T-2-05 | Denial of Service | Refresh-Token Bug #486 (Silent Logout) | mitigate | authStateChanges .signedOut-Case setzt appState = .unauthenticated + currentUser = nil; User wird zu Login-Screen geleitet ohne Crash |
| T-2-06 | Elevation of Privilege | RLS nach Login | accept (inherited) | Phase 1 RLS-Policies sind aktiv sobald JWT im Authorization-Header; kein zusaetzliches Massnehmen in Phase 2 noetig |
| T-2-07 | Information Disclosure | authError @Published | accept | authError enthaelt nur lokalisierte Fehlermeldungen (kein Stack-Trace, keine Server-Details); `localizedError()` filtert rohe Server-Errors |
</threat_model>

<verification>
## Plan 02 Verifikation

**Build:**
```bash
xcodebuild build \
  -project "FamilyScore/FamilyScore.xcodeproj" \
  -scheme FamilyScore \
  -destination "platform=iOS Simulator,name=iPhone 16" \
  -quiet 2>&1 | grep -E "BUILD|error:"
# Erwartet: BUILD SUCCEEDED
```

**AuthService ist ObservableObject (iOS 16 kompatibel):**
```bash
grep "ObservableObject" FamilyScore/Services/AuthService.swift
# Erwartet: Treffer

grep "@Observable" FamilyScore/Services/AuthService.swift
# Erwartet: 0 Zeilen
```

**Kein hardcoded Key in neuen Dateien:**
```bash
git grep -r "supabase.co" FamilyScore/Services/ FamilyScore/Views/ --include="*.swift"
# Erwartet: 0 Zeilen
```

**RootView verwendet EnvironmentObject (nicht Environment fuer ObservableObject):**
```bash
grep "@EnvironmentObject" FamilyScore/Views/RootView.swift
# Erwartet: Treffer
```

**Tests nach Wave 1 gruenschalten:**
```bash
xcodebuild test \
  -project "FamilyScore/FamilyScore.xcodeproj" \
  -scheme FamilyScore \
  -destination "platform=iOS Simulator,name=iPhone 16" \
  -only-testing FamilyScoreTests/AuthServiceTests 2>&1 | grep -E "PASS|FAIL|error"
# Erwartet: Alle Tests PASS (da Mock-basiert, keine Netzwerkabhaengigkeit)
```
</verification>

<success_criteria>
- AuthService.swift: ObservableObject, @Published appState, authStateChanges-Schleife mit INITIAL_SESSION + SIGNED_IN + SIGNED_OUT Handling
- Supabase.swift: KeychainLocalStorage(service: "com.familyscore") konfiguriert
- RootView.swift: switch auf alle 4 AppState-Cases, @EnvironmentObject
- LoginView.swift: E-Mail + PW Felder, Submit-Validation, Error-Banner
- RegisterView.swift: Name + E-Mail + PW + PW-Confirm Felder, Passwort-Match-Validierung
- xcodebuild build → BUILD SUCCEEDED
- MockAuthService-basierte Tests in AuthServiceTests alle gruengeschaltet
</success_criteria>

<output>
Nach Abschluss `.planning/phases/02-authentication/02-02-SUMMARY.md` erstellen mit:
- AuthService Pfad + implementierte Methoden
- Supabase.swift Aenderung (KeychainLocalStorage hinzugefuegt)
- Views-Liste (5 Views erstellt)
- iOS 16 Kompatibilitaet: ObservableObject bestaetigt
- Build-Status: PASS / FAIL
- Test-Ergebnis: X/6 Tests gruengeschaltet
</output>
