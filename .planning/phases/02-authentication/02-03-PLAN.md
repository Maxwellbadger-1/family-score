---
phase: 02-authentication
plan: "03"
type: execute
wave: 2
depends_on: ["02-02-PLAN"]
files_modified:
  - FamilyScore/Views/Auth/SignInWithAppleView.swift
  - FamilyScore/Views/Auth/LoginView.swift
  - FamilyScore/FamilyScore/FamilyScoreApp.swift
autonomous: false
requirements:
  - AUTH-02
  - AUTH-03

user_setup:
  - service: apple_developer_portal
    why: "Sign in with Apple Capability muss im App Target aktiviert sein — kann nicht programmatisch erledigt werden"
    dashboard_config:
      - task: "In Xcode: FamilyScore Target → Signing & Capabilities → + Capability → Sign in with Apple hinzufuegen"
        location: "Xcode → Target Editor → Signing & Capabilities"
      - task: "Falls Fehler 'Capability requires paid developer account': Apple Developer Program Mitgliedschaft benoetigt"
        location: "developer.apple.com"

must_haves:
  truths:
    - "FamilyScoreApp.swift injiziert AuthService als @StateObject und startet startObserving() via .task{}"
    - "LoginView zeigt SignInWithAppleButton unterhalb der E-Mail+PW-Felder"
    - "SignInWithAppleView generiert rawNonce kryptographisch via SecRandomCopyBytes + sendet sha256(rawNonce) an Apple"
    - "signInWithIdToken erhaelt rawNonce (NICHT sha256(rawNonce)) — Pitfall 3 vermieden"
    - "Apple-Cancel (ASAuthorizationError.canceled) setzt KEINEN authError — Pitfall 5 vermieden"
    - "Apple fullName wird direkt nach erstem Login via supabase.auth.update() persistiert — Pitfall 1 vermieden"
    - "App baut fehlerfrei und RootView ist der Entry Point"
  artifacts:
    - path: "FamilyScore/Views/Auth/SignInWithAppleView.swift"
      provides: "SignInWithAppleButton Wrapper mit vollstaendigem Nonce-Fluss"
      exports: ["SignInWithAppleView"]
    - path: "FamilyScore/FamilyScore/FamilyScoreApp.swift"
      provides: "App Entry Point mit @StateObject AuthService + .environmentObject + .task{}"
  key_links:
    - from: "FamilyScore/Views/Auth/SignInWithAppleView.swift"
      to: "FamilyScore/Services/AuthService.swift"
      via: "@EnvironmentObject authService + supabase.auth.signInWithIdToken"
      pattern: "signInWithIdToken"
    - from: "FamilyScore/FamilyScore/FamilyScoreApp.swift"
      to: "FamilyScore/Views/RootView.swift"
      via: ".environmentObject(authService) → @EnvironmentObject in RootView"
      pattern: "environmentObject.*authService"
    - from: "FamilyScore/FamilyScore/FamilyScoreApp.swift"
      to: "FamilyScore/Services/AuthService.swift"
      via: "@StateObject + .task { await authService.startObserving() }"
      pattern: "authService.startObserving"
---

<objective>
Liefert Sign in with Apple (vollstaendiger Nonce-Fluss), integriert AuthService in FamilyScoreApp.swift als StateObject, und verbindet LoginView mit dem Apple-Sign-In-Button. Nach diesem Plan ist Phase 2 vollstaendig: alle 4 AUTH-Requirements implementiert.

Purpose: AUTH-02 (Sign in with Apple) benoetigt ASAuthorizationController + SHA256-Nonce + signInWithIdToken. FamilyScoreApp.swift muss AuthService als @StateObject injecten und startObserving() sofort starten — Pitfall 2 (startObserving zu spaet) wird durch .task{} auf WindowGroup-Level vermieden.

Output:
- FamilyScore/Views/Auth/SignInWithAppleView.swift (vollstaendige Nonce-Implementierung)
- FamilyScore/Views/Auth/LoginView.swift (SignInWithAppleView hinzugefuegt — Datei wird modifiziert)
- FamilyScore/FamilyScore/FamilyScoreApp.swift (vollstaendiger Auth-Einstiegspunkt)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication/02-RESEARCH.md
@.planning/phases/02-authentication/02-02-SUMMARY.md

<interfaces>
<!-- Wave 1 (Plan 02) hat geliefert: -->

<!-- FamilyScore/Services/AuthService.swift — vollstaendige Implementierung -->
```swift
@MainActor
final class AuthService: ObservableObject {
    @Published private(set) var appState: AppState = .loading
    @Published private(set) var currentUser: User? = nil
    @Published var authError: String? = nil

    func startObserving() async { ... }   // authStateChanges-Schleife
    func signUp(email:password:displayName:) async throws
    func signIn(email:password:) async throws
    func signOut() async throws
    func localizedError(from error: Error) -> String
}
```

<!-- FamilyScore/Views/Auth/LoginView.swift — wird in Task 1 modifiziert -->
<!-- Bereits vorhanden: E-Mail + PW Felder + Login Button -->
<!-- Hinzuzufuegen: Trennlinie "oder" + SignInWithAppleView() darunter -->

<!-- Phase 1 FamilyScoreApp.swift — wird in Task 2 vollstaendig ersetzt -->
<!-- Aktueller Inhalt: @main struct + WindowGroup + ContentView() + verifySupabaseConnection DEBUG-Code -->
<!-- Neuer Inhalt: @StateObject authService + RootView + .environmentObject + .task{} -->

<!-- KRITISCHER NONCE-FLUSS (immer korrekt beachten): -->
<!-- Apple bekommt: sha256(rawNonce) -->
<!-- Supabase bekommt: rawNonce (unhashed!) -->
<!-- Supabase re-hasht intern und vergleicht mit Apple-Token-eingebettetem Hash -->
<!-- Pitfall 3: signInWithIdToken(nonce: sha256(currentNonce)) ist FALSCH — immer rawNonce uebergeben -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SignInWithAppleView implementieren + LoginView integrieren</name>
  <read_first>
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Pattern 3: Sign in with Apple — vollstaendiger Nonce-Fluss" — vollstaendiger Code-Block ist die Implementierungsvorlage)
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Common Pitfalls" — Pitfall 1 fullName, Pitfall 3 Nonce-Verwechslung, Pitfall 5 Apple-Cancel)
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Anti-Patterns vermeiden" — Apple-Identity-Token-Validierung)
    - FamilyScore/Views/Auth/LoginView.swift (aktuellen Inhalt lesen um Einfuegeposition fuer Apple-Button zu bestimmen)
    - FamilyScore/Services/AuthService.swift (authError und localizedError Signatur pruefen)
  </read_first>
  <files>
    FamilyScore/Views/Auth/SignInWithAppleView.swift,
    FamilyScore/Views/Auth/LoginView.swift
  </files>
  <behavior>
    - SignInWithAppleView: randomNonce() gibt 32-Zeichen String zurueck (nur aus charset-Zeichen)
    - SignInWithAppleView: sha256() gibt lowercase hex-String zurueck (64 Zeichen fuer SHA256-Output)
    - SignInWithAppleView: Apple-Abbruch (canceled) → authService.authError wird NICHT gesetzt
    - SignInWithAppleView: Netzwerkfehler nach Apple-Auth → authService.authError wird gesetzt
    - LoginView: SignInWithAppleView erscheint unterhalb des Login-Buttons, getrennt durch "oder"
  </behavior>
  <action>
**Schritt 1: SignInWithAppleView.swift erstellen**

WICHTIG: iOS 16 Minimum — `@EnvironmentObject` verwenden, NICHT `@Environment(AuthService.self)`.
RESEARCH.md Pattern 3 zeigt `@Environment(AuthService.self)` (iOS 17-Syntax) — das ist ein Beispielfehler.
Die korrekte iOS-16-kompatible Syntax ist `@EnvironmentObject private var authService: AuthService`.

```swift
// FamilyScore/Views/Auth/SignInWithAppleView.swift
// Target Membership: FamilyScore (App) ONLY
// Source: offizielles gotrue-swift Beispiel + Supabase Docs "Sign in with Apple (native)"
// KRITISCH: rawNonce an Supabase, sha256(rawNonce) an Apple — NIEMALS vertauschen
// iOS 16 Minimum: @EnvironmentObject verwenden, NICHT @Environment (iOS 17+)

import SwiftUI
import AuthenticationServices
import CryptoKit
import Supabase

struct SignInWithAppleView: View {
    @EnvironmentObject private var authService: AuthService

    // rawNonce wird zwischen Request (requestedNonce) und Completion (handleAppleSignIn) gespeichert
    @State private var currentNonce: String = ""

    var body: some View {
        SignInWithAppleButton(.signIn) { request in
            // Neuen rawNonce generieren fuer diese spezifische Anfrage
            let nonce = randomNonce()
            currentNonce = nonce
            request.requestedScopes = [.fullName, .email]
            // Apple erhaelt SHA256-Hash — rawNonce bleibt im App-Memory fuer Supabase
            request.nonce = sha256(nonce)
        } onCompletion: { result in
            Task {
                await handleAppleSignIn(result: result)
            }
        }
        .signInWithAppleButtonStyle(.white)
        .frame(height: 50)
        .cornerRadius(12)
    }

    // MARK: - Apple Sign In Handler

    private func handleAppleSignIn(result: Result<ASAuthorization, Error>) async {
        switch result {
        case .success(let auth):
            guard
                let credential = auth.credential as? ASAuthorizationAppleIDCredential,
                let idTokenData = credential.identityToken,
                let idToken = String(data: idTokenData, encoding: .utf8)
            else {
                // identityToken fehlt — sollte bei Apple nie vorkommen
                authService.authError = "Apple-Anmeldung fehlgeschlagen. Bitte erneut versuchen."
                return
            }

            do {
                // KRITISCH: currentNonce ist rawNonce (NICHT sha256(currentNonce))
                // Supabase verifiziert intern: sha256(rawNonce) muss mit Apple-Token-Hash uebereinstimmen
                // Source: RESEARCH.md Pitfall 3
                try await supabase.auth.signInWithIdToken(
                    credentials: OpenIDConnectCredentials(
                        provider: .apple,
                        idToken: idToken,
                        nonce: currentNonce  // rawNonce! Nicht sha256(currentNonce)!
                    )
                )

                // Apple fullName nur beim ERSTEN Login verfuegbar — sofort persistieren!
                // Source: RESEARCH.md Pitfall 1 — bei zweitem Login ist credential.fullName nil
                if let fullName = credential.fullName,
                   let givenName = fullName.givenName,
                   !givenName.isEmpty {
                    let displayName = [givenName, fullName.familyName]
                        .compactMap { $0 }
                        .filter { !$0.isEmpty }
                        .joined(separator: " ")
                    // Fehler hier ist akzeptabel — User ist trotzdem eingeloggt
                    try? await supabase.auth.update(
                        user: UserAttributes(data: ["full_name": .string(displayName)])
                    )
                }
                // authStateChanges feuert SIGNED_IN → AuthService.startObserving() aktualisiert AppState

            } catch {
                authService.authError = authService.localizedError(from: error)
            }

        case .failure(let error as ASAuthorizationError) where error.code == .canceled:
            // User hat Apple-Dialog bewusst abgebrochen — KEIN Fehler anzeigen
            // Source: RESEARCH.md Pitfall 5
            break

        case .failure(let error):
            // Echter Fehler (Netzwerk, ungueltige Credentials etc.)
            authService.authError = error.localizedDescription
        }
    }

    // MARK: - Kryptographische Nonce-Helpers
    // Source: offizielles gotrue-swift Beispiel (github.com/supabase-community/gotrue-swift)

    private func randomNonce(length: Int = 32) -> String {
        var randomBytes = [UInt8](repeating: 0, count: length)
        let errorCode = SecRandomCopyBytes(kSecRandomDefault, randomBytes.count, &randomBytes)
        if errorCode != errSecSuccess {
            // SecRandomCopyBytes-Fehler ist ein Systemfehler — fatalError ist hier korrekt
            fatalError("Nonce-Generierung fehlgeschlagen: SecRandomCopyBytes Fehler \(errorCode)")
        }
        let charset = Array("0123456789ABCDEFGHIJKLMNOPQRSTUVXYZabcdefghijklmnopqrstuvwxyz-._")
        return String(randomBytes.map { charset[Int($0) % charset.count] })
    }

    private func sha256(_ input: String) -> String {
        let hashedData = SHA256.hash(data: Data(input.utf8))
        return hashedData.compactMap { String(format: "%02x", $0) }.joined()
    }
}
```

**Schritt 2: LoginView.swift modifizieren — SignInWithAppleView integrieren**

Die bestehende LoginView.swift LESEN, dann nach dem Login-Button (`.padding(.top, 8)`) folgendes einfuegen:

```swift
// Trennlinie
HStack {
    VStack { Divider().background(Color.secondary) }
    Text("oder")
        .font(.caption)
        .foregroundColor(.secondary)
        .padding(.horizontal, 8)
    VStack { Divider().background(Color.secondary) }
}
.padding(.horizontal, 24)

// Sign in with Apple
SignInWithAppleView()
    .padding(.horizontal, 24)
```

Dieser Block kommt nach dem Login-Button-Block und vor dem abschliessenden Spacer() oder Ende des VStack.
  </action>
  <verify>
    <automated>
xcodebuild build -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" -quiet 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `FamilyScore/Views/Auth/SignInWithAppleView.swift` existiert
    - `grep "import AuthenticationServices" FamilyScore/Views/Auth/SignInWithAppleView.swift` gibt Treffer
    - `grep "import CryptoKit" FamilyScore/Views/Auth/SignInWithAppleView.swift` gibt Treffer
    - `grep "SecRandomCopyBytes" FamilyScore/Views/Auth/SignInWithAppleView.swift` gibt Treffer (kryptographischer Nonce)
    - `grep "nonce: currentNonce" FamilyScore/Views/Auth/SignInWithAppleView.swift` gibt Treffer (rawNonce, nicht sha256)
    - `grep "sha256(nonce)" FamilyScore/Views/Auth/SignInWithAppleView.swift` gibt Treffer (sha256 an Apple-Request)
    - `grep "code == .canceled" FamilyScore/Views/Auth/SignInWithAppleView.swift` gibt Treffer (Cancel-Handling)
    - `grep "supabase.auth.update" FamilyScore/Views/Auth/SignInWithAppleView.swift` gibt Treffer (fullName-Persistenz)
    - `grep "SignInWithAppleView" FamilyScore/Views/Auth/LoginView.swift` gibt Treffer (integriert in LoginView)
    - `grep "@Environment(" FamilyScore/Views/Auth/SignInWithAppleView.swift` gibt 0 Zeilen (iOS 17-Syntax verboten; @EnvironmentObject verwenden)
    - `grep "@EnvironmentObject" FamilyScore/Views/Auth/SignInWithAppleView.swift` gibt Treffer (iOS 16 korrekte Syntax)
    - `xcodebuild build -scheme FamilyScore` gibt `BUILD SUCCEEDED` aus
  </acceptance_criteria>
  <done>SignInWithAppleView ist vollstaendig mit korrektem Nonce-Fluss und iOS-16-kompatibler @EnvironmentObject-Injection. Apple-Cancel zeigt keinen Fehler. fullName wird beim ersten Login persistiert. LoginView zeigt Apple-Button nach dem E-Mail+PW-Bereich.</done>
</task>

<task type="auto">
  <name>Task 2: FamilyScoreApp.swift — AuthService als Entry Point integrieren</name>
  <read_first>
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Common Pitfalls" — Pitfall 2 startObserving zu spaet — erklaert warum .task{} auf WindowGroup-Level sein muss)
    - .planning/phases/02-authentication/02-RESEARCH.md (Abschnitt "Architecture Patterns" → System Architecture Diagram)
    - FamilyScore/FamilyScore/FamilyScoreApp.swift (aktuellen Inhalt VOLLSTAENDIG lesen — hat noch ContentView + verifySupabaseConnection aus Phase 1)
    - FamilyScore/Views/RootView.swift (Wave 1 Ergebnis — wird der neue Entry Point)
    - FamilyScore/Services/AuthService.swift (wave 1 — @StateObject Injection benoetigt ObservableObject)
    - CLAUDE.md (Abschnitt "Supabase" → Realtime Hintergrund-Regel und "Supabase SDK NUR im Hauptapp-Target")
  </read_first>
  <files>
    FamilyScore/FamilyScore/FamilyScoreApp.swift
  </files>
  <action>
FamilyScoreApp.swift vollstaendig ersetzen. Den aktuellen Inhalt lesen (er hat noch ContentView und Phase 1 verifySupabaseConnection DEBUG-Code), dann durch folgenden ersetzen:

```swift
// FamilyScore/FamilyScoreApp.swift
// Target Membership: FamilyScore (App) ONLY
// Entry Point: @StateObject AuthService + RootView als Content + .environmentObject Injection

import SwiftUI

@main
struct FamilyScoreApp: App {

    // @StateObject: Lebenszyklus wird vom SwiftUI-Framework verwaltet (iOS 16 Muster)
    // NICHT @Observable — iOS 17+ und inkompatibel mit @StateObject-Injection
    @StateObject private var authService = AuthService()

    var body: some Scene {
        WindowGroup {
            RootView()
                // EnvironmentObject-Injection fuer alle Views im Hierarchy
                .environmentObject(authService)
                // startObserving() MUSS hier starten — nicht in einer untergeordneten View.
                // Pitfall 2: Wird .task erst in RootView oder LoginView aufgerufen,
                // kann INITIAL_SESSION bereits gesendet worden sein bevor die Beobachtung startet.
                // Source: RESEARCH.md Pitfall 2
                .task {
                    await authService.startObserving()
                }
                // Hintergrund: App wird schwarz wenn scenePhase = .background
                // Foreground: authStateChanges wird durch supabase-swift automatisch weitergefuehrt
                // Realtime reconnect (Phase 5) kommt spaeter; hier nur Auth-State beobachten
        }
    }
}

// MARK: - Phase 1 DEBUG-Verifikationsfunktionen
// Werden beibehalten fuer Rueckwaertskompatibilitaet mit Phase 1 SC-3/SC-4 Checks
// Koennen in Phase 6 (Cleanup) entfernt werden

#if DEBUG
func verifyAppGroup() {
    let suite = "group.com.familyscore"
    guard let defaults = UserDefaults(suiteName: suite) else {
        print("[DEBUG] App Group FAIL — UserDefaults(suiteName:) returned nil")
        return
    }
    let testKey = "phase1_verification"
    defaults.set(true, forKey: testKey)
    defaults.synchronize()
    let result = defaults.bool(forKey: testKey)
    print("[DEBUG] App Group Test: \(result ? "PASS" : "FAIL")")
}

func verifySupabaseConnection() async {
    do {
        struct AnyRow: Decodable {}
        let families: [AnyJSON] = try await supabase
            .from("families")
            .select()
            .execute()
            .value
        print("[DEBUG] Supabase Connection PASS — families returned \(families.count) rows")
    } catch {
        print("[DEBUG] Supabase Connection FAIL: \(error)")
    }
}
#endif
```

HINWEIS: ContentView.swift aus Phase 1 wird durch RootView() ersetzt. ContentView.swift kann bestehen bleiben (ungenutzt) oder geloescht werden — der Executor entscheidet. Wichtig ist nur dass FamilyScoreApp.swift RootView() als Wurzel-View nutzt.
  </action>
  <verify>
    <automated>
xcodebuild build -scheme FamilyScore -destination "platform=iOS Simulator,name=iPhone 16" -quiet 2>&1 | tail -5
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep "@StateObject private var authService = AuthService()" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer
    - `grep "RootView()" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer
    - `grep ".environmentObject(authService)" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer
    - `grep "authService.startObserving()" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer
    - `grep "\.task" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer (nicht in einer Sub-View)
    - `xcodebuild build -scheme FamilyScore` gibt `BUILD SUCCEEDED` aus
  </acceptance_criteria>
  <done>FamilyScoreApp.swift ist der korrekte Entry Point. AuthService als @StateObject injiziert. startObserving() startet via .task auf WindowGroup-Level. RootView ist die Wurzel-View.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: [CHECKPOINT] Sign in with Apple + Session-Persistenz auf echtem Geraet verifizieren</name>
  <what-built>
    Task 1 hat SignInWithAppleView mit vollstaendigem Nonce-Fluss erstellt und in LoginView integriert.
    Task 2 hat FamilyScoreApp.swift als vollstaendigen Auth-Entry-Point konfiguriert.
    Die Automated-Tests (AUTH-01, AUTH-04) laufen im Simulator.
    AUTH-02 (Sign in with Apple) und AUTH-03 (Keychain-Persistenz nach App-Kill) brauchen ein echtes Geraet.
  </what-built>
  <how-to-verify>
**Voraussetzung: Sign in with Apple Capability in Xcode aktivieren**
```
Xcode → FamilyScore Target → Signing & Capabilities → + Capability → Sign in with Apple
```

**Voraussetzung: Automated Tests gruenschalten**
```bash
xcodebuild test \
  -project "FamilyScore/FamilyScore.xcodeproj" \
  -scheme FamilyScore \
  -destination "platform=iOS Simulator,name=iPhone 16" \
  -only-testing FamilyScoreTests/AuthServiceTests 2>&1 | grep -E "passed|failed|PASS|FAIL"
# Erwartet: alle Tests passed
```

**AUTH-02: Sign in with Apple auf echtem Geraet**

1. App auf echtem iPhone deployen (iOS 16+)
2. App starten — Loading-Indicator erscheint kurz
3. "Sign in with Apple" Button antippen
4. Apple-Biometrie oder Passwort bestaetigen
5. Erwartet: App navigiert zu OnboardingPlaceholderView ("Willkommen! Familiengruppe einrichten kommt bald")
6. Supabase Dashboard → Authentication → Users — neuer User muss erscheinen

**AUTH-02 Abbruch-Test:**
1. "Sign in with Apple" antippen
2. Im Apple-Dialog "Abbrechen" tippen
3. Erwartet: KEIN Fehler-Banner erscheint, Login-Screen bleibt unveraendert

**AUTH-03: Session-Persistenz nach App-Kill**
1. Mit E-Mail+Passwort einloggen (Auth-02-Test-Account oder separaten Account)
2. App im App Switcher killen (nach oben wischen)
3. App neu starten
4. Erwartet: Loading-Indicator erscheint kurz, dann direkt OnboardingPlaceholderView — KEIN Login-Screen

**AUTH-04: Ausloggen**
1. Im OnboardingPlaceholderView "Ausloggen" antippen
2. Erwartet: App navigiert sofort zu AuthFlowView (Login-Screen)
3. App killen und neu starten
4. Erwartet: Login-Screen erscheint (kein automatisches Re-Login nach signOut)

**AUTH-01: E-Mail+Passwort im Simulator (falls noch nicht verifiziert)**
```bash
# Simulator starten und App dort testen
xcodebuild build \
  -project "FamilyScore/FamilyScore.xcodeproj" \
  -scheme FamilyScore \
  -destination "platform=iOS Simulator,name=iPhone 16" \
  -quiet
# App im Simulator starten → Registrierung mit Testdaten durchfuehren
```

**Supabase RLS-Verifikation (via Swift Client — NICHT Dashboard):**
Nach Login in Xcode Console:
- Kein `[Phase1] Supabase Connection FAIL` (Phase 1 DEBUG-Code laeuft noch)
- Kein AuthError-Banner fuer normale Aktionen
  </how-to-verify>
  <resume-signal>
Gib "AUTH verifiziert" ein wenn:
- AUTH-01 (E-Mail+PW): Registrierung + Login + Fehler bei falschem PW funktioniert
- AUTH-02 (Apple): Sign in erfolgreich auf echtem Geraet; Abbruch zeigt keinen Fehler
- AUTH-03 (Persistenz): App-Kill + Neustart behaelt Session
- AUTH-04 (Logout): signOut leert Session und zeigt Login-Screen

Oder beschreibe welche der 4 Requirements noch nicht besteht.
  </resume-signal>
</task>

</tasks>

<notes>
## Scope-Klarstellung: Edge Function und Sign in with Apple

ROADMAP.md SC-2 erwähnt eine "Edge Function invite path" — diese bezieht sich NICHT auf Sign in with Apple in diesem Plan.

Klarstellung:
- **Sign in with Apple (dieser Plan):** Funktioniert vollstaendig client-seitig via `signInWithIdToken(provider: .apple)`. Kein Edge Function Layer noetig. Bestaetigt durch offizielle Supabase Docs ("native apps do not need OAuth settings or backend processing"). Source: RESEARCH.md Summary + Pattern 3.
- **Edge Function (Phase 3):** Die Edge Function wird fuer `auth.admin.inviteUserByEmail()` benoetigt — den Familien-Einladungs-Fluss. Der Service-Role-Key darf nie im iOS-Client sein; deshalb braucht Phase 3 eine Edge Function als Proxy. Das ist ein anderer Fluss und ein anderes Requirement.

Wenn SC-2 in der Verifikation geprueft wird: die relevante Pruefung fuer diesen Plan ist ob Sign in with Apple ohne Edge Function funktioniert (ja, per RESEARCH.md bestaetigt). Die Edge Function fuer Invites ist Phase-3-Scope.
</notes>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| iOS App → Apple Identity Service | ASAuthorizationController kommuniziert direkt mit Apple; kein Server-Relay noetig fuer Sign in with Apple in native Apps |
| Apple Identity Token → Supabase Auth | Supabase validiert idToken gegen Apples JWKS-Endpoint; SHA256-Nonce verhindert Token-Replay |
| Keychain → App-Session | supabase-swift liest/schreibt Keychain via KeychainLocalStorage; iOS App-Sandbox isoliert vom Rest des Systems |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-05 | Spoofing | Apple Identity Token Replay | mitigate | SHA256-Nonce: `SecRandomCopyBytes` generiert kryptographisch sicheren rawNonce; Apple baut sha256(rawNonce) in Token ein; Supabase verifiziert; Token ist einmalig verwendbar |
| T-2-06 | Spoofing | Nonce-Verwechslung (sha256 statt raw) | mitigate | Code-Kommentar explizit in SignInWithAppleView: `// rawNonce, NICHT sha256(currentNonce)!`; Acceptance Criterion prueft `grep "nonce: currentNonce"` |
| T-2-07 | Information Disclosure | Apple fullName im Memory | accept | fullName ist kurz im @State bevor .update() aufgerufen wird; kein Logging; iOS-App-Sandbox; Risiko minimal |
| T-2-08 | Elevation of Privilege | Service-Role-Key in iOS-Client | mitigate | signInWithIdToken braucht keinen Service-Role-Key; nur anon/publishable Key im Client; verifiziert in RESEARCH.md "Kein Edge Function" |
| T-2-09 | Information Disclosure | Session-Token im Keychain | accept | iOS Keychain mit `kSecAttrAccessibleAfterFirstUnlock`; App-Sandbox; keine iCloud-Sync da supabase-swift kSecAttrSynchronizable nicht setzt; Angriff erfordert physischen Geraetezugang + Jailbreak |
| T-2-10 | Denial of Service | startObserving() zu spaet gestartet | mitigate | .task{} auf WindowGroup-Level in FamilyScoreApp.swift stellt sicher dass INITIAL_SESSION nie verpasst wird; Acceptance Criterion prueft `grep "\.task" FamilyScoreApp.swift` |
</threat_model>

<verification>
## Plan 03 Verifikation

**Build:**
```bash
xcodebuild build \
  -project "FamilyScore/FamilyScore.xcodeproj" \
  -scheme FamilyScore \
  -destination "platform=iOS Simulator,name=iPhone 16" \
  -quiet 2>&1 | grep -E "BUILD|error:"
# Erwartet: BUILD SUCCEEDED
```

**Nonce-Korrektheit (kritisch fuer AUTH-02 Security):**
```bash
# Supabase bekommt rawNonce (currentNonce), NICHT sha256
grep "nonce: currentNonce" FamilyScore/Views/Auth/SignInWithAppleView.swift
# Erwartet: Treffer

# Apple bekommt sha256(rawNonce)
grep "request.nonce = sha256" FamilyScore/Views/Auth/SignInWithAppleView.swift
# Erwartet: Treffer

# Kein Fehler bei Cancel
grep "code == .canceled" FamilyScore/Views/Auth/SignInWithAppleView.swift
# Erwartet: Treffer
```

**iOS 16 Kompatibilitaet in SignInWithAppleView:**
```bash
grep "@Environment(" FamilyScore/Views/Auth/SignInWithAppleView.swift
# Erwartet: 0 Zeilen (iOS 17-Syntax verboten)

grep "@EnvironmentObject" FamilyScore/Views/Auth/SignInWithAppleView.swift
# Erwartet: Treffer
```

**App Entry Point korrekt:**
```bash
grep "authService.startObserving" FamilyScore/FamilyScore/FamilyScoreApp.swift
# Erwartet: Treffer (nicht in einer Sub-View)

grep "@StateObject" FamilyScore/FamilyScore/FamilyScoreApp.swift
# Erwartet: Treffer
```

**Alle automatisierbaren Tests gruenschalten:**
```bash
xcodebuild test \
  -project "FamilyScore/FamilyScore.xcodeproj" \
  -scheme FamilyScore \
  -destination "platform=iOS Simulator,name=iPhone 16" \
  -only-testing FamilyScoreTests/AuthServiceTests 2>&1 | grep -E "passed|failed"
# Erwartet: 8 tests passed, 0 tests failed
```
</verification>

<success_criteria>
- SignInWithAppleView.swift: rawNonce an Supabase, sha256(rawNonce) an Apple, Apple-Cancel ohne Fehler, @EnvironmentObject (iOS 16)
- fullName-Persistenz via supabase.auth.update() direkt nach erstem Login
- FamilyScoreApp.swift: @StateObject AuthService, RootView als Entry, .task{} auf WindowGroup-Level
- xcodebuild build → BUILD SUCCEEDED
- Alle 8 MockAuthService-basierte Unit-Tests gruengeschaltet
- Checkpoint: AUTH-02 auf echtem Geraet manuell verifiziert
- Checkpoint: AUTH-03 (Session-Persistenz nach App-Kill) auf echtem Geraet manuell verifiziert
</success_criteria>

<output>
Nach Abschluss `.planning/phases/02-authentication/02-03-SUMMARY.md` erstellen mit:
- SignInWithAppleView Pfad + Nonce-Fluss-Status
- FamilyScoreApp.swift Aenderungen
- Checkpoint-Ergebnis (AUTH-02, AUTH-03 manuell verifiziert)
- Alle 4 AUTH-Requirements: PASS / FAIL / PARTIAL
- Phase 2 abgeschlossen: JA / NEIN
</output>
