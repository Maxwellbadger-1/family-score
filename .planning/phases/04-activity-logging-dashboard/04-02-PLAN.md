---
phase: 04-activity-logging-dashboard
plan: 02
type: execute
wave: 1
depends_on:
  - 04-01
files_modified:
  - FamilyScore/FamilyScore/Services/ActivityService.swift
  - FamilyScore/FamilyScore/Models/ActivityEntry.swift
autonomous: true
requirements:
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-04
  - LOG-05
  - LOG-06
  - SCORE-01
  - SCORE-02

must_haves:
  truths:
    - "ActivityService ladt Kategorien, Tageseintraege und Tagesscore via Supabase RPCs ohne Absturz"
    - "logActivity() fuegt einen Eintrag optimistisch lokal ein und bestaetigt per Supabase INSERT"
    - "deleteActivity() entfernt einen Eintrag optimistisch lokal und bestaetigt per Supabase DELETE"
    - "Timer startet (startTimer), stoppt (stopTimer) und ueberlebt App-Kill via @AppStorage"
    - "Kinder-Eintrag wird via create_activity_for_child RPC (SECURITY DEFINER) erstellt — kein direktes INSERT mit fremder user_id"
    - "refreshLocalRingProgress() berechnet dutyProgress, leisureProgress, scoreProgress aus lokalen Eintraegen — kein Akkumulieren, kein total_score-Column"
    - "duration_minutes wird auf max 240 min gecappt (DoS-Schutz T-4-05)"
    - "xcodebuild test ActivityServiceTests gruen"
  artifacts:
    - path: "FamilyScore/FamilyScore/Services/ActivityService.swift"
      provides: "@MainActor ObservableObject — alle CRUD-Methoden + RPC-Calls + Timer + Optimistic UI"
      exports: ["ActivityService"]
      min_lines: 200
    - path: "FamilyScore/FamilyScore/Models/ActivityEntry.swift"
      provides: "Hinweis-Datei: Modelle sind in ActivityServiceProtocol.swift zentralisiert"
      contains: "ActivityServiceProtocol.swift"
  key_links:
    - from: "FamilyScore/FamilyScore/Services/ActivityService.swift"
      to: "supabase.rpc(\"get_today_score\")"
      via: "fetchTodayData() → RPC-Aufruf"
      pattern: "rpc.*get_today_score"
    - from: "FamilyScore/FamilyScore/Services/ActivityService.swift"
      to: "public.activity_entries"
      via: "supabase.from(\"activity_entries\").insert(...)"
      pattern: "from.*activity_entries.*insert"
    - from: "FamilyScore/FamilyScore/Services/ActivityService.swift"
      to: "create_activity_for_child RPC"
      via: "logActivityForChild() → supabase.rpc(...)"
      pattern: "rpc.*create_activity_for_child"
---

<objective>
Wave 1: ActivityService implementieren — vollstaendiger CRUD + Timer + Optimistic UI + RPC-Calls.

Purpose: Datenschicht fuer Phase 4. Alle UI-Komponenten (Wave 2 + 3) erhalten ihre Daten ausschliesslich ueber ActivityService als @EnvironmentObject.

Output:
- ActivityService.swift als @MainActor ObservableObject mit vollstaendiger ActivityServiceProtocol-Konformanz
- ActivityEntry.swift als Redirect-Datei (alle Modelle liegen in ActivityServiceProtocol.swift)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-activity-logging-dashboard/04-RESEARCH.md
@.planning/phases/04-activity-logging-dashboard/04-PATTERNS.md

<interfaces>
<!-- Bestehende Contracts — Executor MUSS lesen bevor er schreibt -->

Aus FamilyScore/FamilyScore/Services/AuthService.swift (Muster-Analog — exakte Struktur kopieren):
```swift
@MainActor
final class AuthService: ObservableObject {
    @Published private(set) var currentUser: User? = nil
    @Published var authError: String? = nil
    func localizedError(from error: Error) -> String { ... }
}
```

Aus FamilyScore/FamilyScore/FamilyScoreApp.swift (Injection-Punkt fuer Wave 3):
```swift
@StateObject private var authService = AuthService()
// Wave 3 ergaenzt: @StateObject private var activityService = ActivityService()
```

Aus FamilyScore/FamilyScore/Services/ActivityServiceProtocol.swift (Wave 0 — MUSS gelesen werden):
```swift
protocol ActivityServiceProtocol: AnyObject {
    var todayEntries: [ActivityEntry] { get }
    var categories: [CategoryConfig] { get }
    var dayScore: DayScore? { get }
    var weeklyScores: [WeeklySummary] { get }
    var familyDayScores: [MemberDayScore] { get }
    var dutyProgress: Double { get }
    var leisureProgress: Double { get }
    var scoreProgress: Double { get }
    var totalScore: Int { get }
    var timerIsRunning: Bool { get }
    var activityError: String? { get set }
    func fetchTodayData() async throws
    func fetchFamilyData() async throws
    func fetchWeeklyData() async throws
    func logActivity(categoryId: UUID, durationMinutes: Int, title: String?) async throws
    func logActivityForChild(childUserId: UUID, categoryId: UUID, durationMinutes: Int, title: String?) async throws
    func deleteActivity(id: UUID) async throws
    func startTimer(categoryId: UUID)
    func stopTimer(title: String?) async throws
}
// Model-Structs: ActivityEntry, NewActivityEntry, CategoryConfig, DayScore,
//                MemberDayScore, WeeklySummary, CategoryBreakdown, WeekMemberScore, RingType
```

Supabase-Query-Patterns aus RESEARCH.md (verifiziert):
```swift
// SELECT:
let result: [T] = try await supabase.from("tabelle").select("*").eq("spalte", value: wert).execute().value
// INSERT + zurueckgelesen:
let saved: T = try await supabase.from("tabelle").insert(entry).select().single().execute().value
// DELETE:
try await supabase.from("tabelle").delete().eq("id", value: id).execute()
// RPC:
let score: [DayScore] = try await supabase.rpc("get_today_score").execute().value
```

Timer-Pattern aus RESEARCH.md Pattern 6:
```swift
@AppStorage("timerStartedAt") private var timerStartedAtInterval: Double = 0
@AppStorage("timerCategoryId") private var timerCategoryIdString: String = ""
// UUID nicht direkt in @AppStorage (Pitfall 2) — als String speichern
```

Optimistic UI Pattern aus RESEARCH.md Pattern 10:
```swift
// Lokal vor DB-Antwort:
todayEntries.insert(optimisticEntry, at: 0)
refreshLocalRingProgress()
do { /* save */ } catch { todayEntries = backup; refreshLocalRingProgress() }
```

ChildActivityParams fuer create_activity_for_child RPC:
```swift
struct ChildActivityParams: Encodable {
    let pChildUserId: UUID; let pCategoryId: UUID
    let pDurationMin: Int; let pPoints: Double; let pTitle: String?
    enum CodingKeys: String, CodingKey {
        case pChildUserId = "p_child_user_id"; case pCategoryId = "p_category_id"
        case pDurationMin = "p_duration_min"; case pPoints = "p_points"; case pTitle = "p_title"
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ActivityService.swift implementieren</name>
  <files>
    FamilyScore/FamilyScore/Services/ActivityService.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/Services/ActivityServiceProtocol.swift (Wave 0 — Protocol + alle Modelle)
    - FamilyScore/FamilyScore/Services/AuthService.swift (exakter Stil-Analog: @MainActor, @Published, localizedError)
    - FamilyScore/FamilyScore/Supabase.swift (Singleton: `supabase`-Konstante)
    - .planning/phases/04-activity-logging-dashboard/04-RESEARCH.md (Pattern 3 logActivity, Pattern 5 deleteActivity, Pattern 6 Timer, Pattern 10 Optimistic UI)
    - .planning/phases/04-activity-logging-dashboard/04-PATTERNS.md (ActivityService Abschnitt — Imports, Klassendefinition, Supabase-Query-Kette)
  </read_first>
  <behavior>
    - Klasse: `@MainActor final class ActivityService: ObservableObject, ActivityServiceProtocol`
    - Alle @Published private(set) Properties: todayEntries, categories, dayScore, weeklyScores, familyDayScores, dutyProgress, leisureProgress, scoreProgress, totalScore
    - activityError: @Published var (ohne private(set) — UI darf nil setzen)
    - Timer-Persistenz via @AppStorage (String, nicht UUID direkt — Pitfall 2)
    - fetchTodayData(): laedt categories (category_config), todayEntries (activity_entries), dayScore (get_today_score RPC). Nach Laden: refreshLocalRingProgress()
    - fetchFamilyData(): laedt familyDayScores (get_family_today_scores RPC)
    - fetchWeeklyData(): laedt weeklyScores (weekly_summaries Tabelle — ORDER BY week_start DESC LIMIT 1 fuer aktuelle Woche)
    - logActivity(): Optimistic Insert → INSERT activity_entries → ersetze optimistischen Eintrag durch echten → refreshLocalRingProgress() → get_today_score RPC neu laden (Pitfall 6 aus RESEARCH.md)
    - logActivityForChild(): create_activity_for_child RPC via supabase.rpc(). KEIN direktes INSERT. Nach Erfolg: fetchTodayData()
    - deleteActivity(): Optimistic Delete → DELETE activity_entries → bei Fehler Rollback → refreshLocalRingProgress()
    - refreshLocalRingProgress(): berechnet dutyProgress/leisureProgress/scoreProgress aus lokalen todayEntries. 60 pts = Ring full (UI-SPEC). NIEMALS akkumuliert gespeichert.
    - duration_minutes Swift-seitig auf min 1 / max 240 gecappt bevor INSERT (T-4-05)
    - localizedError(from:): exakt selbe Struktur wie AuthService.localizedError — nur nutzerfreundliche Strings
    - target: FamilyScore App-Target ONLY. Supabase-Import NUR hier, nicht in Protocol-Datei.
  </behavior>
  <action>
Erstelle `FamilyScore/FamilyScore/Services/ActivityService.swift`. Lese zuerst AuthService.swift und Supabase.swift um den genauen Singleton-Namen (`supabase`) zu bestaetigen.

Vollstaendige Implementierung gemaess den behavior-Punkten oben. Kritische Stellen:

**Imports + Klassen-Header:**
```swift
// FamilyScore/Services/ActivityService.swift
// Target Membership: FamilyScore (App) ONLY
// Supabase SDK NUR hier — nicht in FamilyScoreKit, nicht in Widget Extension (CLAUDE.md)
// iOS 16.0 Minimum: ObservableObject + @Published (NICHT @Observable — iOS 17+)

import Foundation
import Supabase

@MainActor
final class ActivityService: ObservableObject, ActivityServiceProtocol {

    @Published private(set) var todayEntries: [ActivityEntry] = []
    @Published private(set) var categories: [CategoryConfig] = []
    @Published private(set) var dayScore: DayScore? = nil
    @Published private(set) var weeklyScores: [WeeklySummary] = []
    @Published private(set) var familyDayScores: [MemberDayScore] = []
    @Published private(set) var dutyProgress: Double = 0.0
    @Published private(set) var leisureProgress: Double = 0.0
    @Published private(set) var scoreProgress: Double = 0.0
    @Published private(set) var totalScore: Int = 0
    @Published var activityError: String? = nil

    // Timer-Persistenz (Pitfall 2: kein UUID direkt in @AppStorage — als String)
    @AppStorage("timerStartedAt") private var timerStartedAtInterval: Double = 0
    @AppStorage("timerCategoryId") private var timerCategoryIdString: String = ""

    var timerIsRunning: Bool { timerStartedAtInterval > 0 }
    private var timerStartedAt: Date? {
        timerStartedAtInterval > 0 ? Date(timeIntervalSince1970: timerStartedAtInterval) : nil
    }
    var elapsedSeconds: TimeInterval {
        guard let start = timerStartedAt else { return 0 }
        return Date.now.timeIntervalSince(start)
    }
```

**fetchTodayData():** categories aus category_config (family_id-Filter aus currentFamilyId), todayEntries aus activity_entries (today + family_id), dayScore via get_today_score RPC. currentFamilyId aus AppState (via AuthService) oder aus erstem category_config-Eintrag.

**Wichtig fuer currentFamilyId:** ActivityService benoetigt die family_id. Injiziere sie bei init oder via setter. Einfachste Loesung fuer Phase 4: `var currentFamilyId: UUID?` als Property, wird von FamilyScoreApp.swift nach dem Family-Load gesetzt. Alternativ: family_id aus AppState lesen falls AppState diese enthaelt. Da AppState aus Phase 2 nur `authenticated(hasFamily:)` kennt, ist die setter-Variante sicherer.

**refreshLocalRingProgress() — NIEMALS akkumulieren:**
```swift
func refreshLocalRingProgress() {
    let dutyIds = Set(categories.filter { $0.ringType == .duty }.map(\.id))
    let leisureIds = Set(categories.filter { $0.ringType == .leisure }.map(\.id))
    let dutyPts = todayEntries.filter { dutyIds.contains($0.categoryId) }.reduce(0.0) { $0 + $1.points }
    let leisurePts = todayEntries.filter { leisureIds.contains($0.categoryId) }.reduce(0.0) { $0 + $1.points }
    let totalPts = todayEntries.reduce(0.0) { $0 + $1.points }
    // 60 pts = Ring full (UI-SPEC Score Contract)
    dutyProgress = dutyPts / 60.0
    leisureProgress = leisurePts / 60.0
    scoreProgress = totalPts / 60.0
    totalScore = Int(totalPts)
}
```

**Timer:**
```swift
func startTimer(categoryId: UUID) {
    timerStartedAtInterval = Date.now.timeIntervalSince1970
    timerCategoryIdString = categoryId.uuidString
}
func stopTimer(title: String?) async throws {
    guard let start = timerStartedAt,
          let categoryId = UUID(uuidString: timerCategoryIdString) else { return }
    let minutes = max(1, min(240, Int(Date.now.timeIntervalSince(start) / 60)))
    timerStartedAtInterval = 0
    timerCategoryIdString = ""
    try await logActivity(categoryId: categoryId, durationMinutes: minutes, title: title)
}
```

**logActivity() — Optimistic UI + Rollback (RESEARCH.md Pattern 3 + 10):**
```swift
func logActivity(categoryId: UUID, durationMinutes: Int, title: String?) async throws {
    guard let familyId = currentFamilyId,
          let userId = currentUserId else { throw ActivityError.notAuthenticated }
    let capped = max(1, min(240, durationMinutes))  // T-4-05 DoS-Schutz
    let weight = categories.first(where: { $0.id == categoryId })?.pointWeight ?? 1.0
    let pts = Double(capped) * weight
    // Optimistischer Eintrag (temp UUID)
    let tempId = UUID()
    let optimistic = ActivityEntry(id: tempId, familyId: familyId, userId: userId,
                                   categoryId: categoryId, durationMinutes: capped,
                                   points: pts, title: title, loggedAt: Date())
    todayEntries.insert(optimistic, at: 0)
    refreshLocalRingProgress()
    let backup = todayEntries
    do {
        let entry = NewActivityEntry(familyId: familyId, userId: userId,
                                     categoryId: categoryId, durationMinutes: capped,
                                     points: pts, title: title)
        let saved: ActivityEntry = try await supabase
            .from("activity_entries").insert(entry).select().single().execute().value
        todayEntries.removeAll { $0.id == tempId }
        todayEntries.insert(saved, at: 0)
        // Authoritativen Wert vom Server laden (Pitfall 6)
        let scores: [DayScore] = try await supabase.rpc("get_today_score").execute().value
        dayScore = scores.first
        refreshLocalRingProgress()
    } catch {
        todayEntries = backup
        refreshLocalRingProgress()
        activityError = localizedError(from: error)
        throw error
    }
}
```

**logActivityForChild() — SECURITY DEFINER RPC (T-4-01 mitigiert):**
```swift
func logActivityForChild(childUserId: UUID, categoryId: UUID, durationMinutes: Int, title: String?) async throws {
    let capped = max(1, min(240, durationMinutes))
    let weight = categories.first(where: { $0.id == categoryId })?.pointWeight ?? 1.0
    struct ChildParams: Encodable {
        let pChildUserId: UUID; let pCategoryId: UUID
        let pDurationMin: Int; let pPoints: Double; let pTitle: String?
        enum CodingKeys: String, CodingKey {
            case pChildUserId = "p_child_user_id"; case pCategoryId = "p_category_id"
            case pDurationMin = "p_duration_min"; case pPoints = "p_points"; case pTitle = "p_title"
        }
    }
    let params = ChildParams(pChildUserId: childUserId, pCategoryId: categoryId,
                              pDurationMin: capped, pPoints: Double(capped) * weight, pTitle: title)
    do {
        let _: ActivityEntry = try await supabase.rpc("create_activity_for_child", params: params).execute().value
        try await fetchTodayData()
    } catch {
        activityError = localizedError(from: error)
        throw error
    }
}
```

**deleteActivity() — RLS entscheidet (Pattern 5):**
```swift
func deleteActivity(id: UUID) async throws {
    let backup = todayEntries
    todayEntries.removeAll { $0.id == id }
    refreshLocalRingProgress()
    do {
        try await supabase.from("activity_entries").delete().eq("id", value: id).execute()
    } catch {
        todayEntries = backup
        refreshLocalRingProgress()
        activityError = localizedError(from: error)
        throw error
    }
}
```

**localizedError(from:):** Exakt wie AuthService.localizedError, aber angepasste Strings fuer Activity-Fehler.

**currentUserId:** aus `supabase.auth.currentUser?.id` — kein zusaetzlicher Parameter.

**ActivityError enum** am Ende der Datei:
```swift
enum ActivityError: Error { case notAuthenticated }
```
  </action>
  <verify>
    <automated>xcodebuild build -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "class ActivityService: ObservableObject, ActivityServiceProtocol" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer
    - `grep "@MainActor" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer
    - `grep "import Supabase" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer
    - `grep "rpc.*get_today_score" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer
    - `grep "rpc.*create_activity_for_child" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer
    - `grep "from.*activity_entries.*insert\|activity_entries.*delete" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt mindestens 2 Treffer
    - `grep "timerStartedAtInterval\|timerCategoryIdString" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer (Timer-Persistenz)
    - `grep "LEAST\|min(240\|max(1" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer (DoS-Cap T-4-05)
    - `grep "dutyProgress / 60\|/ 60.0" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer (Ring-Progress-Berechnung)
    - xcodebuild ergibt "BUILD SUCCEEDED"
  </acceptance_criteria>
  <done>ActivityService.swift kompiliert ohne Fehler. Alle Protocol-Methoden implementiert. Optimistic UI mit Rollback, Timer-AppStorage, create_activity_for_child via RPC.</done>
</task>

<task type="auto">
  <name>Task 2: ActivityEntry.swift Stub + ActivityServiceTests auf echte Assertions aktualisieren</name>
  <files>
    FamilyScore/FamilyScore/Models/ActivityEntry.swift
    FamilyScore/FamilyScoreTests/ActivityServiceTests.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/Services/ActivityServiceProtocol.swift (alle Modelle sind hier — nicht in Models/ActivityEntry.swift)
    - FamilyScore/FamilyScoreTests/ActivityServiceTests.swift (Wave-0-Stubs — jetzt echte Assertions ergaenzen)
    - FamilyScore/FamilyScoreTests/Mocks/MockActivityService.swift (MockActivityService — wird in Tests verwendet)
  </read_first>
  <action>
**ActivityEntry.swift** — Erstelle eine Stub-Datei als Hinweis fuer Entwickler (Modelle liegen in ActivityServiceProtocol.swift). Kein doppelter Code.

```swift
// FamilyScore/Models/ActivityEntry.swift
// Target Membership: FamilyScore (App) ONLY
//
// HINWEIS: ActivityEntry, NewActivityEntry, CategoryConfig, DayScore,
// MemberDayScore, WeeklySummary, RingType und alle weiteren Phase-4-Modelle
// sind in FamilyScore/Services/ActivityServiceProtocol.swift definiert.
//
// Diese Datei existiert nur damit der Dateipfad in der Projektstruktur sichtbar ist.
// Kein Code hier — keine Duplikate.
```

**ActivityServiceTests.swift** — Ergaenze die Wave-0-Stubs um echte Verhaltens-Tests mit MockActivityService. Bestehende Stubs NICHT entfernen — nur praezisieren und neue hinzufuegen:

Ergaenze folgende praezise Tests (nach dem bestehenden Stub-Block einfuegen):

```swift
// LOG-02: Dauer wird korrekt uebergeben
func testLogActivityPassesDurationCorrectly() async throws {
    let duration = 45
    try await mock.logActivity(categoryId: UUID(), durationMinutes: duration, title: nil)
    XCTAssertEqual(mock.lastLoggedDuration, duration)
}

// LOG-05: Fehler bei Log wirft MockActivityError
func testLogActivityThrowsWhenFlagSet() async throws {
    mock.shouldThrowOnLog = true
    do {
        try await mock.logActivity(categoryId: UUID(), durationMinutes: 30, title: nil)
        XCTFail("Soll Fehler werfen")
    } catch MockActivityError.logFailed {
        XCTAssertEqual(mock.logActivityCallCount, 1, "CallCount muss trotz Fehler erhoehen")
    }
}

// DASH-01: Freizeit-Ring-Progress korrekt
func testLeisureProgressReadable() {
    mock.leisureProgress = 0.75
    XCTAssertEqual(mock.leisureProgress, 0.75, accuracy: 0.001)
}

// LOG-04: Kind-Eintrag erhoht logForChildCallCount
func testLogForChildIncreasesCountCorrectly() async throws {
    try await mock.logActivityForChild(childUserId: UUID(), categoryId: UUID(), durationMinutes: 10, title: "Zimmer aufraeumen")
    XCTAssertEqual(mock.logForChildCallCount, 1)
}

// LOG-01: stopTimer ruft logActivity auf und setzt timerIsRunning = false
func testStopTimerCallsLogAndStopsTimer() async throws {
    mock.startTimer(categoryId: UUID())
    XCTAssertTrue(mock.timerIsRunning)
    try await mock.stopTimer(title: "Fertig")
    XCTAssertFalse(mock.timerIsRunning)
    // logActivity wurde in stopTimer aufgerufen
    XCTAssertGreaterThanOrEqual(mock.logActivityCallCount, 1)
}
```
  </action>
  <verify>
    <automated>xcodebuild test -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:FamilyScoreTests/ActivityServiceTests 2>&1 | grep -E "Test.*passed|Test.*failed|TEST SUCCEEDED|TEST FAILED" | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "HINWEIS: ActivityEntry" FamilyScore/FamilyScore/Models/ActivityEntry.swift` gibt Treffer (Stub-Datei vorhanden)
    - `grep -c "func test" FamilyScore/FamilyScoreTests/ActivityServiceTests.swift` gibt mindestens 14 (11 Stubs + 5 neue = 16 gesamt)
    - xcodebuild test ActivityServiceTests: "TEST SUCCEEDED" oder alle Failures sind Simulator-Infrastrukturfehler (nicht Logic-Fehler)
    - `grep "testLogForChildIncreasesCountCorrectly\|testStopTimerCallsLogAndStopsTimer\|testLogActivityPassesDurationCorrectly" FamilyScore/FamilyScoreTests/ActivityServiceTests.swift` gibt 3 Treffer
  </acceptance_criteria>
  <done>ActivityEntry.swift als Stub vorhanden. ActivityServiceTests hat 16+ Tests — alle gruen per xcodebuild.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| ActivityService → Supabase | JWT-authentifizierte REST-Calls; RLS auf activity_entries aktiv |
| logActivity → activity_entries INSERT | Client berechnet points; DB speichert was Client sendet |
| logActivityForChild → create_activity_for_child RPC | RPC prueft Eltern-Kind-Beziehung server-seitig |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|------------|-------------|-----------------|
| T-4-01 | Tampering | `logActivityForChild` — fremde user_id | mitigate | Ausschliesslich via `create_activity_for_child` RPC (SECURITY DEFINER); kein direktes INSERT mit fremder user_id in ActivityService |
| T-4-02 | Tampering | `deleteActivity` — fremde Eintraege | accept | RLS-Policy aus Phase 1 entscheidet; kein Swift-seitiger Role-Check noetig. Echte Pruefung nur auf Geraet mit JWT. |
| T-4-03 | Tampering | Client berechnet `points` bei logActivity | accept (Phase 4) | Phase 4: pointWeight = 1.0 fix. DB-Constraint `points >= 0` vorhanden. Phase 6: DB-Trigger. |
| T-4-04 | Tampering | INSERT mit falscher family_id | mitigate | currentFamilyId kommt aus authentifizierter Session; RLS `is_family_member(family_id)` als zweite Linie |
| T-4-05 | DoS | duration_minutes ohne Cap | mitigate | Swift-seitig: `max(1, min(240, durationMinutes))` vor jedem INSERT; RPC: `GREATEST(1, LEAST(240, p_duration_min))` |
</threat_model>

<verification>
Nach Abschluss beider Tasks:

1. `xcodebuild build -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16'` — BUILD SUCCEEDED
2. `xcodebuild test -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:FamilyScoreTests/ActivityServiceTests` — TEST SUCCEEDED
3. `grep "rpc.*get_today_score" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer
4. `grep "rpc.*create_activity_for_child" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer
5. `grep "min(240" FamilyScore/FamilyScore/Services/ActivityService.swift` gibt Treffer (DoS-Cap)
</verification>

<success_criteria>
- ActivityService.swift kompiliert und conformt vollstaendig zu ActivityServiceProtocol
- Optimistic UI (Insert + Delete) mit Rollback implementiert
- Timer ueber @AppStorage persistent (ueberlebt App-Kill)
- Kind-Eintrag via SECURITY DEFINER RPC (kein direktes INSERT)
- duration_minutes auf 1–240 gecappt
- xcodebuild test ActivityServiceTests gruen
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/04-activity-logging-dashboard/04-02-SUMMARY.md` mit:
- xcodebuild test Ergebnis (Anzahl Tests, Failures)
- Bestaetigung: create_activity_for_child via RPC (kein direktes INSERT)
- Bestaetigung: duration_minutes-Cap implementiert
- currentUserId/currentFamilyId Loesung dokumentiert
</output>
