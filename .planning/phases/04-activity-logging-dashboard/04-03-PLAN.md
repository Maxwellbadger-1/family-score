---
phase: 04-activity-logging-dashboard
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift
  - FamilyScore/FamilyScore/Views/Dashboard/RingClusterView.swift
  - FamilyScore/FamilyScore/Views/Dashboard/SingleRingView.swift
  - FamilyScore/FamilyScore/Views/Dashboard/WeekSummaryView.swift
  - FamilyScore/FamilyScore/Views/ActivityLog/ActivityLogSheet.swift
autonomous: true
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - LOG-01
  - LOG-02
  - LOG-03
  - LOG-06

must_haves:
  truths:
    - "DashboardView zeigt drei konzentrische Ringe (Pflicht/Freizeit/Score) in Apple-Health-Aesthetik mit korrekten Farben (systemRed/systemGreen/systemBlue)"
    - "RingClusterView zeigt im Zentrum den heutigen Tagesscore als Display-Typo (34pt semibold)"
    - "Ringe animieren mit .spring(response: 0.6, dampingFraction: 0.75) bei Score-Updates"
    - "FAB-Button oeffnet ActivityLogSheet als .sheet mit .presentationDetents([.medium])"
    - "ActivityLogSheet hat 4 Kategorie-Buttons, Wheel-Picker (5–240 min, Default 30), optionales Notiz-Feld, dynamische Punkte-Vorschau"
    - "WeekSummaryView zeigt BarMark-Chart (Swift Charts) mit Pflicht/Freizeit pro Mitglied"
    - "WeekSummaryView zeigt 'Wochensieger: [Name]' Header — Mitglied mit hoechstem total_points aus weeklyScores (DASH-03)"
    - "familyMemberRow zeigt 'Pflicht: Npt · Freizeit: Npt' Label-Zeile aus member.dutyPoints/leisurePoints (DASH-02)"
    - "Alle Accessibility-Labels gemaess UI-SPEC gesetzt"
    - "Leerstand: Ringe bei 0% sichtbar + Heading-Text"
  artifacts:
    - path: "FamilyScore/FamilyScore/Views/Dashboard/SingleRingView.swift"
      provides: "Einzelne Ring-Komponente mit Ueberlauf-Unterstuetzung"
      contains: "Circle().trim"
    - path: "FamilyScore/FamilyScore/Views/Dashboard/RingClusterView.swift"
      provides: "Drei konzentrische Ringe, outerDiameter 280pt, lineWidth 20pt, gap 8pt"
      contains: "outerDiameter"
    - path: "FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift"
      provides: "Tab-1-View mit NavigationStack, RingCluster, FAB, ActivityLogSheet-Trigger, familyMemberRow mit Pflicht/Freizeit-Labels"
      contains: "showingLogSheet"
    - path: "FamilyScore/FamilyScore/Views/Dashboard/WeekSummaryView.swift"
      provides: "Swift Charts BarMark Wochenbilanz + Wochensieger-Header (DASH-03)"
      contains: "Wochensieger"
    - path: "FamilyScore/FamilyScore/Views/ActivityLog/ActivityLogSheet.swift"
      provides: "Halbscreen-Modal fuer Activity-Logging"
      contains: "presentationDetents"
  key_links:
    - from: "FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift"
      to: "FamilyScore/FamilyScore/Services/ActivityService.swift"
      via: "@EnvironmentObject activityService"
      pattern: "@EnvironmentObject.*activityService"
    - from: "FamilyScore/FamilyScore/Views/Dashboard/RingClusterView.swift"
      to: "FamilyScore/FamilyScore/Views/Dashboard/SingleRingView.swift"
      via: "3x SingleRingView in ZStack"
      pattern: "SingleRingView"
    - from: "FamilyScore/FamilyScore/Views/ActivityLog/ActivityLogSheet.swift"
      to: "FamilyScore/FamilyScore/Services/ActivityService.swift"
      via: "@ObservedObject activityService"
      pattern: "@ObservedObject.*activityService"
---

<objective>
Wave 2: Dashboard-UI und Activity-Log-Sheet implementieren — alle visuellen Komponenten die dem User den Core-Loop praesentieren.

Purpose: Der User sieht seine Ringe, kann eine Aktivitaet in 3 Taps loggen und die Wochenbilanz lesen. Das ist der sichtbare Kern des Produkts.

Output:
- SingleRingView.swift (wiederverwendbare Ring-Komponente)
- RingClusterView.swift (drei konzentrische Ringe, Apple Fitness-Stil)
- DashboardView.swift (Tab 1: NavigationStack + RingCluster + familyMemberRow mit Pflicht/Freizeit + WeekSummaryView + FAB)
- WeekSummaryView.swift (Swift Charts BarMark Wochenbilanz + Wochensieger-Header per DASH-03)
- ActivityLogSheet.swift (Half-Screen Modal: Kategorie + Dauer + Notiz + Punkte-Vorschau)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-activity-logging-dashboard/04-UI-SPEC.md
@.planning/phases/04-activity-logging-dashboard/04-RESEARCH.md
@.planning/phases/04-activity-logging-dashboard/04-PATTERNS.md

<interfaces>
<!-- Bestehende Contracts — Executor MUSS lesen bevor er schreibt -->

Aus FamilyScore/FamilyScore/Services/ActivityServiceProtocol.swift (Wave 0):
```swift
// Views empfangen diese Properties via @EnvironmentObject activityService:
activityService.dutyProgress: Double      // 0.0–1.0+ fuer Pflicht-Ring
activityService.leisureProgress: Double   // Freizeit-Ring
activityService.scoreProgress: Double     // Score-Ring
activityService.totalScore: Int           // Punkte heute (Zentrum des Clusters)
activityService.categories: [CategoryConfig]  // 4 Kategorien fuer Sheet-Picker
activityService.weeklyScores: [WeeklySummary] // fuer WeekSummaryView
activityService.familyDayScores: [MemberDayScore] // fuer Familien-Mini-Ringe
activityService.activityError: String?
func fetchTodayData() async throws
func fetchFamilyData() async throws
func fetchWeeklyData() async throws
func logActivity(categoryId: UUID, durationMinutes: Int, title: String?) async throws

// MemberDayScore — alle drei Felder relevant fuer DASH-02:
struct MemberDayScore: Decodable, Identifiable, Sendable {
    let userId: UUID
    let displayName: String
    let avatarColor: String
    let dutyPoints: Double    // DASH-02: anzeigen
    let leisurePoints: Double // DASH-02: anzeigen
    let totalPoints: Double
    var id: UUID { userId }
}

// WeeklySummary — totalPoints fuer Wochensieger-Sortierung (DASH-03):
struct WeeklySummary: Decodable, Identifiable, Sendable {
    let userId: UUID
    let totalPoints: Double
    // weitere Felder...
}
```

Ring Layout Contract (UI-SPEC — verbindlich):
```
outerDiameter = 280pt  (Pflicht-Ring, systemRed)
gap = 8pt (sm spacing token)
lineWidth = 20pt (fix)
Freizeit-Ring: diameter = 280 - 2*(20+8) = 224pt (systemGreen)
Score-Ring:    diameter = 280 - 4*(20+8) = 168pt (systemBlue)
Track opacity: 0.15
Animation: .spring(response: 0.6, dampingFraction: 0.75)
Rotation: .rotationEffect(.degrees(-90)) → 12-Uhr-Start
Ueberlauf: zweite Runde mit color.opacity(0.6)
```

Typography (UI-SPEC — verbindlich):
```swift
.font(.system(size: 34, weight: .semibold))  // Display — Tagesscore
.font(.system(size: 22, weight: .semibold))  // Heading — Sektions-Titel
.font(.system(size: 17))                     // Body — Listen-Haupttext
.font(.system(size: 13))                     // Label — Zeitstempel, Tags
```

Aus FamilyScore/FamilyScore/Views/Auth/RegisterView.swift (Analog fuer ActivityLogSheet):
```swift
// Muster: @ObservedObject, canSubmit, async saveActivity(), ProgressView in Button
struct RegisterView: View {
    @ObservedObject var authService: AuthService
    @State private var isLoading: Bool = false
    private var canSubmit: Bool { /* checks */ }
}
```

Aus FamilyScore/FamilyScore/Views/RootView.swift (Analog fuer DashboardView NavigationStack):
```swift
// Muster: NavigationStack + .navigationTitle(.large) + .task {} zum Laden
struct RootView: View {
    @EnvironmentObject private var authService: AuthService
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SingleRingView + RingClusterView erstellen</name>
  <files>
    FamilyScore/FamilyScore/Views/Dashboard/SingleRingView.swift
    FamilyScore/FamilyScore/Views/Dashboard/RingClusterView.swift
  </files>
  <read_first>
    - .planning/phases/04-activity-logging-dashboard/04-RESEARCH.md (Pattern 1: vollstaendige SingleRingView + RingClusterView Implementierung — exakt verwenden)
    - .planning/phases/04-activity-logging-dashboard/04-PATTERNS.md (Abschnitte SingleRingView + RingClusterView — ZStack-Muster)
    - .planning/phases/04-activity-logging-dashboard/04-UI-SPEC.md (Ring Layout Contract + Accessibility Contract)
    - FamilyScore/FamilyScore/Services/ActivityServiceProtocol.swift (um RingType zu importieren)
  </read_first>
  <action>
Erstelle zwei Dateien. Verwende exakt die Implementierungen aus RESEARCH.md Pattern 1 und PATTERNS.md. Keine Abweichungen von den Ring-Groessen oder Farben.

**FamilyScore/FamilyScore/Views/Dashboard/SingleRingView.swift:**
```swift
// FamilyScore/Views/Dashboard/SingleRingView.swift
// Target Membership: FamilyScore (App) ONLY
// Kein Canvas, kein Custom Shape — Circle().trim() ist idiomatisch (RESEARCH.md Pattern 1)
// iOS 16 kompatibel — kein API nach iOS 16

import SwiftUI

struct SingleRingView: View {
    let progress: Double     // 0.0–1.0+ (Ueberlauf erlaubt, UI-SPEC)
    let color: Color
    let lineWidth: CGFloat = 20   // UI-SPEC: fix 20pt — hardware-independent

    var body: some View {
        ZStack {
            // Track (Hintergrund-Arc) — 15% Opacity (UI-SPEC)
            Circle()
                .stroke(color.opacity(0.15),
                        style: StrokeStyle(lineWidth: lineWidth, lineCap: .round))

            // Progress-Arc (erste Runde)
            Circle()
                .trim(from: 0, to: min(progress, 1.0))
                .stroke(color, style: StrokeStyle(lineWidth: lineWidth, lineCap: .round))
                .rotationEffect(.degrees(-90))   // 12-Uhr-Start (UI-SPEC)

            // Zweite Runde bei Ueberlauf (UI-SPEC: "slight opacity reduction on second lap")
            if progress > 1.0 {
                Circle()
                    .trim(from: 0, to: progress - 1.0)
                    .stroke(color.opacity(0.6),
                            style: StrokeStyle(lineWidth: lineWidth, lineCap: .round))
                    .rotationEffect(.degrees(-90))
            }
        }
        // Accessibility: Label wird von RingClusterView gesetzt — diese View versteckt sich
        .accessibilityHidden(true)
    }
}

#Preview {
    VStack(spacing: 16) {
        SingleRingView(progress: 0.0, color: Color(UIColor.systemRed))
            .frame(width: 100, height: 100)
        SingleRingView(progress: 0.75, color: Color(UIColor.systemGreen))
            .frame(width: 100, height: 100)
        SingleRingView(progress: 1.5, color: Color(UIColor.systemBlue))
            .frame(width: 100, height: 100)
    }
    .padding()
}
```

**FamilyScore/FamilyScore/Views/Dashboard/RingClusterView.swift:**
```swift
// FamilyScore/Views/Dashboard/RingClusterView.swift
// Target Membership: FamilyScore (App) ONLY
// Drei konzentrische Ringe — Apple Activity/Fitness Stil

import SwiftUI

struct RingClusterView: View {
    let dutyProgress: Double      // Pflicht-Ring (systemRed, aussen)
    let leisureProgress: Double   // Freizeit-Ring (systemGreen, mitte)
    let scoreProgress: Double     // Score-Ring (systemBlue, innen)
    let totalScore: Int

    // Ring-Groessen per UI-SPEC Ring Layout Contract
    // outerDiameter=280, lineWidth=20, gap=8 (sm spacing token)
    private let outerDiameter: CGFloat = 280
    private let gap: CGFloat = 8
    private let lineWidth: CGFloat = 20

    var body: some View {
        ZStack {
            // Pflicht-Ring (aussen, systemRed)
            SingleRingView(progress: dutyProgress, color: Color(UIColor.systemRed))
                .frame(width: outerDiameter, height: outerDiameter)
                .accessibilityLabel("Pflicht-Ring: \(Int(dutyProgress * 60)) von 60 Punkten (\(Int(dutyProgress * 100))%)")

            // Freizeit-Ring (mitte, systemGreen)
            // Diameter = 280 - 2*(20+8) = 224pt
            SingleRingView(progress: leisureProgress, color: Color(UIColor.systemGreen))
                .frame(width: outerDiameter - 2 * (lineWidth + gap),
                       height: outerDiameter - 2 * (lineWidth + gap))
                .accessibilityLabel("Freizeit-Ring: \(Int(leisureProgress * 60)) von 60 Punkten (\(Int(leisureProgress * 100))%)")

            // Score-Ring (innen, systemBlue)
            // Diameter = 280 - 4*(20+8) = 168pt
            SingleRingView(progress: scoreProgress, color: Color(UIColor.systemBlue))
                .frame(width: outerDiameter - 4 * (lineWidth + gap),
                       height: outerDiameter - 4 * (lineWidth + gap))
                .accessibilityLabel("Score-Ring: \(Int(scoreProgress * 60)) von 60 Punkten (\(Int(scoreProgress * 100))%)")

            // Zentrum: Tagesscore (Display + Label Typography — UI-SPEC)
            VStack(spacing: 2) {
                Text("\(totalScore)")
                    .font(.system(size: 34, weight: .semibold))  // Display
                    .monospacedDigit()
                Text("Punkte heute")
                    .font(.system(size: 13))                      // Label
                    .foregroundStyle(.secondary)
            }
            .accessibilityElement(children: .ignore)
            .accessibilityLabel("\(totalScore) Punkte heute")
        }
        // Spring-Animation fuer Apple-Fitness-Feeling (UI-SPEC Interaction Contract)
        .animation(.spring(response: 0.6, dampingFraction: 0.75), value: dutyProgress)
        .animation(.spring(response: 0.6, dampingFraction: 0.75), value: leisureProgress)
        .animation(.spring(response: 0.6, dampingFraction: 0.75), value: scoreProgress)
    }
}

#Preview {
    RingClusterView(dutyProgress: 0.4, leisureProgress: 0.6, scoreProgress: 1.2, totalScore: 72)
        .padding()
}
```
  </action>
  <verify>
    <automated>xcodebuild test -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:FamilyScoreTests/RingProgressTests 2>&1 | grep -E "Test.*passed|TEST SUCCEEDED|TEST FAILED" | tail -5</automated>
  </verify>
  <done>SingleRingView und RingClusterView kompilieren. Ringe haben korrekte Groessen (280/224/168pt), Farben (Red/Green/Blue), Animationen und Accessibility-Labels. RingProgressTests gruen (Wave 0 deckt Ring-Progress-Berechnung ab — Nyquist-kontinuierlich).</done>
</task>

<task type="auto">
  <name>Task 2: DashboardView + WeekSummaryView mit Wochensieger erstellen</name>
  <files>
    FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift
    FamilyScore/FamilyScore/Views/Dashboard/WeekSummaryView.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/Views/RootView.swift (Analog fuer DashboardView-Struktur: NavigationStack, @EnvironmentObject, .task)
    - .planning/phases/04-activity-logging-dashboard/04-PATTERNS.md (DashboardView, WeekSummaryView Abschnitte — vollstaendige Code-Snippets)
    - .planning/phases/04-activity-logging-dashboard/04-UI-SPEC.md (Screen 1, Screen 2, Copywriting Contract, Typography, Spacing, Accessibility Contract)
    - .planning/phases/04-activity-logging-dashboard/04-RESEARCH.md (Pattern 7: WeekSummaryView Swift Charts)
    - FamilyScore/FamilyScore/Services/ActivityServiceProtocol.swift (MemberDayScore.dutyPoints/leisurePoints fuer DASH-02, WeeklySummary.totalPoints fuer DASH-03)
  </read_first>
  <action>
Erstelle zwei Dateien. UI-SPEC ist verbindlich. Zwei kritische Anforderungen gegenueber dem Basis-Entwurf:

**DASH-02 (familyMemberRow):** Zeige `dutyPoints` und `leisurePoints` aus MemberDayScore als zweite Zeile unterhalb des Namens. Format: "Pflicht: Npt · Freizeit: Npt" in Label-Typo (13pt, .secondary).

**DASH-03 (WeekSummaryView):** Sortiere `weeklyScores` nach `totalPoints` absteigend. Zeige "Wochensieger: [Name]" als Heading-Typo ueber dem BarChart. Den displayName des Leaders via `familyDayScores.first(where: { $0.userId == leader.userId })?.displayName` auflosen.

**FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift:**

Layout (Screen 1 aus UI-SPEC):
- NavigationStack + .navigationTitle("Uebersicht") + .navigationBarTitleDisplayMode(.large)
- Datum-Header unterhalb Nav: aktuelles Datum in "Montag, 16. Mai"-Format, Label-Typo (13pt), systemSecondaryLabel
- RingClusterView zentriert (dutyProgress/leisureProgress/scoreProgress/totalScore von activityService)
- Leerstand: wenn todayEntries.isEmpty → Heading "Noch keine Aktivitaeten heute" + Body-Text (UI-SPEC Copywriting)
- Ring-Labels unterhalb Cluster: horizontaler HStack — "Pflicht Npt · Freizeit Npt · Score Npt" in Label-Typo
- Divider + "Familie heute" Heading
- Familien-Mini-Ringe: ForEach ueber familyDayScores, je eine Zeile mit kleinem Score-Ring (36pt) + displayName + "Pflicht: Npt · Freizeit: Npt" (Label-Typo, .secondary) — DASH-02
- WeekSummaryView am Ende
- FAB: .overlay(alignment: .bottomTrailing) — Button mit "+" SF Symbol, systemBlue, 56pt diameter, 24pt margin, accessibilityLabel "Aktivitaet erfassen"
- .sheet(isPresented: $showingLogSheet) { ActivityLogSheet(...).presentationDetents([.medium]).presentationDragIndicator(.visible) }
- .task { await loadDashboard() } — laedt fetchTodayData() + fetchFamilyData() + fetchWeeklyData()
- .alert fuer activityError (dismiss-Button "Verstanden", UI-SPEC Error State)

```swift
// FamilyScore/Views/Dashboard/DashboardView.swift
import SwiftUI

struct DashboardView: View {
    @EnvironmentObject private var activityService: ActivityService
    @State private var showingLogSheet: Bool = false

    var body: some View {
        NavigationStack {
            ScrollView {
                VStack(spacing: 0) {
                    // Datum-Header
                    HStack {
                        Text(Date(), format: .dateTime.weekday(.wide).day().month(.wide))
                            .font(.system(size: 13))
                            .foregroundStyle(Color(UIColor.secondaryLabel))
                        Spacer()
                    }
                    .padding(.horizontal, 16)
                    .padding(.top, 8)

                    // Ring-Cluster
                    RingClusterView(
                        dutyProgress: activityService.dutyProgress,
                        leisureProgress: activityService.leisureProgress,
                        scoreProgress: activityService.scoreProgress,
                        totalScore: activityService.totalScore
                    )
                    .padding(.vertical, 24)

                    // Leerstand
                    if activityService.todayEntries.isEmpty {
                        VStack(spacing: 16) {
                            Text("Noch keine Aktivitaeten heute")
                                .font(.system(size: 22, weight: .semibold))
                            Text("Tippe auf \u{201E}+\u{201C} um deinen ersten Eintrag zu starten.")
                                .font(.system(size: 17))
                                .foregroundStyle(.secondary)
                                .multilineTextAlignment(.center)
                        }
                        .padding(.horizontal, 32)
                        .padding(.bottom, 24)
                    }

                    // Ring-Labels
                    HStack(spacing: 16) {
                        ringLabel("Pflicht", color: Color(UIColor.systemRed),
                                  value: Int(activityService.dutyProgress * 60))
                        ringLabel("Freizeit", color: Color(UIColor.systemGreen),
                                  value: Int(activityService.leisureProgress * 60))
                        ringLabel("Score", color: Color(UIColor.systemBlue),
                                  value: activityService.totalScore)
                    }
                    .padding(.horizontal, 16)
                    .padding(.bottom, 24)

                    Divider().padding(.horizontal, 16)

                    // Familien-Uebersicht
                    if !activityService.familyDayScores.isEmpty {
                        VStack(alignment: .leading, spacing: 0) {
                            Text("Familie heute")
                                .font(.system(size: 22, weight: .semibold))
                                .padding(.horizontal, 16)
                                .padding(.vertical, 16)
                            ForEach(activityService.familyDayScores) { member in
                                familyMemberRow(member)
                            }
                        }
                        Divider().padding(.horizontal, 16)
                    }

                    // Wochenbilanz
                    WeekSummaryView(
                        weekScores: weekScoresForChart,
                        weeklyLeaderName: weeklyLeaderName
                    )
                    .padding(.bottom, 80) // Platz fuer FAB
                }
            }
            .navigationTitle("Uebersicht")
            .navigationBarTitleDisplayMode(.large)
            .task { await loadDashboard() }
            .alert("Fehler", isPresented: Binding(
                get: { activityService.activityError != nil },
                set: { if !$0 { activityService.activityError = nil } }
            )) {
                Button("Verstanden") { activityService.activityError = nil }
            } message: {
                Text(activityService.activityError ?? "")
            }
        }
        .overlay(alignment: .bottomTrailing) {
            Button {
                showingLogSheet = true
            } label: {
                Image(systemName: "plus")
                    .font(.system(size: 24, weight: .semibold))
                    .foregroundStyle(.white)
                    .frame(width: 56, height: 56)
                    .background(Color(UIColor.systemBlue))
                    .clipShape(Circle())
            }
            .accessibilityLabel("Aktivitaet erfassen")
            .padding(.trailing, 24)
            .padding(.bottom, 24)
        }
        .sheet(isPresented: $showingLogSheet) {
            ActivityLogSheet(activityService: activityService)
                .presentationDetents([.medium])
                .presentationDragIndicator(.visible)
        }
    }

    // MARK: - Helper Views

    @ViewBuilder
    private func ringLabel(_ name: String, color: Color, value: Int) -> some View {
        VStack(spacing: 2) {
            Circle()
                .stroke(color, lineWidth: 3)
                .frame(width: 10, height: 10)
            Text("\(name)")
                .font(.system(size: 13))
                .foregroundStyle(.secondary)
            Text("\(value) pt")
                .font(.system(size: 13))
        }
        .frame(maxWidth: .infinity)
    }

    @ViewBuilder
    private func familyMemberRow(_ member: MemberDayScore) -> some View {
        HStack(spacing: 12) {
            // Mini Score-Ring (36pt)
            let progress = member.totalPoints / 60.0
            SingleRingView(progress: progress, color: Color(UIColor.systemBlue))
                .frame(width: 36, height: 36)
                .accessibilityHidden(true)
            VStack(alignment: .leading, spacing: 2) {
                Text(member.displayName)
                    .font(.system(size: 17))
                // DASH-02: Pflicht- und Freizeit-Punkte anzeigen
                Text("Pflicht: \(Int(member.dutyPoints))pt · Freizeit: \(Int(member.leisurePoints))pt")
                    .font(.system(size: 13))
                    .foregroundStyle(.secondary)
            }
            Spacer()
            Text("\(Int(member.totalPoints)) pt")
                .font(.system(size: 17))
                .foregroundStyle(.secondary)
        }
        .padding(.horizontal, 16)
        .padding(.vertical, 8)
    }

    // MARK: - Data

    private var weekScoresForChart: [WeekMemberScore] {
        // weekly_summaries → WeekMemberScore fuer Swift Charts
        var result: [WeekMemberScore] = []
        for summary in activityService.weeklyScores {
            let breakdown = summary.byCategory ?? [:]
            let dutyMin = (breakdown["Haushalt"]?.minutes ?? 0)
                + (breakdown["Besorgungen"]?.minutes ?? 0)
                + (breakdown["Arbeit/Schule"]?.minutes ?? 0)
            let leisureMin = breakdown["Hobby/Freizeit"]?.minutes ?? 0
            let name = activityService.familyDayScores.first(where: { $0.userId == summary.userId })?.displayName ?? "Mitglied"
            if dutyMin > 0 { result.append(WeekMemberScore(memberName: name, category: "Pflicht", minutes: dutyMin)) }
            if leisureMin > 0 { result.append(WeekMemberScore(memberName: name, category: "Freizeit", minutes: leisureMin)) }
        }
        return result
    }

    // DASH-03: Wochensieger — Mitglied mit hoechstem totalPoints in weeklyScores
    private var weeklyLeaderName: String? {
        guard let leader = activityService.weeklyScores.max(by: { $0.totalPoints < $1.totalPoints }),
              leader.totalPoints > 0 else { return nil }
        return activityService.familyDayScores.first(where: { $0.userId == leader.userId })?.displayName
    }

    private func loadDashboard() async {
        do {
            try await activityService.fetchTodayData()
            try await activityService.fetchFamilyData()
            try await activityService.fetchWeeklyData()
        } catch {
            // Fehler bereits in activityService.activityError gesetzt
        }
    }
}

#Preview {
    DashboardView()
        .environmentObject(ActivityService())
}
```

**FamilyScore/FamilyScore/Views/Dashboard/WeekSummaryView.swift:**

DASH-03: Empfaengt `weeklyLeaderName: String?` vom DashboardView (berechnet via `weeklyScores.max(by: { $0.totalPoints < $1.totalPoints })`). Zeigt "Wochensieger: [Name]" als Heading-Text ueber dem BarChart wenn `weeklyLeaderName != nil`.

```swift
// FamilyScore/Views/Dashboard/WeekSummaryView.swift
// Target Membership: FamilyScore (App) ONLY
// Swift Charts — iOS 16 nativ (kein Third-Party)
// DASH-03: weeklyLeaderName fuer Wochensieger-Heading

import SwiftUI
import Charts

struct WeekSummaryView: View {
    let weekScores: [WeekMemberScore]
    let weeklyLeaderName: String?  // DASH-03: nil wenn noch keine Daten

    var body: some View {
        VStack(alignment: .leading, spacing: 16) {
            Text("Diese Woche")
                .font(.system(size: 22, weight: .semibold))
                .padding(.horizontal, 16)

            // DASH-03: Wochensieger-Label (ROADMAP SC-6)
            if let leader = weeklyLeaderName {
                Text("Wochensieger: \(leader)")
                    .font(.system(size: 17, weight: .semibold))
                    .foregroundStyle(Color(UIColor.systemBlue))
                    .padding(.horizontal, 16)
            }

            if weekScores.isEmpty {
                Text("Noch keine Wochendaten")
                    .font(.system(size: 17))
                    .foregroundStyle(.secondary)
                    .padding(.horizontal, 16)
            } else {
                Chart(weekScores) { score in
                    BarMark(
                        x: .value("Mitglied", score.memberName),
                        y: .value("Minuten", score.minutes)
                    )
                    .foregroundStyle(by: .value("Kategorie", score.category))
                    .position(by: .value("Kategorie", score.category), axis: .horizontal)
                }
                .chartForegroundStyleScale([
                    "Pflicht": Color(UIColor.systemRed),
                    "Freizeit": Color(UIColor.systemGreen)
                ])
                .frame(height: 200)
                .padding(.horizontal, 16)
            }
        }
        .padding(.vertical, 16)
    }
}

#Preview {
    WeekSummaryView(
        weekScores: [
            WeekMemberScore(memberName: "Max", category: "Pflicht", minutes: 120),
            WeekMemberScore(memberName: "Max", category: "Freizeit", minutes: 60),
            WeekMemberScore(memberName: "Anna", category: "Pflicht", minutes: 90),
        ],
        weeklyLeaderName: "Max"
    )
}
```
  </action>
  <verify>
    <automated>xcodebuild build -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "BarMark" FamilyScore/FamilyScore/Views/Dashboard/WeekSummaryView.swift` gibt Treffer
    - `grep "import Charts" FamilyScore/FamilyScore/Views/Dashboard/WeekSummaryView.swift` gibt Treffer
    - `grep "Wochensieger" FamilyScore/FamilyScore/Views/Dashboard/WeekSummaryView.swift` gibt Treffer (DASH-03)
    - `grep "weeklyLeaderName" FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift` gibt Treffer (Leader-Berechnung)
    - `grep "dutyPoints\|leisurePoints\|Pflicht.*pt.*Freizeit" FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift` gibt Treffer (DASH-02)
    - `grep "showingLogSheet" FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift` gibt Treffer
    - `grep "Noch keine Aktivitaeten heute" FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift` gibt Treffer (UI-SPEC Copywriting)
    - `grep "accessibilityLabel.*Aktivitaet erfassen" FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift` gibt Treffer (FAB Accessibility)
    - xcodebuild ergibt "BUILD SUCCEEDED"
  </acceptance_criteria>
  <done>DashboardView und WeekSummaryView kompilieren. WeekSummaryView zeigt 'Wochensieger: [Name]' Header (DASH-03). familyMemberRow zeigt 'Pflicht: Npt · Freizeit: Npt' (DASH-02). FAB oeffnet Sheet.</done>
</task>

<task type="auto">
  <name>Task 3: ActivityLogSheet erstellen</name>
  <files>
    FamilyScore/FamilyScore/Views/ActivityLog/ActivityLogSheet.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/Views/Auth/RegisterView.swift (Analog fuer ActivityLogSheet: @ObservedObject, canSubmit, async save, ProgressView)
    - .planning/phases/04-activity-logging-dashboard/04-PATTERNS.md (ActivityLogSheet Abschnitt — vollstaendiges Code-Snippet)
    - .planning/phases/04-activity-logging-dashboard/04-UI-SPEC.md (Screen 2, Copywriting Contract, Typography, Spacing)
    - .planning/phases/04-activity-logging-dashboard/04-RESEARCH.md (Pattern 8: Half-Sheet)
    - FamilyScore/FamilyScore/Services/ActivityServiceProtocol.swift (logActivity() Signatur)
  </read_first>
  <action>
Erstelle eine Datei. UI-SPEC Screen 2 ist verbindlich.

**FamilyScore/FamilyScore/Views/ActivityLog/ActivityLogSheet.swift:**

Layout (Screen 2 aus UI-SPEC):
- Sheet-Titel "Aktivitaet erfassen" in Heading-Typo (navigationBarTitleDisplayMode: .inline)
- 4 Kategorie-Buttons (segmentiert oder HStack) aus activityService.categories
- Wheel-Picker 5–240 min, 5-min-Schritte, Default 30 min
- Notiz-TextField (optional) mit Placeholder "Kurze Beschreibung (optional)"
- Dynamische Punkte-Vorschau: "= N Punkte" in Heading-Typo
- Speichern-Button: full-width, systemBlue, 44pt, Semibold, ProgressView bei isLoading
- Abbrechen: top-left TextButton systemBlue
- @ObservedObject (NICHT @EnvironmentObject — Sheet-Scope) pro PATTERNS.md

```swift
// FamilyScore/Views/ActivityLog/ActivityLogSheet.swift
// Target Membership: FamilyScore (App) ONLY
// .sheet(.presentationDetents([.medium])) — iOS 16 nativ (RESEARCH.md Pattern 8)

import SwiftUI

struct ActivityLogSheet: View {
    @ObservedObject var activityService: ActivityService
    @Environment(\.dismiss) private var dismiss

    @State private var selectedCategoryId: UUID? = nil
    @State private var selectedDuration: Int = 30    // Default 30 min (UI-SPEC)
    @State private var noteText: String = ""
    @State private var isLoading: Bool = false

    private var canSubmit: Bool { selectedCategoryId != nil && !isLoading }

    private var calculatedPoints: Int {
        let weight = activityService.categories
            .first(where: { $0.id == selectedCategoryId })?.pointWeight ?? 1.0
        return Int(Double(selectedDuration) * weight)
    }

    var body: some View {
        NavigationStack {
            Form {
                // Kategorie-Auswahl
                Section("Kategorie") {
                    ForEach(activityService.categories) { category in
                        Button {
                            selectedCategoryId = category.id
                        } label: {
                            HStack {
                                Image(systemName: category.sfSymbol)
                                    .foregroundStyle(category.ringType == .duty
                                                     ? Color(UIColor.systemRed)
                                                     : Color(UIColor.systemGreen))
                                Text(category.name)
                                    .font(.system(size: 17))
                                    .foregroundStyle(Color.primary)
                                Spacer()
                                if selectedCategoryId == category.id {
                                    Image(systemName: "checkmark")
                                        .foregroundStyle(Color(UIColor.systemBlue))
                                }
                            }
                        }
                        .frame(minHeight: 44)   // iOS HIG Touch Target
                    }
                }

                // Dauer-Picker (UI-SPEC: Wheel, 5–240 min, 5-min-Schritte)
                Section("Dauer") {
                    Picker("Dauer", selection: $selectedDuration) {
                        ForEach(Array(stride(from: 5, through: 240, by: 5)), id: \.self) { minutes in
                            Text("\(minutes) min").tag(minutes)
                        }
                    }
                    .pickerStyle(.wheel)
                    .frame(height: 120)

                    // Dynamische Punkte-Vorschau (UI-SPEC)
                    HStack {
                        Spacer()
                        Text("= \(calculatedPoints) Punkte")
                            .font(.system(size: 22, weight: .semibold))  // Heading
                            .foregroundStyle(Color(UIColor.systemBlue))
                    }
                }

                // Notiz-Feld (optional, UI-SPEC Screen 2)
                Section("Notiz (optional)") {
                    TextField("Kurze Beschreibung (optional)", text: $noteText)
                        .font(.system(size: 17))
                }
            }
            .navigationTitle("Aktivitaet erfassen")
            .navigationBarTitleDisplayMode(.inline)
            .toolbar {
                ToolbarItem(placement: .cancellationAction) {
                    Button("Abbrechen") { dismiss() }
                        .foregroundStyle(Color(UIColor.systemBlue))
                }
            }
            .safeAreaInset(edge: .bottom) {
                Button {
                    Task { await saveActivity() }
                } label: {
                    Group {
                        if isLoading {
                            ProgressView().tint(.white)
                        } else {
                            Text("Speichern").font(.headline)
                        }
                    }
                    .frame(maxWidth: .infinity)
                    .frame(height: 44)
                    .background(canSubmit
                                ? Color(UIColor.systemBlue)
                                : Color(UIColor.systemBlue).opacity(0.3))
                    .foregroundStyle(.white)
                    .cornerRadius(12)
                }
                .disabled(!canSubmit)
                .padding(16)
            }
        }
    }

    // MARK: - Actions

    private func saveActivity() async {
        guard canSubmit, let categoryId = selectedCategoryId else { return }
        isLoading = true
        defer { isLoading = false }
        do {
            let note = noteText.trimmingCharacters(in: .whitespaces)
            try await activityService.logActivity(
                categoryId: categoryId,
                durationMinutes: selectedDuration,
                title: note.isEmpty ? nil : note
            )
            dismiss()
        } catch {
            activityService.activityError = activityService.localizedError(from: error)
        }
    }
}

#Preview {
    ActivityLogSheet(activityService: ActivityService())
        .presentationDetents([.medium])
}
```
  </action>
  <verify>
    <automated>xcodebuild build -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@ObservedObject var activityService" FamilyScore/FamilyScore/Views/ActivityLog/ActivityLogSheet.swift` gibt Treffer (kein @EnvironmentObject in Sheet)
    - `grep "pickerStyle.*wheel\|pickerStyle(.wheel)" FamilyScore/FamilyScore/Views/ActivityLog/ActivityLogSheet.swift` gibt Treffer
    - `grep "calculatedPoints\|Punkte" FamilyScore/FamilyScore/Views/ActivityLog/ActivityLogSheet.swift` gibt Treffer (Punkte-Vorschau)
    - `grep "presentationDetents.*medium" FamilyScore/FamilyScore/Views/ActivityLog/ActivityLogSheet.swift` gibt Treffer ODER im Caller (DashboardView)
    - xcodebuild ergibt "BUILD SUCCEEDED"
  </acceptance_criteria>
  <done>ActivityLogSheet kompiliert. Kategorie-Selektion, Wheel-Picker, Punkte-Vorschau, async Save mit ProgressView. @ObservedObject (nicht @EnvironmentObject).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| SwiftUI-View → ActivityService | Views empfangen nur Published-Properties; kein direkter DB-Zugriff aus Views |
| ActivityLogSheet → logActivity() | User-Input (Dauer, Kategorie) landet als Methodenparameter im Service |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|------------|-------------|-----------------|
| T-4-01 | Tampering | ActivityLogSheet — user_id immer currentUser | accept | logActivity() entnimmt user_id aus supabase.auth.currentUser — kein UI-Parameter; View kann user_id nicht beeinflussen |
| T-4-03 | Tampering | calculatedPoints in ActivityLogSheet | accept | Nur Vorschau; authoritative Berechnung im ActivityService (pointWeight aus DB); nach INSERT: get_today_score RPC |
| T-4-05 | DoS | selectedDuration 5–240 in UI | mitigate | Picker-Range 5–240 in View; ActivityService cappt zusaetzlich mit min(1, max(240, ...)) |
</threat_model>

<verification>
Nach Abschluss aller drei Tasks:

1. `xcodebuild build -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16'` — BUILD SUCCEEDED
2. `xcodebuild test -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16' -only-testing:FamilyScoreTests/RingProgressTests` — TEST SUCCEEDED (Nyquist: Wave-0-RingProgressTests decken Ring-Progress-Berechnung ab)
3. `grep "spring(response: 0.6" FamilyScore/FamilyScore/Views/Dashboard/RingClusterView.swift | wc -l` gibt 3
4. `grep "Wochensieger" FamilyScore/FamilyScore/Views/Dashboard/WeekSummaryView.swift` gibt Treffer (DASH-03)
5. `grep "dutyPoints\|Pflicht.*pt" FamilyScore/FamilyScore/Views/Dashboard/DashboardView.swift` gibt Treffer (DASH-02)
6. `grep "import Charts" FamilyScore/FamilyScore/Views/Dashboard/WeekSummaryView.swift` gibt Treffer
</verification>

<success_criteria>
- SingleRingView und RingClusterView implementieren Ring Layout Contract exakt (280/224/168pt, 15% track, Spring-Animation)
- DashboardView laedt Daten via .task, zeigt Leerstand korrekt, oeffnet Sheet via FAB
- DashboardView.familyMemberRow zeigt "Pflicht: Npt · Freizeit: Npt" (DASH-02)
- WeekSummaryView zeigt "Wochensieger: [Name]" Header wenn weeklyLeaderName != nil (DASH-03)
- ActivityLogSheet hat Kategorie-Selektion, Wheel-Picker, Punkte-Vorschau, async Save mit ProgressView
- WeekSummaryView zeigt Swift Charts BarMark (iOS 16 nativ, kein Third-Party)
- RingProgressTests (Wave 0) gruen — Nyquist-Kontinuitaet gewahrt
- BUILD SUCCEEDED
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/04-activity-logging-dashboard/04-03-SUMMARY.md` mit:
- xcodebuild build Ergebnis
- Bestaetigung Ring Layout Contract umgesetzt (Groessen, Farben, Animation)
- Bestaetigung WeekSummaryView mit Wochensieger-Header (DASH-03)
- Bestaetigung familyMemberRow mit Pflicht/Freizeit-Labels (DASH-02)
- Bestaetigung WeekSummaryView mit Swift Charts
</output>
