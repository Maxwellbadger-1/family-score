---
phase: 04-activity-logging-dashboard
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift
  - FamilyScore/FamilyScore/Views/ActivityLog/ActivityRowView.swift
  - FamilyScore/FamilyScore/FamilyScoreApp.swift
  - FamilyScore/FamilyScore/Views/RootView.swift
autonomous: false
requirements:
  - DASH-04
  - DASH-05
  - LOG-05
  - SCORE-01
  - SCORE-02
  - SCORE-03

must_haves:
  truths:
    - "ActivityListView zeigt ALLE Familien-Eintraege von heute chronologisch nach Tag gruppiert (Section-Header) — family-weiter Feed (DASH-05)"
    - "ActivityListView zeigt 'Alle Zeit'-Section mit Gesamt-Stunden und Gesamt-Punkten der Familie (DASH-04)"
    - "Swipe-left auf Zeile zeigt Loeschen-Button; .confirmationDialog erscheint vor Ausfuehrung"
    - "Eigene Eintraege koennen geloescht werden; fremde Eintraege sind swipe-geschuetzt (RLS entscheidet auf DB-Seite)"
    - "TabView hat zwei Tabs: Uebersicht (DashboardView) und Verlauf (ActivityListView)"
    - "ActivityService wird als @StateObject in FamilyScoreApp erstellt und als .environmentObject injiziert"
    - "activityService.currentFamilyId wird nach FamilyService-Load explizit gesetzt (z.B. via RootView.onAppear oder nach family-Load in authService)"
    - "xcodebuild test mit allen ActivityServiceTests + RingProgressTests gruen"
    - "Geraete-Checkpoint besteht: 6 Success Criteria aus ROADMAP.md manuell verifiziert auf echtem iOS-Geraet"
  artifacts:
    - path: "FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift"
      provides: "Tab-2-View: family-weiter Familien-Feed + 'Alle Zeit'-Section (DASH-04) + Swipe-to-Delete + confirmationDialog"
      contains: "swipeActions"
    - path: "FamilyScore/FamilyScore/Views/ActivityLog/ActivityRowView.swift"
      provides: "List-Zeile: SF Symbol + Kategoriename + Dauer + Punkte + Timestamp"
      contains: "ActivityRowView"
    - path: "FamilyScore/FamilyScore/FamilyScoreApp.swift"
      provides: "@StateObject activityService + .environmentObject Injection + TabView"
      contains: "activityService"
    - path: "FamilyScore/FamilyScore/Views/RootView.swift"
      provides: "RootView mit TabView (DashboardView + ActivityListView) + currentFamilyId-Injection nach Family-Load"
      contains: "TabView"
  key_links:
    - from: "FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift"
      to: "FamilyScore/FamilyScore/Services/ActivityService.swift"
      via: "@EnvironmentObject activityService — todayEntries (family-weiter Feed)"
      pattern: "@EnvironmentObject.*activityService"
    - from: "FamilyScore/FamilyScore/FamilyScoreApp.swift"
      to: "FamilyScore/FamilyScore/Views/RootView.swift"
      via: "@StateObject activityService → .environmentObject"
      pattern: "@StateObject.*activityService"
    - from: "FamilyScore/FamilyScore/Views/RootView.swift"
      to: "FamilyScore/FamilyScore/Services/ActivityService.swift"
      via: "activityService.currentFamilyId = familyService.currentFamily?.id nach authenticated-State"
      pattern: "currentFamilyId"
    - from: "FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift"
      to: "FamilyScore/FamilyScore/Services/ActivityService.swift"
      via: "activityService.allTimeStats fuer 'Alle Zeit'-Section (DASH-04)"
      pattern: "allTimeStats"
---

<objective>
Wave 3: Aktivitaets-Verlauf-View + App-Verdrahtung + currentFamilyId-Injection + vollstaendige Test-Suite + Geraete-Checkpoint.

Purpose: Letzte fehlende View (Verlauf-Tab) einbauen, alle Komponenten verdrahten, currentFamilyId korrekt injizieren damit alle DB-Calls funktionieren, Test-Suite gruen machen und die 6 ROADMAP-Erfolgskriterien auf echtem Geraet verifizieren.

Output:
- ActivityListView.swift (Tab 2: family-weiter Feed + 'Alle Zeit'-Section + Swipe-to-Delete)
- ActivityRowView.swift (wiederverwendbare List-Zeile)
- FamilyScoreApp.swift aktualisiert (@StateObject activityService + Injection)
- RootView.swift aktualisiert (TabView + currentFamilyId-Injection nach Family-Load)
- xcodebuild test gruen
- Geraete-Checkpoint (Checkpoint-Task)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-activity-logging-dashboard/04-UI-SPEC.md
@.planning/phases/04-activity-logging-dashboard/04-PATTERNS.md

<interfaces>
<!-- Bestehende Contracts — Executor MUSS lesen bevor er schreibt -->

Aus FamilyScore/FamilyScore/FamilyScoreApp.swift (aktuelle Datei — VOR Aenderung lesen):
```swift
@main
struct FamilyScoreApp: App {
    @StateObject private var authService = AuthService()
    var body: some Scene {
        WindowGroup {
            RootView()
                .environmentObject(authService)
                .task { await authService.startObserving() }
        }
    }
}
```
Wave 3 ergaenzt: @StateObject activityService = ActivityService(), .environmentObject(activityService)

Aus FamilyScore/FamilyScore/Views/RootView.swift (aktuelle Datei — VOR Aenderung lesen):
```swift
// Aktuelle RootView zeigt OnboardingPlaceholder oder AuthFlowView je nach AppState
// Wave 3 ersetzt authenticated-Zweig durch TabView(DashboardView + ActivityListView)
// Wave 3 MUSS ZUSAETZLICH currentFamilyId setzen (siehe unten)
```

Aus FamilyScore/FamilyScore/Services/ActivityServiceProtocol.swift (Wave 0):
```swift
// currentFamilyId: UUID? — setter-Property in ActivityService
// Wird NICHT im Protocol deklariert (nur ActivityService kennt ihn)
// Muss von aussen gesetzt werden, BEVOR fetchTodayData() aufgerufen wird

var todayEntries: [ActivityEntry] { get }     // family-weiter Feed (DASH-05)
var allTimeStats: AllTimeStats? { get }        // DASH-04 Gesamt-Statistiken
func fetchAllTimeStats() async throws          // DASH-04

// AllTimeStats:
struct AllTimeStats: Sendable {
    let totalHours: Double
    let totalPoints: Double
}
```

ActivityEntry fuer List-Darstellung:
```swift
struct ActivityEntry: Codable, Identifiable, Sendable {
    let id: UUID
    let familyId: UUID
    let userId: UUID       // Fuer "eigener Eintrag"-Logik in der View (optional)
    let categoryId: UUID
    let durationMinutes: Int
    let points: Double
    let title: String?
    let loggedAt: Date
}
```

ActivityRowView aus PATTERNS.md (vollstaendiges Code-Snippet):
```swift
struct ActivityRowView: View {
    let entry: ActivityEntry
    let category: CategoryConfig?
    var body: some View {
        HStack(spacing: 12) {
            Image(systemName: category?.sfSymbol ?? "circle.fill")
                .foregroundStyle(ringColor)
                .frame(width: 20, height: 20)
            VStack(alignment: .leading, spacing: 2) {
                Text(category?.name ?? "Unbekannt").font(.system(size: 17))
                Text("\(entry.durationMinutes) min · \(Int(entry.points)) Punkte")
                    .font(.system(size: 13)).foregroundStyle(.secondary)
            }
            Spacer()
            Text(entry.loggedAt, style: .time).font(.system(size: 13))
                .foregroundStyle(Color(UIColor.tertiaryLabel))
        }
        .accessibilityElement(children: .ignore)
        .accessibilityLabel(...)
    }
}
```

FamilyService aus Phase 3 (Quelle fuer currentFamily?.id):
```swift
// FamilyScore/FamilyScore/Services/FamilyService.swift (Phase 3)
// @Published private(set) var currentFamily: Family?
// currentFamily.id = family_id die ActivityService braucht
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: ActivityListView + ActivityRowView erstellen (family-weiter Feed + 'Alle Zeit'-Section)</name>
  <files>
    FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift
    FamilyScore/FamilyScore/Views/ActivityLog/ActivityRowView.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/Views/Auth/LoginView.swift (Analog: @EnvironmentObject, @State, async-Button-Muster)
    - FamilyScore/FamilyScore/Views/RootView.swift (Analog: NavigationStack, .task, Empty-State-Muster)
    - .planning/phases/04-activity-logging-dashboard/04-PATTERNS.md (ActivityListView + ActivityRowView Abschnitte — vollstaendige Code-Snippets)
    - .planning/phases/04-activity-logging-dashboard/04-UI-SPEC.md (Screen 3: Verlauf — Layout, Swipe, Delete Confirmation, Empty State, Copywriting)
    - .planning/phases/04-activity-logging-dashboard/04-RESEARCH.md (Pattern 9: Swipe-to-Delete — .swipeActions, NICHT .onDelete; confirmationDialog)
    - FamilyScore/FamilyScore/Services/ActivityServiceProtocol.swift (ActivityEntry + CategoryConfig + AllTimeStats Modelle)
  </read_first>
  <action>
Erstelle zwei Dateien. UI-SPEC Screen 3 (Verlauf) ist verbindlich.

**DASH-05:** todayEntries enthaelt den family-weiten Feed (Inhalt wird von ActivityService.fetchTodayData() mit WHERE family_id = currentFamilyId ohne user_id-Filter befuellt). ActivityListView zeigt alle Eintraege — keine weitere Filterung noetig.

**DASH-04:** ActivityListView zeigt oben eine "Alle Zeit"-Section mit `activityService.allTimeStats`. Die Section ladet ihre Daten via `activityService.fetchAllTimeStats()` im `.task`. Wenn `allTimeStats == nil` (noch nicht geladen), zeige einen kurzen Ladeindikator. Format:
- Heading "Gesamt (alle Zeit)"
- Zwei Label-Zeilen: "N Stunden" und "N Punkte"

**FamilyScore/FamilyScore/Views/ActivityLog/ActivityRowView.swift:**
```swift
// FamilyScore/Views/ActivityLog/ActivityRowView.swift
// Target Membership: FamilyScore (App) ONLY
// HStack-Row-Muster analog zu LoginView.swift Error-Banner

import SwiftUI

struct ActivityRowView: View {
    let entry: ActivityEntry
    let category: CategoryConfig?

    var body: some View {
        HStack(spacing: 12) {
            // Leading: SF Symbol in Ring-Farbe (UI-SPEC Screen 3)
            Image(systemName: category?.sfSymbol ?? "circle.fill")
                .foregroundStyle(ringColor)
                .frame(width: 20, height: 20)

            // Primaer: Kategoriename (Body) + Dauer/Punkte (Label)
            VStack(alignment: .leading, spacing: 2) {
                Text(displayName)
                    .font(.system(size: 17))   // Body
                Text("\(entry.durationMinutes) min · \(Int(entry.points)) Punkte")
                    .font(.system(size: 13))   // Label
                    .foregroundStyle(.secondary)
            }

            Spacer()

            // Trailing: Zeitstempel (Label, tertiaryLabel)
            Text(entry.loggedAt, style: .time)
                .font(.system(size: 13))
                .foregroundStyle(Color(UIColor.tertiaryLabel))
        }
        .frame(minHeight: 44)  // iOS HIG Touch Target
        // Accessibility (UI-SPEC Accessibility Contract)
        .accessibilityElement(children: .ignore)
        .accessibilityLabel("\(displayName), \(entry.durationMinutes) Minuten, \(Int(entry.points)) Punkte, \(entry.loggedAt.formatted(date: .omitted, time: .shortened))")
    }

    private var displayName: String {
        if let title = entry.title, !title.isEmpty {
            return title
        }
        return category?.name ?? "Aktivitaet"
    }

    private var ringColor: Color {
        switch category?.ringType {
        case .duty:    return Color(UIColor.systemRed)
        case .leisure: return Color(UIColor.systemGreen)
        case .none:    return Color(UIColor.systemBlue)
        }
    }
}
```

**FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift:**

Layout (Screen 3 aus UI-SPEC + DASH-04):
- NavigationStack + .navigationTitle("Verlauf")
- OBEN: "Alle Zeit"-Section mit allTimeStats (DASH-04)
- List mit Section-Headern pro Kalendertag (z.B. "Montag, 12. Mai")
- Eintraege absteigend nach loggedAt sortiert (ALLE Familienmitglieder, DASH-05)
- .swipeActions (NICHT .onDelete — Pitfall 5 aus RESEARCH.md) — Loeschen-Button destructive
- .confirmationDialog: "Eintrag loeschen" + "Dieser Eintrag wird dauerhaft geloescht." + "Loeschen" (destructive) + "Abbrechen" (cancel)
- Leerstand: SF Symbol "tray" + Heading "Noch keine Eintraege" + Body-Text
- .task: fetchTodayData() + fetchAllTimeStats()

```swift
// FamilyScore/Views/ActivityLog/ActivityListView.swift
// Target Membership: FamilyScore (App) ONLY
// Pitfall 5: .swipeActions (NICHT .onDelete) fuer korrekte confirmationDialog-Integration
// DASH-05: todayEntries ist family-weiter Feed (kein user_id-Filter hier — ActivityService liefert das)
// DASH-04: 'Alle Zeit'-Section mit allTimeStats

import SwiftUI

struct ActivityListView: View {
    @EnvironmentObject private var activityService: ActivityService

    @State private var entryToDelete: ActivityEntry? = nil
    @State private var showDeleteConfirmation: Bool = false

    var body: some View {
        NavigationStack {
            List {
                // DASH-04: "Alle Zeit"-Section oben in der Liste
                Section("Gesamt (alle Zeit)") {
                    if let stats = activityService.allTimeStats {
                        HStack {
                            Label {
                                Text(String(format: "%.1f Stunden", stats.totalHours))
                                    .font(.system(size: 17))
                            } icon: {
                                Image(systemName: "clock.fill")
                                    .foregroundStyle(Color(UIColor.systemBlue))
                            }
                            Spacer()
                            Text(String(format: "%.0f Punkte", stats.totalPoints))
                                .font(.system(size: 17, weight: .semibold))
                                .foregroundStyle(Color(UIColor.systemBlue))
                        }
                    } else {
                        HStack {
                            ProgressView()
                                .scaleEffect(0.8)
                            Text("Lade Statistiken...")
                                .font(.system(size: 13))
                                .foregroundStyle(.secondary)
                        }
                    }
                }

                // DASH-05: family-weiter Tages-Feed (groupedByDay aus todayEntries — alle Familienmitglieder)
                if activityService.todayEntries.isEmpty {
                    Section {
                        // Leerstand (UI-SPEC Screen 3 Empty State)
                        VStack(spacing: 16) {
                            Image(systemName: "tray")
                                .font(.system(size: 48))
                                .foregroundStyle(.secondary)
                            Text("Noch keine Eintraege")
                                .font(.system(size: 22, weight: .semibold))
                            Text("Aktivitaeten erscheinen hier, sobald du deinen ersten Eintrag erfasst hast.")
                                .font(.system(size: 17))
                                .foregroundStyle(.secondary)
                                .multilineTextAlignment(.center)
                        }
                        .frame(maxWidth: .infinity)
                        .padding(.vertical, 32)
                    }
                } else {
                    ForEach(groupedByDay, id: \.date) { group in
                        Section(header: Text(group.dateLabel)) {
                            ForEach(group.entries) { entry in
                                ActivityRowView(
                                    entry: entry,
                                    category: category(for: entry)
                                )
                                // .swipeActions statt .onDelete (Pitfall 5 aus RESEARCH.md)
                                .swipeActions(edge: .trailing, allowsFullSwipe: false) {
                                    Button(role: .destructive) {
                                        entryToDelete = entry
                                        showDeleteConfirmation = true
                                    } label: {
                                        Label("Loeschen", systemImage: "trash")
                                    }
                                }
                            }
                        }
                    }
                }
            }
            .listStyle(.insetGrouped)
            .navigationTitle("Verlauf")
            .navigationBarTitleDisplayMode(.large)
            .task {
                await loadData()
            }
            // Delete Confirmation (UI-SPEC Copywriting Contract)
            .confirmationDialog("Eintrag loeschen",
                                isPresented: $showDeleteConfirmation,
                                titleVisibility: .visible) {
                Button("Loeschen", role: .destructive) {
                    if let entry = entryToDelete {
                        Task { try? await activityService.deleteActivity(id: entry.id) }
                    }
                }
                Button("Abbrechen", role: .cancel) {}
            } message: {
                Text("Dieser Eintrag wird dauerhaft geloescht.")
            }
        }
    }

    // MARK: - Helpers

    private func category(for entry: ActivityEntry) -> CategoryConfig? {
        activityService.categories.first(where: { $0.id == entry.categoryId })
    }

    // Eintraege nach Tag gruppieren, absteigend sortiert — alle Familienmitglieder (DASH-05)
    private var groupedByDay: [DayGroup] {
        let sorted = activityService.todayEntries.sorted { $0.loggedAt > $1.loggedAt }
        let grouped = Dictionary(grouping: sorted) { entry in
            Calendar.current.startOfDay(for: entry.loggedAt)
        }
        return grouped.keys.sorted(by: >).map { date in
            DayGroup(
                date: date,
                dateLabel: date.formatted(.dateTime.weekday(.wide).day().month(.wide)),
                entries: grouped[date] ?? []
            )
        }
    }

    private func loadData() async {
        do {
            try await activityService.fetchTodayData()
            try await activityService.fetchAllTimeStats()  // DASH-04
        } catch {
            // Fehler bereits in activityService.activityError gesetzt
        }
    }
}

// MARK: - Helper Struct

private struct DayGroup {
    let date: Date
    let dateLabel: String
    let entries: [ActivityEntry]
}

#Preview {
    ActivityListView()
        .environmentObject(ActivityService())
}
```
  </action>
  <verify>
    <automated>xcodebuild build -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "error:|BUILD SUCCEEDED|BUILD FAILED" | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep "swipeActions.*trailing" FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift` gibt Treffer
    - `grep "confirmationDialog" FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift` gibt Treffer
    - `grep "Noch keine Eintraege" FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift` gibt Treffer (UI-SPEC Copywriting)
    - `grep "Dieser Eintrag wird dauerhaft geloescht" FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift` gibt Treffer
    - `grep "onDelete" FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift` gibt KEINEN Treffer (Pitfall 5 — .swipeActions statt .onDelete)
    - `grep "ActivityRowView" FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift` gibt Treffer
    - `grep "allTimeStats\|fetchAllTimeStats\|Gesamt.*alle Zeit" FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift` gibt Treffer (DASH-04)
    - `grep "sfSymbol\|ringColor" FamilyScore/FamilyScore/Views/ActivityLog/ActivityRowView.swift` gibt Treffer
    - `grep "accessibilityElement.*ignore\|accessibilityLabel" FamilyScore/FamilyScore/Views/ActivityLog/ActivityRowView.swift` gibt Treffer
    - xcodebuild ergibt "BUILD SUCCEEDED"
  </acceptance_criteria>
  <done>ActivityListView und ActivityRowView kompilieren. 'Alle Zeit'-Section zeigt AllTimeStats (DASH-04). Swipe-to-Delete mit confirmationDialog korrekt. Leerstand zeigt tray-Icon und UI-SPEC-Texte.</done>
</task>

<task type="auto">
  <name>Task 2: App-Verdrahtung — FamilyScoreApp + RootView TabView + currentFamilyId-Injection + Test-Suite gruen</name>
  <files>
    FamilyScore/FamilyScore/FamilyScoreApp.swift
    FamilyScore/FamilyScore/Views/RootView.swift
  </files>
  <read_first>
    - FamilyScore/FamilyScore/FamilyScoreApp.swift (aktuelle Datei VOR Aenderung — @StateObject authService, .task Pattern)
    - FamilyScore/FamilyScore/Views/RootView.swift (aktuelle Datei VOR Aenderung — AppState-Switch, OnboardingPlaceholder)
    - FamilyScore/FamilyScore/Models/AppState.swift (AppState enum — authenticated(hasFamily:) case)
    - FamilyScore/FamilyScore/Services/FamilyService.swift (currentFamily Property — Quelle der family_id)
    - .planning/phases/04-activity-logging-dashboard/04-PATTERNS.md (Abschnitt DashboardView — EnvironmentObject-Injection)
    - .planning/STATE.md (Entscheidung aus Wave 0: activityService wird in FamilyScoreApp gestartet, NICHT in RootView)
  </read_first>
  <action>
**FamilyScoreApp.swift:** Ergaenze @StateObject activityService und injiziere als .environmentObject. Bestehende authService-Logik und .task darf NICHT veraendert werden (Pitfall: zwei startObserving()-Loops vermeiden).

Aenderungen minimal halten — nur Zeilen ergaenzen, keine bestehenden Zeilen entfernen oder umschreiben:

```swift
// Ergaenzungen in FamilyScoreApp.swift:
// 1. Nach @StateObject private var authService = AuthService():
@StateObject private var activityService = ActivityService()

// 2. In WindowGroup body, nach .environmentObject(authService):
.environmentObject(activityService)
// Reihenfolge: .environmentObject(authService) dann .environmentObject(activityService)
// .task {} fuer authService.startObserving() bleibt unveraendert
```

**RootView.swift:** Ersetze den `authenticated(hasFamily: true)`-Zweig durch ein TabView mit zwei Tabs. Setze dabei `activityService.currentFamilyId` wenn die FamilyID bekannt ist.

**CRITICAL — currentFamilyId-Injection (alle DB-Calls haengen davon ab):**

FamilyService (Phase 3) stellt `currentFamily` bereit. Die family_id muss an ActivityService weitergeleitet werden, BEVOR fetchTodayData() aufgerufen wird. Zwei moegliche Injection-Stellen — waehle die passende nach Lesen der aktuellen RootView und FamilyService:

```swift
// Option A (bevorzugt): In RootView im .onAppear oder .onChange des authenticated-States:
case .authenticated(let hasFamily) where hasFamily:
    TabView {
        DashboardView()
            .tabItem { Label("Uebersicht", systemImage: "house.fill") }
        ActivityListView()
            .tabItem { Label("Verlauf", systemImage: "list.bullet") }
    }
    .onAppear {
        // currentFamilyId aus FamilyService setzen
        // FamilyService muss als @EnvironmentObject verfuegbar sein
        // ODER: familyId direkt aus AppState auslesen falls AppState sie traegt
        if let family = familyService.currentFamily {
            activityService.currentFamilyId = family.id
        }
    }

// Option B (falls FamilyService currentFamily nach AppState-Wechsel async laedt):
// .task { activityService.currentFamilyId = await familyService.loadCurrentFamily() }

// Option C (falls AppState authenticated(familyId:) case die ID traegt):
// activityService.currentFamilyId = familyId  // direkt aus AppState-Enum-Case
```

Lese FamilyService.swift und AppState.swift VOR dem Schreiben um zu bestimmen welche Option anwendbar ist. Dokumentiere die getroffene Wahl in einem Kommentar in RootView.swift.

Lese zuerst die aktuelle RootView.swift vollstaendig, dann passe nur den authenticated-Zweig an. Nicht-authentifizierte Zweige (unauthenticated → AuthFlowView, loading → ProgressView, authenticated(hasFamily: false) → FamilyOnboardingPlaceholder) unveraendert lassen.

**Minimales Beispiel des authenticated-Zweigs:**
```swift
case .authenticated(let hasFamily) where hasFamily:
    TabView {
        DashboardView()
            .tabItem { Label("Uebersicht", systemImage: "house.fill") }
        ActivityListView()
            .tabItem { Label("Verlauf", systemImage: "list.bullet") }
    }
    // currentFamilyId-Injection: Option A/B/C per oben — Kommentar warum
```

**Nach beiden Datei-Aenderungen:**
Fuehre die vollstaendige Test-Suite aus und pruefe ob alle Tests gruen sind:
`xcodebuild test -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16'`

Falls Tests rot sind: Fehler analysieren und beheben. Haeufige Ursachen:
- MockActivityService conformiert nicht vollstaendig zu ActivityServiceProtocol (fehlende fetchAllTimeStats-Methode — muss im Wave-0-Mock vorhanden sein)
- Modell-Struct-Initialisierung in Tests schlaegt fehl (fehlende Properties)
- Kompilierungsfehler durch falsche Imports
  </action>
  <verify>
    <automated>xcodebuild test -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16' 2>&1 | grep -E "Test.*passed|Test.*failed|TEST SUCCEEDED|TEST FAILED" | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep "@StateObject private var activityService" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer
    - `grep "environmentObject(activityService)" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer
    - `grep "task.*authService.startObserving" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer (unveraendert — kein zweites .task fuer activityService)
    - `grep "TabView" FamilyScore/FamilyScore/Views/RootView.swift` gibt Treffer
    - `grep "DashboardView()" FamilyScore/FamilyScore/Views/RootView.swift` gibt Treffer
    - `grep "ActivityListView()" FamilyScore/FamilyScore/Views/RootView.swift` gibt Treffer
    - `grep "currentFamilyId" FamilyScore/FamilyScore/Views/RootView.swift` gibt Treffer (Injection-Stelle dokumentiert)
    - xcodebuild test ergibt "TEST SUCCEEDED"
    - Keine roten Tests in ActivityServiceTests oder RingProgressTests
  </acceptance_criteria>
  <done>FamilyScoreApp injiziert activityService. RootView zeigt TabView mit DashboardView und ActivityListView. currentFamilyId-Injection nach Family-Load implementiert und kommentiert. Vollstaendige Test-Suite gruen.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Geraete-Checkpoint — 6 Success Criteria auf echtem iOS-Geraet</name>
  <what-built>
    Vollstaendiger Phase-4-Core-Loop: ActivityService (CRUD, Timer, Optimistic UI, fetchAllTimeStats), Ringe (DashboardView mit Wochensieger-Header und Pflicht/Freizeit-Labels), ActivityListView mit family-weitem Feed + 'Alle Zeit'-Section + Swipe-Delete, ActivityLogSheet, TabView-Integration, Supabase RPCs live.
  </what-built>
  <how-to-verify>
    App auf echtem iOS-Geraet (kein Simulator — RLS mit echtem JWT!) starten. Folgende 6 Success Criteria aus ROADMAP.md verifizieren:

    **SC-1: Aktivitaet in max 3 Taps loggen (Timer oder retroaktiv)**
    1. App starten → Tab "Uebersicht" (1 Tap auf FAB)
    2. Kategorie waehlen (Tap 2)
    3. "Speichern" tippen (Tap 3)
    Erwartet: Eintrag erscheint in Verlauf; Ringe aktualisieren sich animiert.

    **SC-2: Eintrag fuer Kind-Profil erstellen**
    Voraussetzung: Phase 3 abgeschlossen + Kind-Profil existiert in DB.
    1. Kind-Profil in ActivityLogSheet auswaehlen (falls UI vorhanden) oder via `select insert_default_categories('<family-id>')` in Supabase pruefen
    Erwartet: Eintrag erscheint unter Kind-Daten.
    *Hinweis: Falls Kind-UI nicht in Phase 4 implementiert — SC-2 defer bis Phase 3 klar.*

    **SC-3: Eigenen Eintrag loeschen; Admin loescht fremden Eintrag**
    1. Als normaler User: eigenen Eintrag in Verlauf nach links wischen → Loeschen → Bestaetigen
    Erwartet: Eintrag verschwindet, Ring aktualisiert.
    2. Als normaler User: fremden Eintrag loeschen versuchen → RLS muss ablehnen (Fehlermeldung oder kein Loeschen)
    3. Als Admin: fremden Eintrag loeschen → OK.

    **SC-4: Ringe spiegeln heutige Aktivitaeten (Pflicht/Freizeit/Score)**
    Nach dem Loggen: Ringe haben sichtbaren Fortschritt. Werte stimmen mit Supabase ueberein:
    ```sql
    SELECT * FROM get_today_score('<user-id>');
    ```

    **SC-5: Familienvergleich zeigt alle Mitglieder mit heutiger Bilanz**
    In DashboardView: Familien-Mitglieder-Liste sichtbar mit "Pflicht: Npt · Freizeit: Npt" (DASH-02).

    **SC-6: Wochenbilanz zeigt alle Mitglieder + Wochensieger**
    WeekSummaryView: "Wochensieger: [Name]" sichtbar ueber dem BarChart (DASH-03).
    weekly_summaries in Supabase nach Activity-Log pruefen:
    ```sql
    SELECT * FROM weekly_summaries WHERE family_id = '<family-id>';
    ```
    Muss aktualisierte Werte zeigen (Trigger aus Phase 1 aktiv).

    **Zusaetzlich: Verlauf-Tab pruefen**
    - "Alle Zeit"-Section zeigt korrekte Stunden/Punkte (DASH-04)
    - Alle Familienmitglieder-Eintraege von heute sichtbar (DASH-05)
  </how-to-verify>
  <resume-signal>Tippe "Phase 4 verifiziert" wenn alle 6 Success Criteria bestanden. Beschreibe Abweichungen falls vorhanden.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|-------------|
| ActivityListView → deleteActivity() | UI uebergibt nur entry.id; kein user_id-Parameter |
| RootView → TabView | Nur authenticated-Zustand zeigt Tabs; unauthenticated → AuthFlowView |
| RootView → activityService.currentFamilyId | currentFamilyId kommt aus FamilyService (auth. Session) — kein User-Input |

## STRIDE Threat Register

| Threat ID | Kategorie | Komponente | Disposition | Mitigationsplan |
|-----------|-----------|------------|-------------|-----------------|
| T-4-02 | Tampering | ActivityListView Swipe-Delete fremder Eintrag | accept | RLS-Policy `user_id = auth.uid()` ODER `is_family_admin()` auf DELETE in DB. Swift-seitig kein Role-Check noetig. Geraete-Checkpoint prueft SC-3 (echter JWT). |
| T-4-04 | Tampering | TabView nur fuer authenticated User | mitigate | RootView zeigt TabView nur bei `.authenticated(hasFamily: true)`; unauthenticated → AuthFlowView |
| T-4-06 | Information Disclosure | allTimeStats zeigt Familien-Gesamt-Daten | accept | allTimeStats wird mit family_id = currentFamilyId aus auth. Session gefiltert; RLS auf activity_entries aktiv; kein Cross-Family-Leak moeglich |
</threat_model>

<verification>
Nach Abschluss aller Tasks:

1. `xcodebuild test -scheme FamilyScore -destination 'platform=iOS Simulator,name=iPhone 16'` — TEST SUCCEEDED
2. `grep "@StateObject private var activityService" FamilyScore/FamilyScore/FamilyScoreApp.swift` gibt Treffer
3. `grep "currentFamilyId" FamilyScore/FamilyScore/Views/RootView.swift` gibt Treffer (Injection dokumentiert)
4. `grep "TabView" FamilyScore/FamilyScore/Views/RootView.swift` gibt Treffer
5. `grep "swipeActions" FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift` gibt Treffer
6. `grep "onDelete" FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift` gibt KEINEN Treffer
7. `grep "allTimeStats\|fetchAllTimeStats" FamilyScore/FamilyScore/Views/ActivityLog/ActivityListView.swift` gibt Treffer (DASH-04)
8. Geraete-Checkpoint: 6 SC manuell bestaetigt
</verification>

<success_criteria>
- ActivityListView mit family-weitem Feed (DASH-05) und 'Alle Zeit'-Section (DASH-04) implementiert
- Swipe-to-Delete mit confirmationDialog (kein .onDelete) implementiert
- FamilyScoreApp injiziert @StateObject activityService als .environmentObject
- RootView zeigt TabView (DashboardView + ActivityListView) fuer authenticated(hasFamily: true)
- RootView setzt activityService.currentFamilyId nach Family-Load (dokumentiert per Kommentar)
- xcodebuild test: TEST SUCCEEDED (alle ActivityServiceTests + RingProgressTests gruen)
- Geraete-Checkpoint: SC-1 bis SC-6 aus ROADMAP.md bestaetigt (SC-2 ggf. defer bis Phase-3-Kind-Architektur klar)
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/04-activity-logging-dashboard/04-04-SUMMARY.md` mit:
- xcodebuild test Ergebnis (Anzahl Tests, Failures)
- Geraete-Checkpoint Ergebnis (SC-1 bis SC-6: bestanden/nicht-bestanden/deferred)
- Etwaige Abweichungen von UI-SPEC oder RESEARCH.md
- Offene Punkte (falls SC-2 deferred wegen Kind-Architektur)
- ROADMAP.md Update: Phase 4 Status auf "Complete" wenn alle SC bestanden

Aktualisiere STATE.md:
- Current focus: Phase 4 — Activity Logging & Dashboard (completed)
- Progress zu Phase 5
- Decisions: alle Phase-4-Architekturentscheidungen eintragen
</output>
